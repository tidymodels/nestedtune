# AC2: an object with no method is refused, naming both that answer

    Code
      extract_procedure(1:3)
    Condition
      Error in `extract_procedure()`:
      ! `extract_procedure()` has no method for an integer vector.
      i It answers for a <nested_results> object, from `nested_tune_grid()` or one of its siblings, or a <nested_final_fit> object, from `nested_final_fit()`.

