#' @section Differences from calling <%= PKG %> directly:
#' **Refused: none.** No slot is refused on its own. What is refused at
#' entry is a control of another class, such as a `<%= OTHER %>` that
#' <%= PKG %> itself accepts here, and the `event_level` conflict above.
