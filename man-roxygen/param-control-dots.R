#' @param ... On the generic, the arguments of the method that `object`
#'   selects. On a method, a control object from [<%= CONSTRUCTOR %>], as
#'   `control`, and nothing else. Every argument after `...` is matched by
#'   name. The section on differences from <%= PKG %> says what becomes of
#'   each slot.
