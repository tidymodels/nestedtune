#' @section Differences from calling finetune directly:
#' This classification was read on finetune 1.3.0. The version that added
#' `workflow_size` to `<%= VERSION_CTRL %>` is not named in finetune's NEWS,
#' and the `>= 1.0.1` floor this package declares does not require it.
