# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-09-05 (M63 done via PR #73, merged on green at 984e12d after one ceiling stop with the held-local commits carried here; the published article read for its executed pool output; validate green with the references advisories; ROADMAP and LESSONS under budget; the M17, M09 and M28 lessons trimmed to fit an M63 line on ANSI codes and warning handlers in knitted output; M58's row pruned to keep five terminal rows; no candidate row, no known issue, nothing graduated.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M60 | The guide splits into a getting-started path and a page on what the estimate means, and the citation guard sweeps every page under vignettes/ | done | — | normal | milestones/archive/M60-guide-split.md |
| M61 | A vignette runs the four alternative inner tuners on one design and shows what each records | done | M60 | normal | milestones/archive/M61-tuner-vignette.md |
| M62 | A vignette reads the results object: summary, agreement, both plots, a failed fold, dplyr and the survival arguments | done | M60 | normal | milestones/archive/M62-results-vignette.md |
| M63 | A site-only article runs the outer loop on mirai daemons and shows the result identical to the serial run | done | M60 | normal | milestones/archive/M63-parallel-article.md |
| M64 | A site-only article repeats a null-data simulation showing tuned-CV optimism and the nested estimate removing it | review | M60 | normal | milestones/M064-why-nest-article.md |
| M59 | Every driver refuses a design whose inner splits are not rsplits, do not share the outer fold's frame or its analysis set, or index rows the outer fold holds out | done | — | normal | milestones/archive/M59-inner-split-frame-checks.md |
<!-- rows grouped by status, not sorted by ID; keep only the 5 most recent terminal (done/dropped) rows — older ones live in milestones/archive/ + git -->

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- What M56 and M57 left of the M52, M51, M37 and M34 review rows — added 2026-09-03 at M56/M57's plan gate, which absorbed the other seventeen findings. Kept because each buys nothing on inspection or lost at the gate: a `HangTraceReporter` occurrence counter for duplicate descriptions and a disk-backed fixture cache shared across workers (M52 F1, F4 structural halves); a finetune floor bump to the version that added `workflow_size`, unnamed in finetune's NEWS and a dependency re-pin (M51 O-8; D-045); re-templating `vec_cast.nested_results.nested_results()` on `to`, which the plan audit found would stamp the destination's run description onto rows it did not produce (IP4), and `rbind.nested_results()` on its first results argument, where a bare first argument already yields a bare table (M37 R6, R9); a top-level `param_info` attribute, which already rides inside the `procedure` record (M34 F7); hardening the `collect_metrics(` source scan M57 removes (M34 F6); and, learned at M57 T3, a fixture-cache report that reaches a log under parallel files — a worker's teardown output on either stream reaches nothing in the parent, so the report prints only serially. Promote on a hang the trace misattributes, on the next finetune re-pin, on a user reaching a cast with the wrong id columns, or on something needing `param_info` off the results object. Dispositioned 2026-09-03 at the status audit (records-hygiene §7): extended once more by explicit choice; the next extension takes a promote, Known-issues or prune disposition
- The pkgdown workflow external PR #30 collapsed, and the three M17 findings that came back with it — added 2026-08-31 — M35 review P1–P3. `72c3be2` made `.github/workflows/pkgdown.yaml` one job that both executes the ref's vignette and `@examples` code and holds `contents: write` (F2), and restored the `any::pkgdown` line D-022 rejected (F1). `topepo` left the question open on the PR hunk and it is unanswered. Whatever answers this row re-points the published-site and CI-records rows below
- Fixture-family and closure-binding coverage the derived fixture-key test does not carry — added 2026-09-01 — M42 review O4/S1/S2/S3/R3 and O5/R2. The per-formal axis test keys no real fixture family pairwise and no two recipe-based workflows apart, and its attribute and environment clauses are exercised by nothing; the depth guard covers only the sorted arguments, with `canonical_form(fn)` and the bindings hashed unguarded. Promote on a fixture-key collision between two real fixtures, or when a family's sorted-argument depth approaches the cut (30 of 40 today)
- The tidymodels conventions M32 and M33 deliberately leave behind — added 2026-08-28; address (M30), community files, site template, lifecycle badge (M32) and shared CI workflows (M33) shipped 2026-08-30; `README.Rmd` and `R-CMD-check-hard.yaml` arrived in `72c3be2` — D-025, D-026. **Posit attribution:** siblings' `DESCRIPTION` and LICENSE name Posit as copyright holder and funder, where D-025 retains Jeffrey Girard as owner. **Site analytics:** all nine siblings carry a Plausible `in_header` block. Promote either on an answer from the tidymodels maintainers — the plan-gate answer was to ask, not adopt or refuse. **`.github/CODEOWNERS`:** promote once understudies are assigned here. CRAN and downloads badges belong to the first release
- Route fold data to daemons through `mori`'s shared memory instead of by value, keeping the by-value path as the remote fallback and the one-frame-per-fold invariant gate regardless — added 2026-07-31 — M26 Out; `references/mori-backend-assessment.md` (P1-P9) owns the derivations over `benchmarks/mori-wire-manifest.json`: ~941.7 kB / 1 copy per fold today against a modelled ~103.1 kB / 0 copies (factor 9.13, a gap of ~838.6 kB; installed, the worker closure is 524 B and near-negligible), fold records `identical()` to serial. Adoption deletes only the blanking and rehydration in `R/parallel.R` and must replace `is_fold_payload()`'s fat-path gate; mori is same-machine and needs `R (>= 4.3)`; it is a dependency change needing its own gate and D-entry. Absorbs 'Preload the data into each daemon once per run with `mirai::everywhere()`', added 2026-07-30 — M23 Out: one copy per daemon rather than per fold, declined because the user's pool would hold state across runs. Promote on evidence that per-fold transfer is a real cost on a user's run, or on tune adopting mori upstream (tune#1188)
- Let the metric that selects differ from the metric that scores, instead of one `metrics` argument driving both loops — added 2026-07-31 — `references/stone1974a.md`, §2 item VII, owns the quotation and Table 1 figures. One `metrics` reaches the inner tune call and `last_fit()` alike, so the inner loop always selects under the metric the outer loop reports; Stone measures cases where matching is worse. Not a defect: coupling is the one obvious path GP3 prefers and matches tune's idiom; decoupling needs a second argument and a story for `collect_metrics()` and `.selected`. Promote on evidence a real workflow selects better under a different metric than it reports, or on a user asking for it
- Detect daemons running different code that still carries the same symbols — a build hash or fingerprint rather than symbol presence — added 2026-07-30 — M24 Out. M24's capability probe asks whether the symbols `dispatch_folds()` resolves by name are present, which catches the observed failure (a stale daemon with no `rehydrate_payload`) but not a daemon whose symbols all exist and whose behaviour differs; that case breaks IP2 invisibly, since the same seed would give a different answer depending on which daemon took the fold. A version comparison cannot substitute: `DESCRIPTION`'s `Version:` has read `0.0.0.9000` since M01 (`fafb31f`, the only commit touching it in 23 milestones), so a stale daemon reports the host's own string. Promote on any evidence a heterogeneous pool has produced differing results, or once the package versions releases
- Stop paying for source references on the wire — the worker closure `dispatch_folds()` sends is 203,790 B under `pkgload::load_all()` against 524 B re-parsed, because a function's srcrefs serialize with it and their presence depends on how the package was installed rather than on the run — added 2026-07-30 — M23 Out. `utils::removeSource()` at dispatch was implemented and measured at M23, then removed: `utils` is not in Imports, a dependency change needs its own gate (tracking-rules), and the gain is development-only since `install.packages()` defaults to `keep.source.pkgs = FALSE`. The same closure was equally srcref-laden as `.f` before M23, so this is not a regression M23 introduced. Promote if a dependency gate opens `utils` for another reason, or on evidence a user installs with srcrefs kept
- Generate the candidates rather than read back the ones that scored, so a candidate that failed on every inner resample is recorded too and outer folds stop searching different grids — added 2026-07-30 — M21 Out, absorbing the M20 row; M21's archive owns the measurements and the two declined shapes. A `tune_results` carries no record of its expansion, and continuous-parameter expansion is stochastic, so each outer fold searches a different menu under its own seed. Promote on any evidence a user needs the attempted-but-failed candidates, or reads the per-fold records expecting one shared grid
- A resampling specification can no longer see its own calling frame: `nested_resamples()` evaluates `outside`/`inside` in a child environment binding `.nestedtune_data`, so `parent.frame()` inside the specification is that transient environment — added 2026-07-30 — M18 review finding A2, accepted rather than fixed because binding the data to a name is what removes the frame from the error message. A spec capturing `match.call()` stores a call naming `.nestedtune_data`, unresolvable once the environment is gone. Promote on any evidence a real specification depends on its calling frame
- Index-slot shapes `check_inner_splits()` leaves to rsample — added 2026-09-04 — M59 review O5, O6, O7, O9, O15. A non-numeric `in_id` coerces with a warning and reports `NA`; a fractional index truncates and passes containment; an element-wise `NA` in `out_id` is dropped; a non-data.frame outer `$data` reports as a frame mismatch; an empty inner `in_id` passes containment (the fixture vehicle `break_inner_split()` relies on it). Promote on a user reaching one of these shapes past the entry check, or on rsample refusing one of them earlier
- Normalize cli glyphs at the daemon so BC6 compares identical text — added 2026-09-04 — M59 review O13, S1, P1. `test-parallel-identity.R`'s `unwrap()` reads a daemon's `i` and the host's `ℹ` as one, since a daemon has no UTF-8 console; setting `cli.unicode` on the daemon (or the host) in the identity fixture would let BC6 compare byte-identical notes without widening its exemption. Promote when a second glyph joins the normalization, or when BC6's exemption is next weighed
- The shared `reg_metrics()` still equals tune's regression default — added 2026-07-30, trimmed 2026-07-30 at M20's plan gate and again 2026-09-02 at M49's (M49 keeps each fold's inner metrics as `.inner_metrics`, which was this row's other half). Its original rationale is weakened now that M18's `sep_*` fixture gives the suite a metric set that separates; changing it would re-record snapshots across ~6 files and still miss the `tune_grid()` site. Promote when a second milestone needs the same metric separation from a different fixture
- Name the cause of the two pre-M14 stalls — `R CMD check` on macOS 52 min and `covr` on ubuntu 40 min, silence after `* checking tests ...`, an identical re-run passing — added 2026-07-27 (`0db6f5f`), trimmed 2026-07-27 at M16's plan gate; M12–M16 own the mitigation, localization and later occurrences. What is left predates M14's `collect_bounded()` sweep, when a bare `[` collect could block forever, so it may be unbounded in a way M16's budget does not reach; two suspects were refuted (M15/M16 lessons line). Not `/hotfix`-able. Promote on a recurrence whose duration exceeds the worst case M16's ledger establishes
- Share one mirai worker pool across `test-parallel-identity.R`'s tests instead of restarting it 8 times, each restart reloading the package into every daemon — added 2026-07-27 — M12 Out, declined at its plan gate. The file is 40.5s, 17.5% of the suite, and the pool restarts are most of it; but the tests it holds are what prove IP2 (same seed, same answer, any worker count), and leaked state across a shared pool would corrupt exactly that guarantee. Promote on evidence that a reused pool is observably clean between tests — a probe showing no state carried across a reuse, which M14's suite-end pool assertion (AC4) may supply
- Take the pre-flight deadline off the wall clock: `R/parallel.R` computes it from `Sys.time()` (the `deadline <-` lines), so a backwards system-clock step silently extends the only bound in the package — added 2026-07-27, corrected 2026-07-30 at M24's plan gate, whose audit found `proc.time()` reads the same realtime source, so M24 declined a misnamed helper. Promote on evidence the bound has overrun in the field, or once a genuinely step-immune R-level clock is established
- Cut the CI matrix on pull requests to ubuntu+windows, and cancel superseded runs on the default branch too — added 2026-07-27 — M11 Out, dropped at its gate. The repo is public, so minutes are free (`billable: 0`); the PR cut saves a median 0.0 min because windows is the critical path in 43 of 55 runs while losing R-devel and oldrel-1 coverage, and cancelling on the default branch can leave a commit with no completed check. Promote on evidence either premise has failed — the repo goes private, or windows stops being the critical path
- Keep the R-devel dependency cache alive across quiet weeks — GitHub deletes any Actions cache not accessed for 7 days, and a devel leg starting cold pays a source build of 129 packages where a warm one takes 9m28s (measured 2026-08-01) — added 2026-07-27, premise corrected 2026-08-30 at M31's plan gate (idle expiry, not the 10 GB limit) — M11 Out, M31 Out. M31 fixes the cap so a cold build completes and writes the cache; the shape left to price is a scheduled warm-up run against its recurring minutes and red surface. Promote on evidence cold devel builds recur often enough to dominate the leg's wall-clock, or if the cache total nears 10 GB
- Published-site rows, one pass over `.github/workflows/pkgdown.yaml` reaching them — added 2026-07-27, grouped 2026-09-01 at M39's cap gate, compressed 2026-09-03 after `72c3be2` (PR #30) rewrote the workflow — M17 Out, M17 review F4, F5, F6, F10, M33, D-003, D-028. **Token-holding actions on moving tags:** the deploy action is SHA-pinned (now v4.8.0, Node target unverified), while `r-lib/actions/pr-push@v2`, `posit-dev/setup-air@v1` and `reviewdog/action-suggester@v1` ride moving tags under write permissions, held at the organization's shared blobs (D-028). Promote on the organization pinning them, on the deploy annotation becoming a failure, or on evidence a moving tag under a write token was altered. **Site hygiene:** `clean: false` makes publication additive, so a page renamed away pre-1.0 (D-003) stays served, and nothing asserts repo-internal markdown stays off the site. Promote on any repo-internal markdown file added at the root or in `.github/`. **The `/dev/` site:** `mode: release` builds to the site root; promote on the first tagged release, whose site would otherwise overwrite the development pages
- Bring the CI records up to the workflows that exist — added 2026-07-27 — M17 review F9, F13; M31 review F5; M37 (RR04 recs 3 and 5). `.github/ci-usage-baseline.md:5` still names two workflows and M11's baseline numbers predate the later ones (eight on 2026-09-03); `cairn/PROFILE.md`'s test-doctrine slot copies `timeout-minutes` numbers guarded by prose only, where `.github/ci-usage.py` already re-reads the `paths-ignore` copies. RR04 also wants a check leg against development vctrs (`vec_cbind_frame_ptype.nested_results()` rests on an experimental generic, D-033) and an upstream ask to stabilize it. Promote on the next CI-economy audit, on any vctrs release note touching `vec_cbind()`, or on the vctrs compatibility test going red
- Stop sending the recipe's own retained copy of the training data to every fold — `recipes::recipe(formula, data)` keeps all training rows in `$template`, serialized once per task in `mirai_map()`'s `.args` — added 2026-07-30 — M23 Out. Measured at M23's plan gate: 847,146 B of workflow against 840,540 B of data, the largest remaining term on the wire for a recipe user. Left out as surgery on a recipes-owned object whose other readers of those rows are unestablished (GP1). Promote on evidence that no recipes code path reads `template` for its rows, or on a user report of dispatch cost dominated by a recipe
- Probe remote mirai daemon pools, which nothing verifies today — RR03 Q5 established the load requirement by execution only for local daemons and marked the remote case *inferred* from the same mechanism, so a remote host missing the package still surfaces as opaque per-fold worker failures — added 2026-07-26 — M10 Out, carried over from the absorbed pre-flight rows; needs a remote host to test against before it is plannable. M58 (planned 2026-09-04) makes the local pre-flight ask each daemon for the workflow's and the tuner's packages, the second question this row asks of a remote pool. **M26 records the consequence of `mori` being same-machine-only, which bears directly on whether a remote pool stays a supported shape**
- Variance estimation / inference on the nested estimate — added 2026-07-25 — G6; the caveat half went to M25, only the interval half remains. The shelf's twelve pages in `references/` (bates2023 through gauran2025) own the numbers; this row carries the synthesis: **four blockers a plan must clear.** (1) Loss stability for a tune-then-fit procedure is unestablished and the shelf splits the notion five ways; luo2025 shows the closest one uncertifiable from data, bayle2026 that it fails for comparing two stable algorithms, so nothing here may test "workflow A beats workflow B". (2) The valid interval's estimand is the k-fold test error of the rules this run fitted, not the final model's. (3) The estimators need per-observation losses, and the orchestrators keep only `collect_metrics(fitted)` per outer fold, discarding `.predictions`. (4) luo2026 bounds the power of any assumption-free valid black-box risk test, and v-fold nesting runs at the ratio where such an interval cannot exist. Promote only on all four blockers clearing
- Re-cut the nested-CV vignette onto a p > n example where nesting demonstrably earns its cost — added 2026-07-31 — M25 Out. M25 corrects `vignettes/nested-cv.Rmd:81-84`'s claim that `mtcars` is where the optimism is largest rather than changing the example, because `references/tibshirani2009.md` (bias material only at p >> n), `references/vabalas2019a.md` (feature-to-sample ratio predicts overfitting; mtcars sits at ~0.31, the low end of its 1/3-20 sweep) and `references/wilimitis2023.md` (a measured null at n >> p with a small grid) put the worked example in the regime where nesting buys least. A wide example would make the claim true instead of corrected, but M06 built every prose number as inline `r` over mtcars plus a 6-point ranger grid, so the re-cut invalidates both autoplot figures, the write-up template and the runtime budget
- An analytic oracle for the outer loop via `parsnip::null_model()` — added 2026-07-31 — from `references/nachum2026.md`. Theorem 4.10 plus Lemma 4.9 give an exact closed form for the k-fold CV MSE of the majority algorithm under Bernoulli(1/2) labels: MSE = ((k-1)/k)*Cov(n,m) + 1/(4n) with Cov(n,m) = 2^-n * sum_j C(m-1,j)^2 * C(n-2m, floor((n-m)/2) - j). `parsnip::null_model(mode = "classification")` instantiates that algorithm and carries no tuning parameters (verified present 2026-07-31). Would be the shelf's first GP2 type (3) oracle for the outer loop. Two unmeasured risks before it is plannable: `null_model()`'s tie-breaking at Y = n/2 is unchecked against the theorem's `Y > n/2` rule, and MSE is a distributional property, so the replicate count must be priced against GP4 the way `references/varma2006.md`'s null-data invariant already was
- A synthesis note reconciling the shelf's five stability notions — added 2026-07-31 — hypothesis and loss and squared-loss (`references/nachum2026.md` Definitions 2.1-2.3), prediction-based (eps, delta) (`references/luo2025.md`), and *relative* loss stability, a ratio rather than a bound (`references/bayle2026.md`). They are not interchangeable and results proved under one do not transfer to another, but the G6 row above still says "loss stability" as though it named one condition. `references/` owns synthesis notes; this is what stops a future G6 plan conflating them
- Document IP2's enforceable scope in DESIGN.md — it binds only randomness flowing through R's RNG, so engines that bypass it (kernlab SVM, keras/torch) are unreachable by any R-side scheme — added 2026-07-25 — RR01 B4; amending IP text needs a D-entry
- What `...` still does not carry after M48: the Gaussian-process fitter's options (`corr`, `nug_thres`, `maxit`, `optim_start`, which `tune_bayes()` forwards from its own `...`) and the outer-loop `control` topepo reserved the name for in [#33](https://github.com/tidymodels/nestedtune/issues/33) — added 2026-09-02 — M48 Out, D-042. Promote on a user needing a GP option, or on a first outer-loop setting that is not its own argument
- An `autoplot()` view of each fold's inner search trajectory over `.inner_metrics` — the best-so-far by `.iter` for a Bayesian run, which is what [#57](https://github.com/tidymodels/nestedtune/issues/57) wants the column for — added 2026-09-02 — M49 Out, D-019. Promote once M49 ships and a user asks for the plot rather than the table. Extended 2026-09-02 at M50/M51's plan gate: a racing view (the per-resample elimination order `finetune::plot_race()` draws is not kept by the fold record, M50 Out) and an annealing trajectory (M51 Out) join it
<!-- drift-check: lean_bundle_bytes=941.7 kB; mori_bundle_bytes=103.1 kB; ratio_lean_over_mori=9.13; gap_bytes=838.6 kB; worker_closure_bytes=524 B@2 -->
