# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-09-11 (twelfth pass, M086 merged: M086 archived and its row set done, M83's row pruned, eight candidate rows compressed for the byte budget, the #91 row updated, one LESSONS line added, the M81 line corrected; both files within caps.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M86 | The guides and README pass the plain-English sweep | done | — | high | milestones/archive/M086-guides-plain-sweep.md |
| M87 | The help pages pass the plain-English sweep | review | M86 | high | milestones/M087-help-pages-plain-sweep.md |
| M88 | Prose that fails the sweep cannot merge | planned | M86, M87 | normal | milestones/M088-prose-sweep-gate.md |
| M84 | The guides and README read on one pass for a tune_grid user | done | — | high | milestones/archive/M084-guides-plain-prose.md |
| M85 | The help pages read on one pass for a tune_grid user | done | M84 | high | milestones/archive/M085-help-pages-plain-prose.md |
<!-- rows grouped by status, not sorted by ID; keep only the 3 most recent terminal (done/dropped) rows — older ones live in milestones/archive/ + git -->

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- What #91 still asks for after M84-M87: `@param` entries inherited from tune's own pages with the nested qualifications in Details, the guides' use of "you" (M82 dropped its cap; the SimpleEnglish rule names the actor), and roxygen templates for text repeated across pages, and the 49 passive-voice and -ing-verb entries of M86's pass-1 reader report (`cairn/surveys/M086-reader-review.md`, rules 3.6 and 3.5) — added 2026-09-11 at M86/M87's plan gate, extended at M86's review gate ([#91](https://github.com/tidymodels/nestedtune/issues/91) stays open; M86's merge posted the one comment naming the guides half and this row). Promote on the issue author's PR offer landing, or on the next help-page milestone
- The workflow identity does not read case weights, a postprocessor, or what a function-valued step setting closes over, so a workflow differing there alone passes the final fit's check — added 2026-09-11 at M83's gates (`milestones/archive/M083-final-fit-workflow-identity.md`). Recording the first two reads `pre$actions$case_weights$col` and `post$actions$tailor`, tailor in Suggests. Promote on a user fitting with any of the three, or on the next milestone touching `R/workflow-identity.R`
- A panel-rate readability pass rule: three fresh readers scoring every paragraph on a closed (a)/(b)/(c) rubric, the reference intro scored blind as a control, pass at most n paragraphs flagged by two of three — added 2026-09-11 at M84's RR07 ingest (`milestones/archive/` will hold RR07; recommendation 5, consider). RR07 shows an open-list empty-reader criterion is undecidable and that this shape needs one calibration run to set n. Promote when a prose milestone wants a pass rule on a reader rather than a triaged report, and can spend the calibration run
- `benchmarks/sweep-prose.R` counts list-item continuation lines and README badge lines as prose, and an indented fence or a multi-line HTML comment would leak in — added 2026-09-11 at M84's review gate (Q1, Q2, P6, O11-O13 in M84's archive summary). `--paragraphs` hands the reader README fragments and an over-cap list item is uncatchable; the script follows M84 AC1's prose definition, so the fix starts at that definition. Promote at the next prose milestone that reuses the script
- `NEWS.md`'s 77 development bullets read to the M84 plain-prose standard — added 2026-09-10 at M84's plan gate — M84 Out. The bullets are consolidated at the first release, so the pass belongs to that release walk's NEWS step. Promote only if a release is declared before then and the walk keeps the bullets as they are
- M079's review leftovers — added 2026-09-10 at M079's review gate (`milestones/archive/M079-review-remainders.md`; findings 5, 8, 9). (1) `test-parallel-interrupt.R:108` fails inside the parallel suite and passes alone ("Expected `interrupted` to be TRUE", byte-identical on `b07beb3` and M079's head), a worker timing interaction that came and went across four full runs. (2) AC2's block drives `name_by_pid()` rather than `daemon_state_snapshot()`; the restored `seed = 999L` run is live and un-memoised, seconds unrecorded; `test-suite-hygiene.R` carries a block reading no source. Promote when (1) costs a review a re-run, on a parallel-suite change that prices it, on the snapshot's naming regressing unseen, or on suite time being priced again
- Coverage M75's pruning removed — added 2026-09-08 at M75's review gate (F1-F4; `milestones/archive/M75-suite-prune.md` names each deletion). Nothing now checks author-year citations against the `cairn/` shelf or the mori page's wire figures against `benchmarks/mori-wire-manifest.json`; every `.predictions`/`.extracts` assertion passes a `tune::control_grid()`, so the Bayesian, racing and annealing paths are covered only through `nested_fold_fit()` and `test-control-slots.R`; the fixture cache report's stream split is unasserted. Promote on a drifted citation or figure reaching the default branch, or on an upstream control dropping `save_pred` or `extract`
- The windows leg's last 1.3 minutes, and the `test-coverage` leg — re-cut 2026-09-08 at M76, compressed through M079 and M086; `milestones/archive/M076-ci-leg-speed.md` owns the figures, run ids, cap changes and the rebuild's two shapes; M76 priced both test-suite levers and reverted them, leaving only the check-time vignette rebuild. The coverage leg's one failure is `readRDS(f)` inside `covr::merge_coverage` near its 20-minute cap, hit by M74's and M75's branches, M83's and M86's PRs and main after M85; the profile declares the leg non-gating. Promote on the rebuild being wanted for itself, on the coverage leg's failure rate rising or its cap being hit, or on windows crossing 30
- What M56 and M57 left of the M52, M51, M37 and M34 review rows — added 2026-09-03 at M56/M57's plan gate, which absorbed the other seventeen. Eight items, named in the M56/M57 files: a duplicate-description counter and a shared fixture cache (M52 F1, F4); a finetune floor bump (M51 O-8; D-045); two re-templatings (M37 R6, R9); a top-level `param_info` (M34 F7); hardening a source scan M57 removes (M34 F6); a fixture-cache report under parallel files. Promote on a hang the trace misattributes, the next finetune re-pin, a cast reached with the wrong id columns, or `param_info` needed off the results object. Dispositioned 2026-09-03: the next extension takes a promote, Known-issues or prune disposition
- Fixture-family and closure-binding coverage the derived fixture-key test does not carry — added 2026-09-01 — M42 review O4/S1/S2/S3/R3 and O5/R2. The per-formal axis test keys no real fixture family pairwise and no two recipe-based workflows apart; its attribute and environment clauses are exercised by nothing; the depth guard covers only the sorted arguments, `canonical_form(fn)` and the bindings hashed unguarded. Promote on a fixture-key collision between two real fixtures, or when a family's sorted-argument depth approaches the cut (30 of 40 today)
- The tidymodels conventions M32 and M33 deliberately leave behind — added 2026-08-28 — D-025, D-026; community files, site template, lifecycle badge and shared CI workflows shipped 2026-08-30, and `README.Rmd`, `R-CMD-check-hard.yaml` and the Plausible site-analytics block arrived in `72c3be2`. **Posit attribution:** siblings' `DESCRIPTION` and LICENSE name Posit as copyright holder and funder, where D-025 retains Jeffrey Girard as owner; promote on an answer from the tidymodels maintainers — the plan-gate answer was to ask, not adopt or refuse. **`.github/CODEOWNERS`:** promote once understudies are assigned here. CRAN and downloads badges belong to the first release
- Route fold data to daemons through `mori`'s shared memory instead of by value, keeping the by-value path as the remote fallback and the one-frame-per-fold gate — added 2026-07-31, compressed 2026-09-08, -09 and at M077 — M26 Out; `references/mori-backend-assessment.md` (P1-P9) owns every derivation over `benchmarks/mori-wire-manifest.json` and the adoption surface in `R/parallel.R`. mori is same-machine and needs `R (>= 4.3)`, a dependency change needing its own gate and D-entry. Absorbs the M23-Out row preloading data with `mirai::everywhere()`, declined because the pool would hold state across runs. Promote on evidence per-fold transfer is a real cost on a user's run, or on tune adopting mori (tune#1188)
- Let the metric that selects differ from the metric that scores, instead of one `metrics` argument driving both loops — added 2026-07-31 — `references/stone1974a.md` §2 item VII owns the quotation and figures. One `metrics` reaches the inner tune call and `last_fit()` alike, so the inner loop selects under the metric the outer loop reports; Stone measures cases where matching is worse. Not a defect: coupling matches tune's idiom (GP3); decoupling needs a second argument and a story for `collect_metrics()` and `.selected`. Promote on evidence a real workflow selects better under a different metric than it reports, or on a user asking for it
- Detect daemons running different code that still carries the same symbols — a build hash rather than symbol presence — added 2026-07-30 — M24 Out. M24's capability probe asks whether the symbols `dispatch_folds()` resolves by name are present, which catches the observed failure (a stale daemon with no `rehydrate_payload`) but not a daemon whose symbols all exist and whose behaviour differs; that breaks IP2 invisibly, the same seed answering differently depending on which daemon took the fold. A version comparison cannot substitute: `DESCRIPTION`'s `Version:` has read `0.0.0.9000` since M01, so a stale daemon reports the host's own string. Promote on evidence a heterogeneous pool has produced differing results, or once the package versions releases
- Stop paying for source references on the wire — the worker closure `dispatch_folds()` sends is 203,790 B under `pkgload::load_all()` against 524 B re-parsed, since srcrefs serialize with a function and depend on how the package was installed — added 2026-07-30 — M23 Out. `utils::removeSource()` at dispatch was measured at M23, then removed: `utils` is not in Imports, and the gain is development-only since `install.packages()` defaults to `keep.source.pkgs = FALSE`. Promote if a dependency gate opens `utils` for another reason, or on evidence a user installs with srcrefs kept
- Generate the candidates rather than read back the ones that scored, so a candidate that failed on every inner resample is recorded and outer folds stop searching different grids — added 2026-07-30 — M21 Out, absorbing the M20 row; M21's archive owns the measurements and the two declined shapes. A `tune_results` carries no record of its expansion, and continuous-parameter expansion is stochastic, so each fold searches its own menu. Promote on evidence a user needs the failed candidates, or reads the per-fold records expecting one shared grid
- A resampling specification can no longer see its own calling frame: `nested_resamples()` evaluates `outside`/`inside` in a child environment binding `.nestedtune_data`, so `parent.frame()` inside the specification is that transient environment — added 2026-07-30 — M18 review finding A2, accepted rather than fixed because binding the data to a name is what removes the frame from the error message. A spec capturing `match.call()` stores a call naming `.nestedtune_data`, unresolvable once the environment is gone. Promote on any evidence a real specification depends on its calling frame
- Index-slot shapes `check_inner_splits()` leaves to rsample — added 2026-09-04 — M59 review O5, O6, O7, O9, O15. A non-numeric `in_id` coerces with a warning and reports `NA`; a fractional index truncates and passes containment; an element-wise `NA` in `out_id` is dropped; a non-data.frame outer `$data` reports as a frame mismatch; an empty inner `in_id` passes containment (the fixture vehicle `break_inner_split()` relies on it). Promote on a user reaching one of these shapes past the entry check, or on rsample refusing one of them earlier
- Normalize cli glyphs at the daemon so BC6 compares identical text — added 2026-09-04 — M59 review O13, S1, P1. `test-parallel-identity.R`'s `unwrap()` reads a daemon's `i` and the host's `ℹ` as one, since a daemon has no UTF-8 console; setting `cli.unicode` on the daemon (or the host) in the identity fixture would let BC6 compare byte-identical notes without widening its exemption. Promote when a second glyph joins the normalization, or when BC6's exemption is next weighed
- The shared `reg_metrics()` still equals tune's regression default — added 2026-07-30, trimmed 2026-07-30 at M20's plan gate and again 2026-09-02 at M49's (M49 keeps each fold's inner metrics as `.inner_metrics`, which was this row's other half). Its original rationale is weakened now that M18's `sep_*` fixture gives the suite a metric set that separates; changing it would re-record snapshots across ~6 files and still miss the `tune_grid()` site. Promote when a second milestone needs the same metric separation from a different fixture
- Name the cause of the two pre-M14 stalls — `R CMD check` on macOS 52 min and `covr` on ubuntu 40 min, silence after `* checking tests ...`, an identical re-run passing — added 2026-07-27 (`0db6f5f`), trimmed 2026-07-27 at M16's plan gate; M12–M16 own the mitigation, localization and later occurrences. What is left predates M14's `collect_bounded()` sweep, when a bare `[` collect could block forever, so it may be unbounded in a way M16's budget does not reach; two suspects were refuted (M15/M16 lessons line). Not `/hotfix`-able. Promote on a recurrence whose duration exceeds the worst case M16's ledger establishes
- Take the pre-flight deadline off the wall clock: `R/parallel.R` computes it from `Sys.time()` (the `deadline <-` lines), so a backwards system-clock step silently extends the only bound in the package — added 2026-07-27, corrected 2026-07-30 at M24's plan gate, whose audit found `proc.time()` reads the same realtime source, so M24 declined a misnamed helper. Promote on evidence the bound has overrun in the field, or once a genuinely step-immune R-level clock is established
- Cut the CI matrix on pull requests to ubuntu+windows, and cancel superseded runs on the default branch too — added 2026-07-27 — M11 Out, dropped at its gate. The repo is public, so minutes are free (`billable: 0`); the PR cut saves a median 0.0 min because windows is the critical path in 43 of 55 runs while losing R-devel and oldrel-1 coverage, and cancelling on the default branch can leave a commit with no completed check. Promote on evidence either premise has failed — the repo goes private, or windows stops being the critical path
- Keep the R-devel dependency cache alive across quiet weeks — GitHub deletes any Actions cache not accessed for 7 days, and a devel leg starting cold pays a source build of 129 packages where a warm one takes 9m28s (measured 2026-08-01) — added 2026-07-27, premise corrected 2026-08-30 at M31's plan gate (idle expiry, not the 10 GB limit) — M11 Out, M31 Out. M31 fixes the cap so a cold build completes and writes the cache; the shape left to price is a scheduled warm-up run against its recurring minutes and red surface. Promote on evidence cold devel builds recur often enough to dominate the leg's wall-clock, or if the cache total nears 10 GB
- What the internal-pages guard cannot see — added 2026-09-09 at M78's review gate (findings O2, O6, O7, which M78's file and git own in full). `.github/workflows/pkgdown.yaml`'s guard matches tracked markdown by stem at the site root, which breaks three ways: on a site URL with a path segment, on untracked root markdown, and on a stem collision with pkgdown's own generated root pages. Promote on any of the three, or on a leak reaching the site
- Published-site rows left after M78 — added 2026-07-27, re-cut at M78's plan gate, compressed since — M17 Out and review F4-F6/F10, M33, D-003, D-028, D-060 carry the detail. Three items: `r-lib/actions/pr-push@v2`, `posit-dev/setup-air@v1` and `reviewdog/action-suggester@v1` ride moving tags under write permissions (D-028), promoting on the organization pinning them or on an altered tag; `clean: false` keeps a pre-1.0 renamed page served (D-003), promoting on the first release or on a stale page misleading a reader; and no `development:` block means the site builds to the root, promoting on the first release tag. Kept 2026-09-10 by explicit choice.
- `devel-vctrs.yaml` and `stress-daemon-tests.yaml` pass identical `extra-packages: any::testthat, any::pkgload` to `setup-r-dependencies@v2` — added 2026-09-10 at M080's review gate (finding F4, `milestones/archive/M080-ci-records-vctrs-watch.md`). That action saves its library cache after the job, and devel-vctrs overwrites vctrs with `r-lib/vctrs@main` after the install, so a stress run on ubuntu may restore a library holding unreleased vctrs; the key scheme is unread. Related to the R-devel cache row. Promote on the key scheme read and found to collide, or on a third workflow sharing that line
- `devel-vctrs.yaml` runs only on `push` and `pull_request`, so an upstream vctrs change waits for the next push here — added 2026-09-10 at M080's review gate (finding F1). M080's plan gate chose that over a schedule-only leg; a `schedule:` beside the two triggers was not priced. Promote on an upstream change to `vec_cbind_frame_ptype()` reaching this repository's released-vctrs legs or a user before this leg saw it
- Stop sending the recipe's own retained copy of the training data to every fold — `recipes::recipe(formula, data)` keeps all training rows in `$template`, serialized once per task in `mirai_map()`'s `.args` — added 2026-07-30 — M23 Out. Measured at M23's plan gate: 847,146 B of workflow against 840,540 B of data, the largest remaining term on the wire for a recipe user. Left out as surgery on a recipes-owned object whose other readers of those rows are unestablished (GP1). Promote on evidence that no recipes code path reads `template` for its rows, or on a user report of dispatch cost dominated by a recipe
- Probe remote mirai daemon pools, which nothing verifies today — added 2026-07-26 — M10 Out, RR03 Q5, which established the load requirement by execution for local daemons only and inferred the remote case, so a remote host missing the package still surfaces as opaque per-fold worker failures. M58 (done 2026-09-04) asks each local daemon for the workflow's and the tuner's packages, the second question this row asks of a remote pool. M26 records that `mori` is same-machine-only, which bears on whether a remote pool stays a supported shape. Promote once a remote host exists to test against
- Variance estimation / inference on the nested estimate — added 2026-07-25, compressed 2026-09-08 — G6; the caveat half went to M25. The `references/` pages own the numbers; **four blockers a plan must clear.** (1) Loss stability for a tune-then-fit procedure is unestablished and the shelf splits the notion five ways; `luo2025` shows the closest one uncertifiable from data, `bayle2026` that it fails for comparing two stable algorithms, so nothing here may compare two workflows. (2) The valid interval's estimand is the k-fold test error of the rules this run fitted, not the final model's. (3) The estimators need per-observation losses, which M68 keeps under `save_pred`. (4) `luo2026` bounds the power of any assumption-free valid black-box risk test, and v-fold nesting runs at the ratio where no such interval exists. Promote only on all four blockers clearing
- An analytic oracle for the outer loop via `parsnip::null_model()` — added 2026-07-31 — from `references/nachum2026.md`. Theorem 4.10 plus Lemma 4.9 give an exact closed form for the k-fold CV MSE of the majority algorithm under Bernoulli(1/2) labels, which the page states. `parsnip::null_model(mode = "classification")` instantiates that algorithm and carries no tuning parameters (verified present 2026-07-31). Would be the shelf's first GP2 type (3) oracle for the outer loop. Two unmeasured risks before it is plannable: `null_model()`'s tie-breaking at Y = n/2 is unchecked against the theorem's `Y > n/2` rule, and MSE is a distributional property, so the replicate count must be priced against GP4 the way `references/varma2006.md`'s null-data invariant already was
- A synthesis note reconciling the shelf's five stability notions — added 2026-07-31 — hypothesis and loss and squared-loss (`references/nachum2026.md` Definitions 2.1-2.3), prediction-based (eps, delta) (`references/luo2025.md`), and *relative* loss stability, a ratio rather than a bound (`references/bayle2026.md`). They are not interchangeable and results proved under one do not transfer to another, but the G6 row above still says "loss stability" as though it named one condition. `references/` owns synthesis notes; this is what stops a future G6 plan conflating them
- Document IP2's enforceable scope in DESIGN.md — it binds only randomness flowing through R's RNG, so engines that bypass it (kernlab SVM, keras/torch) are unreachable by any R-side scheme — added 2026-07-25 — RR01 B4; amending IP text needs a D-entry
- What `...` still does not carry after M48: the Gaussian-process fitter's options (`corr`, `nug_thres`, `maxit`, `optim_start`, which `tune_bayes()` forwards from its own `...`) and the outer-loop `control` topepo reserved the name for in [#33](https://github.com/tidymodels/nestedtune/issues/33) — added 2026-09-02 — M48 Out, D-042. Promote on a user needing a GP option, or on a first outer-loop setting that is not its own argument
- An `autoplot()` view of each fold's inner search trajectory over `.inner_metrics` — the best-so-far by `.iter` for a Bayesian run, which is what [#57](https://github.com/tidymodels/nestedtune/issues/57) wanted the column for — added 2026-09-02 — M49 Out (done 2026-09-02), D-019. Extended 2026-09-02 at M50/M51's plan gate: a racing view (the per-resample elimination order `finetune::plot_race()` draws is not kept by the fold record, M50 Out) and an annealing trajectory (M51 Out) join it; and a view over the out-of-fold predictions M68 keeps (M68 Out, 2026-09-06). Promote on a user asking for a plot rather than the table
- Name the selection rule in `summary()` and the final fit's print when it is not the default best-by-metric rule — added 2026-09-06 — M69 Out; `extract_procedure(res)$select` reaches it. Promote on a user misreading `.selected` for lack of the rule on the printed surface
- Dispatch a `nested_workflow_map()` run as workflow-by-fold units in one parallel round instead of one round per workflow — added 2026-09-06 at M71's plan gate — M71 Out; needs the payload to carry the workflow (`R/parallel.R`, `fold_task()`). Promote on evidence that the per-workflow rounds leave daemons idle on a user's run
- `summarize = TRUE` on `collect_predictions()` for a `nested_results`, averaging each row's predictions across the repeats of a repeated design as tune's method does — added 2026-09-06 — M68 Out. Needs an oracle against tune's own averaging for class probabilities and votes. Promote on a user asking for it on a repeated design
- A `vec_ptype2`/`vec_cast` lattice on `nested_results_set` — added 2026-09-07 at M73's plan gate — M73 Out. Under M73's rule a set combined with a bare table sheds the class through both doors and a direct `vctrs::vec_cbind()` gives a plain tibble. Promote on a user needing a set-with-table combination or `vec_cbind()` to keep the class
