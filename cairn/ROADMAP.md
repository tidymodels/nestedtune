# Roadmap
_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-09-16 (triage pass: dropped the resample-weights row, shipped by M101, and the compute_metrics() repeated-.row row, shipped by M100, both already shipped; split the tidymodels-conventions row into Posit attribution, CODEOWNERS and badges rows; no decision entry. validate green.)_
## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M104 | An exact analytic oracle for the outer loop through the majority rule | planned | — | normal | milestones/M104-null-model-oracle.md |
| M101 | The outer average honors tune's resample weights | done | — | normal | milestones/archive/M101-resample-weights.md |
| M102 | The devel-vctrs leg runs weekly and keeps its own dependency cache | done | — | normal | milestones/archive/M102-devel-vctrs-schedule-cache.md |
| M103 | The workflow identity reads case weights and a postprocessor | done | — | normal | milestones/archive/M103-identity-case-weights-postprocessor.md |
<!-- rows grouped by status, not sorted by ID; keep only the 3 most recent terminal (done/dropped) rows — older ones live in milestones/archive/ + git -->

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- Put the Tibshirani and Tibshirani (2009) corrected flat estimate beside the nested one, read from the final fit's stored tuning run through tune's `debias_estimate()`. Added 2026-09-11 at issue [#95](https://github.com/tidymodels/nestedtune/issues/95)'s plan gate (D-062, tune#917 holds the code). The nested record cannot carry it, because `.inner_metrics` is summarized (D-043). Shipping it trades GP5 and amends D-014's no-claim posture on the stored run, so it takes a D-entry at its gate. Promote when tune exports the function and a source validates the correction for a tuned workflow, or when a user asks for the comparison
- What #91 still asks for after M84-M87 and M96: the maintainers' human pass over the guides and help pages (topepo's PR offer in the thread). The `@param` inheritance and the roxygen templates went to M96 on 2026-09-15; the guides' use of "you" was dropped at M96's plan gate (24 occurrences across the six pages at `ca6c628`, the M082-era overuse gone). The passive-voice and -ing-verb reader entries went to DESIGN Known issues at M87. [#91](https://github.com/tidymodels/nestedtune/issues/91) stays open. Promote on the issue author's PR offer landing
- The workflow identity does not read what a function-valued step setting closes over (a recipe setting that is a function is compared as its body), so two workflows whose settings share a body and close over different values pass the final fit's check — added 2026-09-11 at M83's gates (`milestones/archive/M083-final-fit-workflow-identity.md`), narrowed 2026-09-17 when M103 took case weights and the postprocessor. Promote on a user fitting with such a setting, or on the next milestone touching `R/workflow-identity.R`
- A panel-rate readability pass rule: three fresh readers scoring every paragraph on a closed (a)/(b)/(c) rubric against a blind-scored control, passing at most n paragraphs flagged by two of three. RR07 (`reviews/archive/RR07-readability-criterion.md`, recommendation 5) shows this shape needs one calibration run to set n, and D-061 names this row as the one awaiting it. Promote when a prose milestone wants a pass rule on a reader rather than a triaged report, and can spend the run — added 2026-09-11 at M84's RR07 ingest
- A sentence over the 30-word cap, or one failing a plain clause, inside a list item is not swept — added 2026-09-11 at M089's plan gate, which chose repairing `benchmarks/sweep-prose.R`'s parse leaks over widening what it sweeps (M089 Out). The roxygen path already makes a list item its own paragraph, so the widening is the Rmd path adopting that split. Measured at the gate, a widened parser still leaves the six gating sweeps clean over the real pages, so it costs no prose rewrite. Promote on such a sentence shipping inside a list item on a page
- What `benchmarks/sweep-prose.R`'s prose partition costs, added 2026-09-12 at M089's review gate (findings 3-6, 9, 10, which M089's Review section states in full). A loose list's continuation paragraph is swept, as the row above wants and the partition does not intend; a comment opened partway through a line leaks its body; a prose line opening `2020. ` is dropped as a numbered item, with the lines under it; an indented fence inside a chunk can close that chunk early; and the parse fixture's plant assertions are all negative, so a parser dropping too much is ruled out only indirectly. Each is stated in the script header, so none is a divergence between header and code. M097's review (2026-09-15) added the roxygen side: neither the sweep nor M096's duplicate-line command reads a `@templateVar` body, so the four tail sentences M097 moved into `FORCED_TAIL` and `SETTABLE_TAIL` left both checks' domain. Promote on any of them costing a real page its sweep, or on the next milestone touching `rmd_paragraphs()` or the roxygen reader
- Nothing checks that `.github/workflows/prose-sweep.yaml`'s six steps still match the gating list `benchmarks/sweep-prose.R` declares — added 2026-09-11 at M090's plan gate, which chose removing the duplicate lists over adding a mode that compares them (M090 Out). The yaml's six named steps are the one copy that stays, because a red run then names the failing mode on the job summary. A `cairn/`-only edit reaches neither trigger, the `paths-ignore` filter skipping it. Promote on the two drifting, or on a gating mode added or renamed
- M079's review leftovers, added 2026-09-10 at M079's review gate (`milestones/archive/M079-review-remainders.md`, findings 5, 8, 9, owns the detail). (1) `test-parallel-interrupt.R:108` fails inside the parallel suite and passes alone, a worker timing interaction seen across four full runs. (2) AC2's block drives `name_by_pid()` rather than `daemon_state_snapshot()`, the restored `seed = 999L` run is un-memoised, and `test-suite-hygiene.R` carries a block reading no source. Promote when (1) costs a review a re-run, on a parallel-suite change that prices it, on the snapshot's naming regressing unseen, or on suite time being priced again
- Coverage M75's pruning removed — added 2026-09-08 at M75's review gate (F1-F4; `milestones/archive/M75-suite-prune.md` names each deletion). Nothing now checks author-year citations against the `cairn/` shelf or the mori page's wire figures against `benchmarks/mori-wire-manifest.json`; every `.predictions`/`.extracts` assertion passes a `tune::control_grid()`, so the Bayesian, racing and annealing paths are covered only through `nested_fold_fit()` and `test-control-slots.R`; the fixture cache report's stream split is unasserted. Promote on a drifted citation or figure reaching the default branch, or on an upstream control dropping `save_pred` or `extract`
- The windows leg's last 1.3 minutes: only the check-time vignette rebuild is left to price. Re-cut 2026-09-08 at M76 and 2026-09-14 at M094, which took the `test-coverage` cap. `milestones/archive/M076-ci-leg-speed.md` owns the figures. Promote on the rebuild being wanted for itself, or on windows crossing 30
- What M56 and M57 left of the M52, M51, M37 and M34 review rows, added 2026-09-03 at M56/M57's plan gate. The M56/M57 archive files name the eight items: a duplicate-description counter, a shared fixture cache, a finetune floor bump, two re-templatings, a top-level `param_info`, a source-scan hardening, and a fixture-cache report under parallel files. Promote on a hang the trace misattributes, the next finetune re-pin, a cast reached with the wrong id columns, or `param_info` needed off the results object
- Fixture-family and closure-binding coverage the derived fixture-key test does not carry — added 2026-09-01 — M42 review O4/S1/S2/S3/R3 and O5/R2. The per-formal axis test keys no real fixture family pairwise and no two recipe-based workflows apart; its attribute and environment clauses are exercised by nothing; the depth guard covers only the sorted arguments, `canonical_form(fn)` and the bindings hashed unguarded. Promote on a fixture-key collision between two real fixtures, or when a family's sorted-argument depth approaches the cut (30 of 40 today)
- Posit attribution: tidymodels siblings' `DESCRIPTION` and LICENSE name Posit as copyright holder and funder, where D-025 keeps Jeffrey Girard as owner. Promote on an answer from the tidymodels maintainers, since the plan gate chose to ask rather than adopt or refuse — added 2026-08-28 — D-025, D-026, split 2026-09-16
- `.github/CODEOWNERS`, the tidymodels convention M33 left behind. Promote once understudies are assigned here — added 2026-08-28 — D-025, D-026, split 2026-09-16
- CRAN and downloads badges, the tidymodels convention M32 left behind. Promote at the first release — added 2026-08-28 — D-025, D-026, split 2026-09-16
- Route fold data to daemons through `mori`'s shared memory instead of by value, keeping the by-value path as the remote fallback and the one-frame-per-fold gate — added 2026-07-31, compressed through 2026-09-11 — M26 Out; `references/mori-backend-assessment.md` (P1-P9) owns the derivations and the adoption surface in `R/parallel.R`. mori is same-machine and needs `R (>= 4.3)`, a dependency change needing its own gate and D-entry. Absorbs the M23-Out `mirai::everywhere()` preload row, declined because the pool holds state across runs. Promote on evidence per-fold transfer is a real cost on a user's run, or on tune adopting mori (tune#1188)
- Let the metric that selects differ from the metric that scores, instead of one `metrics` argument driving both loops — added 2026-07-31 — `references/stone1974a.md` §2 item VII owns the quotation and figures. One `metrics` reaches the inner tune call and `last_fit()` alike, so the inner loop selects under the metric the outer loop reports; Stone measures cases where matching is worse. Not a defect: coupling matches tune's idiom (GP3); decoupling needs a second argument and a story for `collect_metrics()` and `.selected`. Promote on evidence a real workflow selects better under a different metric than it reports, or on a user asking for it
- Detect daemons running different code that still carries the same symbols, a build hash rather than symbol presence. Added 2026-07-30, M24 Out. M24's capability probe asks whether the symbols `dispatch_folds()` resolves by name are present, which catches a stale daemon with no `rehydrate_payload` but not one whose symbols all exist and whose behaviour differs, and that breaks IP2 invisibly. A version comparison cannot substitute while `DESCRIPTION`'s `Version:` reads `0.0.0.9000`, since a stale daemon reports the host's own string. Promote on evidence a heterogeneous pool has produced differing results, or once the package versions releases
- Generate the candidates rather than read back the ones that scored, so a candidate that failed on every inner resample is recorded and outer folds stop searching different grids — added 2026-07-30 — M21 Out, absorbing the M20 row; M21's archive owns the measurements and the two declined shapes. A `tune_results` carries no record of its expansion, and continuous-parameter expansion is stochastic, so each fold searches its own menu. Promote on evidence a user needs the failed candidates, or reads the per-fold records expecting one shared grid
- Normalize cli glyphs at the daemon so BC6 compares identical text — added 2026-09-04 — M59 review O13, S1, P1. `test-parallel-identity.R`'s `unwrap()` reads a daemon's `i` and the host's `ℹ` as one, since a daemon has no UTF-8 console; setting `cli.unicode` on the daemon (or the host) in the identity fixture would let BC6 compare byte-identical notes without widening its exemption. Promote when a second glyph joins the normalization, or when BC6's exemption is next weighed
- The shared `reg_metrics()` still equals tune's regression default — added 2026-07-30, trimmed 2026-07-30 at M20's plan gate and again 2026-09-02 at M49's (M49 keeps each fold's inner metrics as `.inner_metrics`, which was this row's other half). Its original rationale is weakened now that M18's `sep_*` fixture gives the suite a metric set that separates; changing it would re-record snapshots across ~6 files and still miss the `tune_grid()` site. Promote when a second milestone needs the same metric separation from a different fixture
- Name the cause of the two pre-M14 stalls — `R CMD check` on macOS 52 min and `covr` on ubuntu 40 min, silence after `* checking tests ...`, an identical re-run passing — added 2026-07-27 (`0db6f5f`), compressed 2026-09-14; M12–M16 own the mitigation, later occurrences and two refuted suspects. What is left predates M14's `collect_bounded()` sweep, so it may be unbounded in a way M16's budget does not reach. Not `/hotfix`-able. Promote on a recurrence longer than the worst case M16's ledger establishes
- Take the pre-flight deadline off the wall clock: `R/parallel.R` computes it from `Sys.time()` (the `deadline <-` lines), so a backwards system-clock step silently extends the only bound in the package — added 2026-07-27, corrected 2026-07-30 at M24's plan gate, whose audit found `proc.time()` reads the same realtime source, so M24 declined a misnamed helper. Promote on evidence the bound has overrun in the field, or once a genuinely step-immune R-level clock is established
- What the internal-pages guard cannot see — added 2026-09-09 at M78's review gate (findings O2, O6, O7, which M78's file and git own in full). `.github/workflows/pkgdown.yaml`'s guard matches tracked markdown by stem at the site root, which breaks three ways: on a site URL with a path segment, on untracked root markdown, and on a stem collision with pkgdown's own generated root pages. Promote on any of the three, or on a leak reaching the site
- Published-site rows left after M78 — added 2026-07-27, re-cut at M78's plan gate, compressed since — M17 Out and review F4-F6/F10, M33, D-003, D-028, D-060 carry the detail. Three items: `r-lib/actions/pr-push@v2`, `posit-dev/setup-air@v1` and `reviewdog/action-suggester@v1` ride moving tags under write permissions (D-028), promoting on the organization pinning them or on an altered tag; `clean: false` keeps a pre-1.0 renamed page served (D-003), promoting on the first release or on a stale page misleading a reader; and no `development:` block means the site builds to the root, promoting on the first release tag. Kept 2026-09-10 by explicit choice.
- Stop sending the recipe's own retained copy of the training data to every fold — `recipes::recipe(formula, data)` keeps all training rows in `$template`, serialized once per task in `mirai_map()`'s `.args` — added 2026-07-30, compressed 2026-09-13 — M23 Out, which owns the measurements (workflow bytes about equal to data bytes) and the source-references half it absorbed (`utils::removeSource()` at dispatch, measured then removed, since `utils` is not in Imports and the gain is development-only). Left out as surgery on a recipes-owned object whose other readers of those rows are unestablished (GP1). Promote on evidence that no recipes code path reads `template` for its rows, on a user report of dispatch cost dominated by a recipe, if a dependency gate opens `utils`, or on evidence a user installs with srcrefs kept
- Probe remote mirai daemon pools, which nothing verifies today — added 2026-07-26 — M10 Out, RR03 Q5, which established the load requirement by execution for local daemons only and inferred the remote case, so a remote host missing the package still surfaces as opaque per-fold worker failures. M58 (done 2026-09-04) asks each local daemon for the workflow's and the tuner's packages, the second question this row asks of a remote pool. M26 records that `mori` is same-machine-only, which bears on whether a remote pool stays a supported shape. Promote once a remote host exists to test against
- What `...` still does not carry after M48: the Gaussian-process fitter's options (`corr`, `nug_thres`, `maxit`, `optim_start`, which `tune_bayes()` forwards from its own `...`) and the outer-loop `control` topepo reserved the name for in [#33](https://github.com/tidymodels/nestedtune/issues/33) — added 2026-09-02 — M48 Out, D-042. Promote on a user needing a GP option, or on a first outer-loop setting that is not its own argument
- An `autoplot()` view of each fold's inner search trajectory over `.inner_metrics` — the best-so-far by `.iter` for a Bayesian run, which is what [#57](https://github.com/tidymodels/nestedtune/issues/57) wanted the column for — added 2026-09-02 — M49 Out (done 2026-09-02), D-019. Extended 2026-09-02 at M50/M51's plan gate: a racing view (the per-resample elimination order `finetune::plot_race()` draws is not kept by the fold record, M50 Out) and an annealing trajectory (M51 Out) join it; and a view over the out-of-fold predictions M68 keeps (M68 Out, 2026-09-06). Promote on a user asking for a plot rather than the table
- Dispatch a `nested_workflow_map()` run as workflow-by-fold units in one parallel round instead of one round per workflow — added 2026-09-06 at M71's plan gate — M71 Out; needs the payload to carry the workflow (`R/parallel.R`, `fold_task()`). Promote on evidence that the per-workflow rounds leave daemons idle on a user's run
- `summarize = TRUE` on `collect_predictions()` for a `nested_results`, averaging each row's predictions across the repeats of a repeated design as tune's method does — added 2026-09-06 — M68 Out. Needs an oracle against tune's own averaging for class probabilities and votes. M092's `augment()` refuses a design that holds a row out other than once until this lands. Promote on a user asking for it, or for `augment()`, on a repeated or Monte Carlo design
- A `vec_ptype2`/`vec_cast` lattice on `nested_results_set` — added 2026-09-07 at M73's plan gate — M73 Out. Under M73's rule a set combined with a bare table sheds the class through both doors and a direct `vctrs::vec_cbind()` gives a plain tibble. Promote on a user needing a set-with-table combination or `vec_cbind()` to keep the class
- `conf_mat_resampled()` for a nested run — added 2026-09-13 at M092's plan gate (M092 Out). tune 2.1.0 defines it as a plain function that checks `inherits(x, "tune_results")`, so no method reaches it, and a same-named export here masks tune's. Promote when tune makes it a generic, or on a user asking for it
- [low] `NEWS.md`'s 77 development bullets read to the M84 plain-prose standard — added 2026-09-10 at M84's plan gate — M84 Out. The bullets are consolidated at the first release, so the pass belongs to that release walk's NEWS step. Promote only if a release is declared before then and the walk keeps the bullets as they are. M095 takes the two bullets that contradicted the help after M091
- [low] Cut the CI matrix on pull requests to ubuntu+windows, and cancel superseded runs on the default branch too — added 2026-07-27 — M11 Out, dropped at its gate. The repo is public, so minutes are free (`billable: 0`); the PR cut saves a median 0.0 min because windows is the critical path in 43 of 55 runs while losing R-devel and oldrel-1 coverage, and cancelling on the default branch can leave a commit with no completed check. Promote on evidence either premise has failed — the repo goes private, or windows stops being the critical path
- [low] Variance estimation / inference on the nested estimate, `tune::int_pctl()`'s bootstrap intervals included (absorbed at M092's plan gate) — added 2026-07-25, compressed 2026-09-14 — G6; the caveat half went to M25. `references/tidymodels-nested-cv-gaps.md` (G6) and the pages it cites own the sources and numbers. Four blockers: (1) loss stability for a tune-then-fit procedure is unestablished, and the shelf splits the notion five ways (`nachum2026`, `luo2025`, `bayle2026`), so a synthesis note must reconcile them first and nothing may compare two workflows; (2) the valid interval's estimand is the k-fold test error of this run's rules, not the final model's; (3) the estimators need per-observation losses, which M68 keeps under `save_pred`; (4) `luo2026` bounds the power of any assumption-free valid black-box risk test at the fold ratio v-fold nesting uses. Promote only on all four blockers clearing
