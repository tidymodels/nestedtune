# M080: The CI records name the workflows that exist and stop copying their caps, and a leg watches the development vctrs one invariant rests on

**Status:** done (2026-09-10, PR #90 https://github.com/tidymodels/nestedtune/pull/90)

**Goal:** The repository's CI records describe the eight workflows that exist rather than the two that existed in July, a check leg watches vctrs' development branch, and the upstream ask that leg was scheduled beside is drafted.

**Outcome:** `cairn/PROFILE.md`'s test-doctrine slot carries no `timeout-minutes` figure of its own, naming each cap by its workflow file and job or step; its `paths-ignore` sentence names the five workflows `read_paths_ignore()` reports, its worker-count sentence names the four whole-suite workflows and says the stress workflow sets none, and its divergence tag list gains M78. The three comments crediting R-CMD-check's check step with 20 minutes (`test-coverage.yaml`, `stress-daemon-tests.yaml`, `pkgdown.yaml`) now state 30, 40 on windows, and four header comments stop counting the filter's copies. `.github/ci-usage-baseline.md` is regenerated over `[2026-08-11, 2026-09-10)`, which `BASELINE_SINCE`/`BASELINE_UNTIL` declare, and a bare rerun reproduces it byte-identically. New `.github/workflows/devel-vctrs.yaml` installs `r-lib/vctrs@main` after the dependency install, logs the version and `RemoteSha` it got, and runs `testthat::test_local(stop_on_failure = TRUE)` under `NOT_CRAN` and `TESTTHAT_CPUS: 4`, on `push` and `pull_request` with the shared filter, a 90-minute job cap sized from its measured 35m24s (run 34526174374), and `permissions: contents: read`. It was shown red on a planted break (run 34529742899) and green without it, and is non-gating: `main` has no branch protection and no rulesets. `test-ci-workflows.R`'s ordering assertion reads the `deploy` job. The upstream issue asking `r-lib/vctrs` to stabilize `vec_cbind_frame_ptype()` is drafted and not posted; its text is the `## Upstream issue draft` section of `git show a6a5a4b:cairn/milestones/M080-ci-records-vctrs-watch.md`, citing `R/nested-results.R:519`, the generic's `\keyword{internal}`, `[Experimental]` and "Expect changes", and the `sf` method's `647d8975` of 2020-03-27, unchanged since.

**Decisions:** none cross-cutting. The plan gate dropped the copied cap figures rather than teach `ci-usage.py` to compare them, regenerated the baseline rather than stamp it historical, and chose a non-blocking `push`/`pull_request` leg over a blocking or schedule-only one. The implement gate widened Scope for the pkgdown ordering test (T1) and for T3's two sibling comments.

**Review:** All seven criteria verified with fresh evidence: `devtools::check()` 0/0/0 in 8m35s, `document()` no diff, `check_pkgdown()` and `cairn_validate` clean. In the three-lens fan-out, blame-history and prior-review returned nothing and the diff-bug lens returned twelve findings, none meeting the return floor. Fixed at the gate: the leg's header claim that a break surfaces the day it lands upstream, the profile's "every workflow that runs the suite", the pkgdown cap comment calling the coverage leg gating, the usage script's "whole day behind the capture", the leg's `read-all` permissions, and the tag list's missing M78; a wrong citation in the review's own evidence was also corrected. Follow-up: two candidate rows, one for the cache key the leg shares with the stress workflow and one for the leg's missing schedule. Rejected with reasons: the Goal's "eight" (nine once the leg landed), AC2's `:195` (now `:197`), a finding read off a mid-flight file, and the baseline's line 5 naming a leg the window predates. The CI watch hit the harness ceiling once; the resume merged on all-green `37c7f54`. The resume's evidence-and-re-approval commit was held local to keep the PR head on verified CI and was deleted with the branch by `--delete-branch`, so the merged tree is the verified head. Graduated: the CI-records and pkgdown-ordering-test candidate rows. LESSONS: the M51 line's cap pointer now names the workflows.
