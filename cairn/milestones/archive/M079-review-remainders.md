# M079: The suite asserts the seed it forces, names its daemon records by the pid each holds, and its comments describe the code they sit on

**Status:** done (2026-09-10, PR #89 https://github.com/tidymodels/nestedtune/pull/89)

**Goal:** Close the eight remainders M74's and M76's reviews left behind: one dropped assertion, one mislabeled record, one doubly-built fixture, and five comments that no longer describe the code under them.

**Outcome:** `test-nested-tune-bayes-results.R`'s merged control block runs `nested_tune_bayes()` under `control_bayes(allow_par = TRUE, seed = 999L)` again and asserts it identical to the no-control run, the case for an overwritten explicit seed that nothing else in the suite makes. `daemon_state_snapshot()`'s naming moved into `name_by_pid()` (`helper-parallel.R`), which orders and names from one `order(pids)` permutation, pinned by a new `test-suite-hygiene.R` block over a shuffled fixture and a gapped one carrying a non-list answer and a `NULL` pid. `fixed_workflow()`'s `step_pca()` carries `id = "pca_fixed"`, so `wset_two()` and `wset_fixed()` key one cache entry: the file's report went 9 builds to 8, the hand call 2 builds to 1 over 6 requests. Both profilers print their per-run counts once, from inside the run loop. Comment sites corrected: `time-examples.R`'s AC7→AC6, `helper-time-budget.R`'s `times` comment naming both BC12 sites, and the three rationales in `helper-orchestration.R` that the written-out id falsified. `R-CMD-check.yaml:119-120` and `test-nested-tune-bayes-oracles.R:431-433` were read and left alone.

**Decisions:** Two of the eight items were not defects; the milestone file records both readings. **O4** — `order(pids)` ordering with `sort(pids)` naming — misaligns nothing, `order()`'s `na.last`, `sort()`'s NA drop and `names<-`'s padding cancelling; what M079 changed is alignment by coincidence to alignment by construction. **O7** — the bayes selection-rule block's cache-hit claim — is accurate, both blocks opening `make_reg_data()`, which seeds inside itself, so the two calls key one entry at `builds 1 / requests 2`. M74's archive line naming them as follow-ups is superseded here, never edited (IP4).

**Review:** Two rounds. Round 1 stopped at an amendment return on AC6, whose excused-item list was an enumeration fixed by author recall — the branch point reports five items, not the four it named. Reclassified under the widening test, so the repair narrowed rather than widened: AC6 now binds the branch head's own run over the files `git diff --name-only b07beb3 HEAD` names, and the branch-point comparison moved into T7. AC6's second re-audit returned an instrument-binding finding on the first amended wording, and the user took the reader's repair; AC6 having spent both amendment rounds, its remaining limit — that it binds failures in two test files in practice — was put to the user and accepted as recorded. Round 2 verified all six criteria: suite `FAIL 4 | PASS 9588` with every failure in `test-ci-workflows.R`, which this branch does not touch; `devtools::check()` 0/0/0; `check_pkgdown()`, `document()` and `air format --check` clean. Three-lens fan-out, 15 merged findings, none meeting the return floor. Fixed at the gate: the three stale rationales in `helper-orchestration.R` and `helper-time-budget.R`'s uncheckable grep citation. Follow-up: two candidate rows, one for the `test-parallel-interrupt.R:108` flake and one for findings 5, 8 and 9. Rejected with reasons: AC3's five-versus-six undercount (holds a fortiori), the pinned build figures (they name their procedure), and two cosmetic items. The M74-remainders candidate row graduates here. LESSONS: the M12 line corrected — a build count is read off `builds`, never `requests` — the M51 line extended with the mid-run edit that invalidated a suite record, the M50 line extended with `gh pr checks` reporting a stale run, and the M57 line trimmed of the worker-count precedence `PROFILE.md` now owns. Merged while the restarted PR matrix was still queued: the re-approval commit pushed before the merge retriggered CI, and `gh pr checks` reported the earlier green run. The merged tree differs from that verified head by one work-log line and no code.
