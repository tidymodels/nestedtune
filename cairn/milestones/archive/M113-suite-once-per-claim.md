# M113: The suite runs in three quarters of its serial time with no line of coverage lost

**Status:** done (2026-09-23, PR #128 https://github.com/tidymodels/nestedtune/pull/128)

**Goal:** Cut the test suite's serial time to three quarters of today's by removing repeated fits, with every line of `R/` that covr reports covered today still covered.

**Outcome:** Serial suite 808.6 s against 1029.3 s at the branch point (78.6%, three passes each, medians), 977 blocks, 0 failures, 0 skips; per-line covr coverage of `R/` identical on both trees (5238 lines, the same 116 uncovered); the slowest `R-CMD-check.yaml` step 23.0 and 22.8 min on PR #128 against 28.3 to 29.6 on main. `test-time-series-{bayes,race,anneal}.R` run their reference loops on the rolling-origin design alone (D-079), the Bayes sliding-window final-fit test staying as D-075's evidence. Gone: the Bayes and anneal M69 selection-rule blocks; the success-path caller-RNG-survival and no-RNG-state blocks in the Bayes, race and anneal rng files; the fold-order and ambient-kind blocks in the race and anneal rng files; BC10, BC12 and BC13 at three daemons. Cached: fit_resamples' two seed-30 runs, grid-oracles' stochastic run and its discriminator, grid-rng's seed-77 run, the workflow-map readers' two broken sets, the time-series designs' eight seed-25 runs, the memory file's one measurement; racing's `best` step reads `race_results(fn)`. BC4 fabricates its worker count. The help of `nested_tune_grid()` and `nested_resamples()` and `NEWS.md` say which function is tested on which design. `benchmarks/coverage-lines.R` (new) writes per-line hits keyed by file and non-roxygen position; both trees' tables are committed beside it, and `benchmarks/test-timing-baseline.md` carries the M113 figures. `Config/testthat/start-first` re-cut to the eleven heaviest files. The milestone file's Removals ledger maps every removed block (git holds it at `5cca5dc`).

**Decisions:** D-079 (the cut and the four dropped families, superseding D-074's rejection), D-080 (corrects D-079's final-fit clause). Three gated amendments: AC1's bar 75% to 80% after the measured 78.6%; AC3 gained exception (e) for the fold-order and ambient blocks; AC6 rebound from the ubuntu release leg's `testthat.R` median (failed at 20 min against 12) to the slowest leg's check step under 25 min. The Goal's "three quarters" was left as written.

**Review:** Three-lens fan-out, 20 findings. Fixed at review: eight stale comments, a dead variable, two ledger citations, a partnerless `memoised()` call, a redundant seed, and the help understating the Bayes sliding-window final-fit test. Rejected with reasons: the race `best` step, BC4's fabricated count, the covr internal, the `start-first` re-cut against the Out line, the one-sided `memoised()` against the M42 lesson's letter. One defect return (AC6) and two amendment returns (AC3, AC6). Not done from the plan: the detection file's pool merges and the per-block serial references (T6), the Bayesian final-fit rng blocks (T5). LESSONS: the M42 line extended with the one-sided cache form, the M51 line with the runner-variance and battery-throttling observations. Candidate row added for a lighter Bayes fixture engine.
