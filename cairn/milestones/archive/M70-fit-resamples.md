# M70: `nested_fit_resamples()` scores a workflow with nothing to tune on the outer folds of a nested design, and the five tuning orchestrators refuse one

**Status:** done (2026-09-06, PR #80 https://github.com/tidymodels/nestedtune/pull/80)

**Goal:** Give a fixed workflow one obvious path through the same nested design a tuned one ran on, so the two score on identical outer folds and the record says no tuning ran.

**Outcome:** `nested_fit_resamples(object, resamples, ..., metrics = NULL, event_level = "first", eval_time = NULL)` in `R/nested-fit-resamples.R`, the sixth orchestrator, shaped like `tune::fit_resamples()` with a `control_resamples()` through `...`; it enters `nested_loop()` with `tuner_fit_resamples()`, `param_info = NULL`, `select = NULL`, `grid = NULL`. A `fit_resamples` entry in `tuner_registry` (`R/tuner.R`) with a new `selects = FALSE` flag (the five carry `TRUE`); `tuner_selects()` reads it, total toward selecting for an unknown or missing name. `nested_fold_fit()` (`R/nested-tune-grid.R`) skips the framed inner rset, the tuner call and the rule for such a tuner, records a 0 x 0 `.selected`, a zero-row `.inner_metrics` with tune's summary columns, both seeds (the tuning seed drawn and consumed by nothing), and runs `last_fit()` under the outer-fit seed; a failing fit records a note at `"outer fit"`. `new_procedure()` omits `select` and `param_info`; `check_results_record()` requires the rule only where the tuner selects. Entry refusals in `R/checks.R` over `tune::extract_parameter_set_dials(object)$id`: `check_tuned_workflow()` on the new export (`nestedtune_tuned_workflow`) and `check_untuned_workflow()` after `check_workflow()` on the five (`nestedtune_untuned_workflow`), both skipped when extraction fails. `plot_selection()`'s abort classed `nestedtune_no_tuned_parameters`. `nested_final_fit()` on the record draws both seeds, fits the workflow as given with `tuning = NULL`, refuses a marked workflow, prints "Procedure: no tuning" and "Selected: nothing to select"; `extract_tune_results()` and `extract_scored_candidates()` refuse it with `nestedtune_no_tuning_run`. Help page with every `control_resamples()` slot classified, the refusal on the five pages, summary/print/extract/autoplot/collect pages naming the new shapes, pkgdown row, NEWS, DESIGN, the "A baseline on the same folds" section in `nested-cv.Rmd` joining the two runs' rmse by `id`, and a sentence in `tuners.Rmd`. Fixtures `fixed_workflow()` and `fixed_stoch_workflow()`; tests `test-nested-fit-resamples-{oracles,readers,checks,rng}.R`, `test-nested-final-fit-resamples.R`, an untuned-refusal test in each `*-checks.R` file, BC16 in `test-parallel-identity.R`.

**Decisions:** D-057 (a sixth orchestrator over accepting an unmarked workflow on the five; refusal on all five without a deprecation cycle; a plain `rset` left to tune). Milestone-local: the final fit refuses a marked workflow on a `fit_resamples` record; its print names the procedure by the registry label with no candidate count; it draws both seeds and does not evaluate the design's inner specification. Plan-level: a 0 x 0 `.selected` over a one-row empty tuple (D-039); the tuning seed recorded as drawn rather than `NA`. Observed on tune 2.1.0: `control_resamples()`, `control_grid()` and `control_last_fit()` share one class vector, so any of the three is accepted.

**Review:** three-lens fan-out on PR #80: [O] 11 findings, no correctness bug; [S] blame-history 0; [S] prior-review 1. Fixed now O2 to O9 and P1 (help pages naming the new refusals and shapes, the pkgdown description, the tuners vignette's mention and its pre-M68 line on `extract`/`save_pred`, an executed chunk behind the vignette's zero-rows claim, an `nrow` guard in the AC1 oracle loop); O1 (the final fit ties no fixed workflow to a `fit_resamples` record) to a candidate row; O10 and O11 rejected with reasons. All seven criteria evidenced by command; suite 8111 passed, `devtools::check()` 0/0/0. PR conversation empty at every read. The CI wait hit the ceiling once after approval; re-approved at the resume and merged on green at 633954d. Nothing graduated or retired.
