# M084: The guides and README read on one pass for a tune_grid user

**Status:** done (2026-09-11, PR #94 https://github.com/tidymodels/nestedtune/pull/94; resolves #91 partial, the help pages following in M085)

**Goal:** Rewrite every paragraph of the four guides, the parallel article and the README that a tidymodels user who has run `tune_grid()` cannot follow on one read, to the standard the accepted intro of `vignettes/nested-cv.Rmd` (lines 17-33 at `0d8611e`) sets.

**Outcome:** The six pages rewritten (15 files, +1412/−407): no prose sentence over 30 words (45 at the branch point), `procedure` and `candidate` glossed at first use on every page and `reader` on `results.Rmd`, `orchestrator` gone, each page's first sentence naming what the reader wants, no sentence naming more than four backtick spans, all 31 paragraphs `cairn/surveys/M084-survey.md` lists rewritten. Word counts 1280 / 1394 / 1388 / 1419 / 562 / 249 against caps 1500 / 1400 / 1500 / 1500 / 700 / 350; M082's "you" cap dropped. `benchmarks/sweep-prose.R` added: the sentence cap, `--terms`, `--spans`, `--openings`, `--paragraphs`, and a `--roxygen` mode for M085. One NEWS bullet. `devtools::check()` 0/0/0; CI green on nine checks, the non-gating coverage leg red on covr's `merge_coverage` `readRDS` failure (the identity the coverage-leg candidate row records), merged past it at the user's decision.

**Decisions:** `orchestrator` replaced by plain words rather than defined per page; the IP3 paragraph's 36-word sentence split in place with its three claims kept verbatim; D-061 promoted from RR07 (a readability criterion binds mechanical proxies and a triaged fresh-reader report, never a pass rule on what the reader lists); RR07's five answers sit in the milestone file's Decisions section, which git holds; the panel-rate reader rule is a candidate row. Two defect returns on AC3's empty-list form (fresh reads of 10 and 17 after nine implement-side reader runs), one amendment narrowing it, a Fable brief (RB07/RR07, archived under `reviews/archive/`), and a second amendment adopting RR07's four-part text; thirteen fresh reads never returned empty.

**Review:** pass 3, three-lens fan-out plus the AC3 reader. The reader listed 14 paragraphs: 8 fixed (the `select` aside moved after the fit-and-score sentence, the four negations set as a list, "That difference is least stable when the two procedures are similar", two paragraph breaks, the feature-selection paragraph led by its action, parallel's "You switch parallelism on", a tail clause made its own sentence), 6 rejected (two share the frozen intro's own sentence shape; four flag density that is the paragraph's content). Lenses reported 2, 9 and 13 findings; fixed: tuners' baseline sentence ("reads the baseline the same way, under the same control settings"), nested-cv's bias sentence cut to one linked sentence (M082's one-page rule restored), README's loop sentence naming select and fit, "Ask `res` or `final` and both refuse", the survey's denominators, the script header, the pass-2 findings' missing dispositions; follow-up: one candidate row for the script's prose-definition gaps (list-item continuation lines and badge lines count as prose; an indented fence or multi-line HTML comment would leak); rejected: the NEWS claim against the frozen intro (RR07 Q5), lines past 76 columns, three plain names for the reading functions, a pre-existing Bayle sentence, the third-negation note. CI's `format-suggest` fixed by `air format` on the script. Hygiene: one LESSONS line (RR07 recommendation 6); the M18 RNG-kind and M55 `addNA()` lessons pruned as the stalest to meet the byte budget; M80's row pruned; nothing graduated or retired.
