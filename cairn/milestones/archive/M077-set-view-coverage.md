# M077: The set's figures say what their averages rest on, and the shapes past `wset_three()` are drawn

**Status:** done (2026-09-09, PR #87 https://github.com/tidymodels/nestedtune/pull/87)

**Goal:** Bring the multi-workflow figures up to the single view's standard on what an average rests on, and draw them over the metric and parameter shapes `wset_three()` cannot produce.

**Outcome:** `set_short_average_line()` (`R/nested-results-plot.R`) adds a subtitle sentence to the set's performance view — "N of K workflows average a metric over fewer folds than they completed; see summary()." — a sibling of `set_shortfall_line()`, not a widening: the two counts answer different questions and a figure can carry both. `plot_set_performance()` keeps its per-workflow `summarize_folds()` so the rules and the count read one `n`; a workflow that failed every fold averages nothing and is left to the other sentence. Two censored fixtures in `helper-orchestration.R`: `srv_set_results()`, two `dist` workflows under `brier_survival` + `concordance_survival` at two evaluation times, giving three metric-and-time keys; and `srv_mixed_results()`, a `dist` workflow beside a spline `deg_free` one. Both set views are drawn over them, expected panels and rules derived from `collect_metrics()` and `collect_selections()`. A third set doppelganger pins the four-line subtitle. NEWS entry and the set `autoplot()` help paragraph. `skip_if_no_censored()` does not cover `workflowsets`, so every reading test guards on both it and `skip_if_no_wset_fixture()`.

**Decisions:** none cross-cutting. Milestone-local, in git: the sibling function over widening `set_shortfall_line()`; the sentence's wording; a spline `deg_free` step as the mixed set's numeric parameter.

**Review:** three-lens fan-out, 14 findings. Six fixed on the branch: a tautological constant comparison replaced by reading both counts off the rendered subtitle (proven by a planted wrong count, which failed six tests); a non-empty floor on AC2's rule/mean correspondence; the help text's lost conditionality; an un-rewrapped 110-character roxygen line, which the prior-review lens tied to the same point M66, M68 and M69 each raised; a provenance header naming the wrong generators; an unpinned warning count on the new snapshot. One to a candidate row: the sentence's denominator counts workflows its numerator structurally excludes. Seven rejected with reasons. No return-floor finding. Suite 9583 passing, 0 skipped; `check()` 0 errors, 0 warnings, 0 notes.
