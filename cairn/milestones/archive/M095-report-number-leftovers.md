# M095: The summary note, plot subtitles and tuner help name the number to report for the deployed model

**Status:** done (2026-09-15, PR #108 https://github.com/tidymodels/nestedtune/pull/108)

**Goal:** Every text M091 left saying the nested estimate is "not a model you can deploy" or is "reported for the procedure" names it as the number to report for the deployed model instead.

**Outcome:** `print_procedure_note(set)` in `R/nested-results-print.R` prints one note for a `nested_results` and one for a `nested_results_set`. Each says the estimate describes the tune-and-fit procedure. Each says that, for a procedure chosen before seeing it, the estimate is the number to report for the model `nested_final_fit()` builds. The set note ties each estimate to the fit built with that workflow's `id`. Both `autoplot(type = "performance")` subtitles in `R/nested-results-plot.R` end with one fixed 67-character line. That line reads "It describes the procedure and is the final fit's number to report." The bayes, race and sim-anneal Descriptions match `?nested_tune_grid`. `final_fit_estimate_msg` and five roxygen sentences in `R/nested-final-fit*.R` add the workflow-set case. That case is limited to a workflow chosen before seeing the set's estimates. Two NEWS bullets no longer say the fit has no performance number, and a new bullet names the change. Tests in three files match each fact. Seven print, four final-fit and three vdiffr snapshots were re-accepted after reading. The commit also carries the `NAMESPACE` regrouping `document()` writes under roxygen2 8.1.0, which closes that candidate row.

**Decisions:** none. The plan gate chose one static set-fit sentence over recording the workflow id on the fit object. It put the "chosen before seeing" condition in both notes, fixed the two contradicting NEWS bullets now, and took mechanical checks over a fresh-reader report. Each choice and what falsifies it is in the work log in git.

**Review:** all seven criteria verified. `devtools::test()` 10116 passed, `devtools::check()` 0/0/0, six gating sweeps and `air format --check` clean, `document()` no diff, DESIGN.md unchanged. Three lenses ran. The blame-history and prior-review lenses found nothing. The diff-bug lens reported twelve findings. Six were fixed at the gate. They were the missing condition on the set case in the final-fit message and help (O1), and an "and" that read as "report both" (O3). Also three sentences naming a results object the fit does not store (O4). Also a stale test name (O7), long lines (O8) and an unreflowed NEWS bullet (O12). Six were rejected with reasons. The set subtitle carries no condition and shortens the procedure name (O2, O10), because 68 characters hold no more. The static message names sets on every fit and the test cannot tell a set-built fit apart (O5, O6), the plan gate's choice. The multi-line NAMESPACE form is what `document()` writes here (O9). The bayes Description's "any one model" pattern mirrors the grid page (O11). The set subtitle's missing condition went to DESIGN Known issues. The CI watch stopped at its time limit on 2026-09-14, and the merge ran the next day under the recorded approval.
