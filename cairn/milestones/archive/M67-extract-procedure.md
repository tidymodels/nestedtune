# M67: `extract_procedure()` reaches the `procedure` record on both objects, and the em dashes leave the package's user-facing text

**Status:** done (2026-09-06, PR #77 https://github.com/tidymodels/nestedtune/pull/77)

**Goal:** Give the `procedure` record an accessor on the two objects that hold it, so no page or help text tells the reader to reach an attribute or list slot by hand, and remove every em dash from the text the package publishes.

**Outcome:** `extract_procedure()` is an exported S3 generic in `R/extract-procedure.R`: the `nested_results` method returns `attr(x, "procedure")`, the `nested_final_fit` method returns `x$procedure`, both refusing a non-empty `...` with `rlib_error_dots_nonempty`; the default aborts with class `nestedtune_no_extract_method` through `abort_no_extract_method()`, which gained a `classes` argument naming each answering class with its origin (the phrase aligned with `agreement()`'s) and refuses an unknown class. Help page with an executed example (`@return` naming the record's four shared entries and the Bayesian record's dropped `seed`), `_pkgdown.yml` row after `agreement`, NEWS entry, DESIGN Function Families and Final-fit lines. Twelve roxygen sites, `tuners.Rmd` and `results.Rmd` rewritten onto the accessor. Em dashes removed: the results print's candidates line reads `Candidates searched: 5, 5, 5. The folds did not search the same grid` (snapshot re-recorded), 59 roxygen lines, 39 NEWS sites, 4 in `README.Rmd` (README re-knit), 2 in `_pkgdown.yml`. Tests: `test-extract-procedure.R` (identity on a grid result and its final fit, the default's class and message, a dots refusal per method, the no-method refusal winning over dots), the new methods in `test-dots-barrier.R`'s probe lists and the Bayesian oracles' coverage table. Absorbs the two candidate rows of 2026-09-05 (M66 T6 F23; M65 Out).

**Decisions:** milestone-local: `extract_procedure()` over `procedure()` (collides with a `tuners.Rmd` binding; breaks D-023's `extract_` idiom) and over a `nested_results`-only method; the em-dash sweep widened from the print line to help, NEWS, README and pkgdown at the user's choice; a full stop for the print line; per-site rewording over nearest-punctuation replacement. No D-entry.

**Review:** three-lens fan-out on PR #77: [O] 11 findings, [S] blame-history 0, [S] prior-review 0 (the probe found two inline comments on untouched workflow files). Fix now, seven: F1 the extract family's origin phrase aligned with `agreement()`'s; F2 `stopifnot()` on an unknown class in `abort_no_extract_method()`; F3 `results.Rmd` names the accessor; F4 a NEWS fragment repunctuated; F5 `@return` names the dropped `seed`; F6 DESIGN's final-fit line names the accessor; F8 a comment's plural. Rejected: F7 and F9 cosmetic; F10, AC1 names a grid result. F11 resolved by the evidence lines. Fresh `devtools::check()` after the fixes 0/0/0 in 6m 54s; `devtools::test()` 7192 passed; six pages rendered free of U+2014 and its entities. PR conversation empty at every read. CI wait hit the ceiling twice (aa85d66, then 3813155 after the re-approval push restarted the matrix); merged on green at 20392e5, the second ceiling-stop line held local and carried here. Nothing graduated or retired.
