# M085: The help pages read on one pass for a tune_grid user

**Status:** done (2026-09-11, PR #96 https://github.com/tidymodels/nestedtune/pull/96; resolves #91 partial, the help-page half; the issue stays open for human review and no comment was posted on it, at the user's decision)

**Goal:** Rewrite every roxygen paragraph that a tidymodels user who has run `tune_grid()` cannot follow on one read, to the standard M84 sets, with the package's own words set up once on `?nested_tune_grid` and linked from every other page that uses them.

**Outcome:** Every exported help page rewritten (22 source files under `R/`, `man-roxygen/` and `benchmarks/`, +1214/−983; 55 files with the regenerated `man/`): no roxygen sentence over 30 words (142 at the branch point), no sentence naming more than four code spans (50), each of 27 description openings naming what the reader gets, `procedure`, `orchestrator` and `candidate` set up on `?nested_tune_grid` and set up again or preceded by a link on every other block, `reader` gone from the roxygen surface, all 107 survey paragraphs rewritten. `extract_scored_candidates()`, `agreement()`, `selection_rule()` and `extract_procedure()` retitled. `benchmarks/sweep-prose.R` gained the `--roxygen` mode across `--spans`, `--openings` and `--paragraphs`, dropping fenced code, pipe-table lines and the bold "Differences" run-in headings that `test-control-slots.R` parses. `NAMESPACE` reformatted to roxygen's one-import-per-line form, same imports. One NEWS bullet. `devtools::check()` 0/0/0; CI green on 12 checks after one `format-suggest` red fixed by `air format`.

**Decisions:** AC3 amended to the D-061 shape before implementation (span cap, per-block openings, a blame check over the survey, one triaged fresh-reader report per review pass, never rerun); the "Differences" run-in headings excluded from the span cap rather than rewriting the pinned test; the package words defined once and linked, not per block; `reader` dropped everywhere rather than defined on the collect page; #91 left open with no cairn-authored comment. One defect return (pass 1, AC2: the final-fit summary block used `candidate` and `procedure` cold).

**Review:** pass 2, three-lens fan-out plus the AC3 (iv) reader. The reader listed 68 paragraphs (`cairn/surveys/M085-reader-review.md`): 14 fixed (openings reordered so the reader's use or question leads: the subsetting rule, the result-manipulation rules, the marginal-over-selection caveat, the standard-error limit, the procedure record, set subsetting, `.config`, the plot subtitle, `sum(n)`, row repetition, seed sharing, rsample compatibility, the set subtitle, `augment()`'s refusal), 54 rejected (shared input specifications, list-shaped rules whose density is their content, reference paragraphs read by someone who hit the condition, openings already naming what the reader gets, the chosen setup site for `orchestrator`, argument items within the AC4 cap). Lenses reported 3, 6 and 0 findings; fixed: `"survival"` restored among the predict page's `type` examples, "must have" on the race page's `grid`, "tune, select and fit" in apposition on the package page's first `procedure`, the survey's garbled `:172` entry; rejected: the summary page's dropped six-tuner list, the `selection_rule()` retitle, "loop function" and "reading function" wording, a trimmed return sentence whose names the page still lists. Four sentences the fixes pushed over the caps were split again before the push. Hygiene: one LESSONS line (`Rscript -e` and backslashes); the M66 render-time lesson pruned for the byte budget; the M81 row pruned; nothing graduated or retired.
