# M098: The summary and final-fit prints name a non-default selection rule

**Status:** done (2026-09-16, PR #111 https://github.com/tidymodels/nestedtune/pull/111)

**Goal:** If a run selected each fold's candidate by a rule other than the default best-by-metric rule, the results summary and the final fit's print and summary name that rule.

**Outcome:** `selection_rule_label()` in `R/selection-rule.R` renders the rule name, then ` by ` and the orderings as written, then ` (limit = <n>)` for `pct_loss`. `format.selection_rule()` now reads it after its class tag. `names_selection_rule()` answers whether a record's rule is one the summaries name, which is a non-default rule and not `NULL`. `summary.nested_results` and `summary.nested_final_fit` carry a `select` component holding the procedure record's rule. Where no rule was applied, it is present as `NULL`. `print_selected_by()` in `R/nested-results-print.R` prints `Selected by: <label>` under the "Selected parameters" heading of both summaries. It also prints after the `Selected:` line of `print.nested_final_fit`. It prints ahead of the early returns. A run in which no fold completed therefore still names its rule. The set summary gets the line inside each tuned workflow's section. Default-rule output is unchanged. The snapshot diff adds 442 lines and removes none. Five help pages and a NEWS bullet describe the line and the component.

**Decisions:** none beyond the plan gate's, which the work log holds. The line prints only under a non-default rule. It prints when no fold completed. It stays off `print.nested_results`. It takes the rule's own label rather than prose.

**Review:** all six criteria verified. `devtools::test()` 10231 passed, `devtools::check()` 0/0/0, six gating sweeps clean, `document()` no diff, `check_pkgdown()` clean. Three lenses ran. The prior-review lens found no prior-review evidence. The history lens found no regression or contradicted decision. The diff-bug lens ranked twelve findings, none a wrong-output bug. Two were fixed. `selection_rule.Rd` now names the set summary among the surfaces the label reaches. The `select` bullet in `summary.nested_results.Rd` was rewritten as prose in the component's order. Ten were rejected. Label wrapping through `cli_text()` matches the sibling lines. A degenerate label on an empty ordering is a state the constructor refuses. The option-sensitive limit rendering predates the branch. The rest were the predicate's name, a test fixture the work log discloses, set-test placement the snapshot pins, the `heading + 2L` spacing assumption, a duplicated test helper, an unrelated `expect_named`, and fixture-gated skips that follow the suite's convention. The PR conversation was empty. Nothing was graduated or retired.
