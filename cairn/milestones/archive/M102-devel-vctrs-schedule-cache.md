# M102: The devel-vctrs leg runs weekly and keeps its own dependency cache

**Status:** done (2026-09-16, PR #115 https://github.com/tidymodels/nestedtune/pull/115)

**Goal:** `.github/workflows/devel-vctrs.yaml` runs on a weekly schedule beside its push and pull-request triggers, and its dependency cache is keyed apart from every other workflow's so a development vctrs is never restored elsewhere.

**Outcome:** `devel-vctrs.yaml` gains `schedule:` (`17 6 * * 1`, Mondays 06:17 UTC, off the hour GitHub documents as most delayed) and `workflow_dispatch:` beside `push` and `pull_request`. Its `setup-r-dependencies@v2` step passes `cache-version: devel-vctrs`, so the library cache it saves after installing `r-lib/vctrs@main` keys apart from every other workflow's (`<os>-<R>-<arch>-devel-vctrs-<hash>` against the siblings' `-1-`). Dispatched run 35149168999 restored nothing and saved that key; `gh cache list` showed it matching none of the other 57. The header comment states the schedule, the 60-day schedule-disable rule, that the cache is saved only on a key miss while the install step re-resolves `r-lib/vctrs@main` every run, and that the cache write needs no extra permission scope. `cairn/PROFILE.md`'s test-doctrine line names the four triggers and the key. `.github/ci-usage.py`'s `read_paths_ignore()` still reads one agreed list over six workflows. Closes the two M080-review candidate rows (F1 schedule, F4 cache collision).

**Decisions:** a distinct `cache-version` over `cache: false`, keeping warm installs while cutting the shared restore-key prefix (plan gate).

**Review:** all four criteria verified. `devtools::check()` 0 errors, 0 warnings, 0 notes. `document()` no diff, `check_pkgdown()`, six gating sweeps and `cairn_validate` clean. Three lenses ran; blame-history and prior-review reported nothing. The diff-bug lens ranked eight findings, none failing a criterion. Fixed at the gate: the cache freezes its development vctrs until the hash changes (comment); cron moved off the hour; PROFILE.md's "(development vctrs inside)" was unobserved (reworded); the 60-day schedule-disable rule (comment); the permissions comment omitted the cache write; PROFILE.md had dropped two reasons for the line cap (restored). Deferred: `ci-usage.py`'s commit-driven baseline cannot attribute scheduled or dispatched runs (F7). Rejected: the 7-day cache eviction coinciding with the weekly cadence (F8, push and PR runs refresh the cache). At hygiene the user routed F7 and the unpriced cold devel build to DESIGN Known issues, replacing the two closed candidate rows. The PR conversation was empty on the resume. Nothing was graduated or retired.
