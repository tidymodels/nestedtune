# M100: compute_metrics() refuses a fold whose saved predictions do not match what it held out

**Status:** done (2026-09-16, PR #113 https://github.com/tidymodels/nestedtune/pull/113)

**Goal:** `compute_metrics()` on a nested run applies the same per-fold row check `augment()` applies (M093), so a fold whose saved predictions repeat, miss, or add a row is refused rather than scored.

**Outcome:** `compute_metrics.nested_results()` calls `check_predictions_rows()` after `check_column_saved()` and before any fold is scored. The checker takes a required `verb` argument, `augment` or `compute_metrics`. It derives its class from it, `nestedtune_<verb>_predictions`, so both readers share one rule. The message names each failing fold, and the set method names the workflow through `for_workflow()` with no code change. A repeated v-fold design is scored, since the check is per fold. The `compute_metrics()` help and NEWS name the refusal, the five shapes as an open list, and the whole-number case. `plant_row_mismatch()` and `edit_fold_predictions()` moved from `test-augment.R` into `tests/testthat/helper-predictions.R`. A counting metric set reads how many folds were scored. `NAMESPACE` now carries roxygen's one `importFrom()` per line.

**Decisions:** none.

**Review:** all four criteria verified. `devtools::test()` 10317 pass, 0 fail, 0 skip. `devtools::check()` 0 errors, 0 warnings, 0 notes. Six gating sweeps, `air format --check`, `document()` no diff, `check_pkgdown()` and `cairn_validate.py` clean. Three lenses ran. The blame-history and prior-review lenses reported nothing. The diff-bug lens ranked eight findings, none failing a criterion. One was fixed at the gate: `verb` lost its default and is required. Six were rejected. A duplicate `.row` from a survival or quantile mode is refuted by the censored-regression rescore test. Quantile predictions hold one row per observation. A `NULL` `.predictions` on a completed fold is reachable only by editing the object, as for `augment()` since M93. The class built by `paste0()` is stated in the comment beside it. The help's order is not precedence. The "not a whole number" sentence holds in effect. `complement()` reads a stored index. One was noted: the `NAMESPACE` form flip, which M094 recorded. The PR conversation was empty on both resumes. At hygiene the M92 lesson on `document()` under an older roxygen2 was corrected in place. A probe showed roxygen2 8.0.0 writes under a declared 8.1.0. Nothing was graduated or retired.
