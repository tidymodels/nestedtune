# M66: The six vignette pages read the results through the package's readers under `library(tidymodels)`, and take a prose pass

**Status:** done (2026-09-05, PR #76 https://github.com/tidymodels/nestedtune/pull/76)

**Goal:** Rewrite every reader-facing chunk on the six pages under `vignettes/` to read through the M65 readers, dplyr and tibble with tidymodels attached, and pass all six pages' prose through one fresh read.

**Outcome:** `tidymodels` in Suggests (D-053). The five code pages (`nested-cv`, `results`, `tuners`, `articles/parallel`, `articles/why-nest`) fold `requireNamespace("tidymodels")` into their `has_*` guard, print one notice naming what is missing and `knitr::knit_exit()`, then attach `library(tidymodels)` and `library(nestedtune)`; core-package prefixes stripped from shown chunks. `nested-cv.Rmd` reads the fold table with `collect_selections()` and `n_distinct()`, the selection-time score with `extract_tune_results(final) |> show_best()`, the comparison with `tibble()`; `results.Rmd` reads both notes tables through `collect_notes()` with `filter()` and `count()`, the partial warning's class with `rlang::catch_cnd(classes = "nestedtune_partial_summary")`, and gains a readers chunk showing `collect_selections()` and `collect_inner_metrics()`; `tuners.Rmd` counts fits per fold with `collect_inner_metrics(race) |> group_by(id) |> summarise()` and `slice_min()`, the win/loss race read the same way; `articles/parallel.Rmd` catches the dispatcher-less pool's class with `catch_cnd()`; `articles/why-nest.Rmd` builds its long table with `bind_rows()` and `tibble()`. A prose pass fixed 28 of 32 reader findings. `benchmarks/sweep-vignette-idioms.R` (the AC1, AC2 and AC6 sweeps: idiom tokens including `\(` lambdas, prefixes, guard shape, em dashes, milestone tokens, foreign `vignette()` names) and `benchmarks/time-vignettes.R` (median of three renders). NEWS entry.

**Decisions:** milestone-local, at the plan gate: `library(tidymodels)` over attaching the Imports individually; `expand.grid()` kept over `dials::grid_regular()`; a review-time sweep over a permanent idiom lint; `rlang::catch_cnd()` over knitr's warning display. Prose-pass rejections: "guide" for the getting-started page's own name; `n_distinct()` where the sentence counts values per parameter; "k-fold test error" as the cited source's term; the results print's em dash to a candidate row. No D-entry.

**Review:** three-lens fan-out on PR #76: [O] 20 findings, [S] blame-history 4, [S] prior-review 0 (the probe found only PR #30's thread). Fix now, sixteen: O1 NEWS names the three code vignettes; O2 the seeds sentence names `tuning_seed` and `fit_seed`; O3 both stackers in the warn and refuse lists; O4 the sweep's guard check requires one `knit_exit()` in an `eval = !has_` chunk before the first attach; O5 `classes = "nestedtune_partial_summary"`; O6 a `requireNamespace` stop before the `tidymodels:::core` read; O7 `new.env(parent = globalenv())` and a stop on a missing argument; O8 a `\(` lambda check; O9 the inline-span pattern anchored on `` `r ``; O10/S1 Suggests order; O11 four lines re-wrapped; O12/S2 a blank line; O13 `tune::` dropped from prose; O15, O16, O19 prose. Rejected: O8's purrr and split-line parts (AC1 enumerates its tokens), O14 (the plan called for the chunk-level mute), O17 (the page holds the median to a tolerance), O18, O20, S3 (consistent with `references/luo2026.md`), S4. Planted-defect run of the amended sweep red on each class, the tree clean. Fresh `devtools::check()` after the fixes 0/0/0 in 6m 29s. PR conversation empty at every read. AC5 medians on a quiet machine: tuners 31.5 s, results 10.4 s, four pages 47.1 s. CI wait hit the ceiling once on d0f0819 (windows-latest pending); merged on green at 75ad92f, the resume and re-approval lines held local and carried here. Nothing graduated or retired.
