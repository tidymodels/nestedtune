# M088: Prose that fails the sweep cannot merge

**Status:** done (2026-09-11, PR #100 https://github.com/tidymodels/nestedtune/pull/100)

**Goal:** Every gating mode of `benchmarks/sweep-prose.R` runs on every pull request and on every local test run from the source tree, red on any hit.

**Outcome:** `.github/workflows/prose-sweep.yaml` runs the six gating sweeps (`sweep-prose.R`, `--spans`, `--plain`, each with `--roxygen`) on every push and pull request, one step per mode, every step running after an earlier hit, with the `paths-ignore` filter on both triggers, a workflow-keyed concurrency block and a 10-minute cap (job 103440877732 ran 46 s). Its `push` carries no `branches` filter because GitHub refuses `workflow_dispatch` on a workflow absent from the default branch. `tests/testthat/test-sweep-prose.R` runs the same six over the real pages and roxygen sources from the source tree, first asserting `--paragraphs` lists each page and `R/nested-tune-grid.R`, and skips under `R CMD check` with `sweep-prose.R not in the source tree`. `cairn/PROFILE.md` names `--plain` and `--roxygen --plain` in `verify`, the six in `consistency-gate`, and the workflow in the filter and hang-cap lists. Discrimination: a planted semicolon sentence in `README.Rmd` failed run 34653377391 at the plain-clauses step and its revert passed run 34653536495.

**Decisions:** none.

**Review:** three-lens fan-out. History and prior-review lenses found nothing. The diff-bug lens returned 12: fixed on the branch were steps stopping at the first hit, the yaml comment claiming the workflow gates a PR (now names the profile's merge clause), the profile's stale hang-cap and divergence lists, the unstated safety of the `setwd` restore under parallel files, and a timing comment naming a run without its job. Follow-up on the `sweep-prose.R` candidate row: the test hardcodes the page list the script owns, and nothing checks the yaml, test and profile name the same six invocations. Rejected: doubled runs while a PR is open (deliberate, bounded to the gate window), the CI-usage baseline not re-measured, `verify` naming only the plain modes (planned), long profile lines, unticked boxes before evidence. `devtools::check()` 0/0/0 at 630a822. CI: eleven checks green, `test-coverage` red with the known non-gating `readRDS` failure in `merge_coverage`, merged on the user's choice after devel-vctrs passed. Hygiene: the M85 lesson's rerun-the-sweeps sentence retired (the test now fails on unswept prose), the M50 lesson extended with the `workflow_dispatch` refusal.
