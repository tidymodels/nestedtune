# M087: The help pages pass the plain-English sweep

**Status:** done (2026-09-11, PR #99 https://github.com/tidymodels/nestedtune/pull/99; resolves #91 partial, a comment naming the shipped half and the candidate row posted on the issue)

**Goal:** Rewrite every roxygen sentence in `R/*.R` and `man-roxygen/*.R` that fails the plain clauses.

**Outcome:** `Rscript benchmarks/sweep-prose.R --roxygen --plain` prints `clean` at the merged head, as do `--roxygen` and `--roxygen --spans`. About 100 roxygen sentences rewritten across 19 `R/*.R` files and `man-roxygen/section-reproducibility.R`: 127 hits at the branch point (72 semicolon, 30 modal, 24 comma-ing, 1 slop). List items lost their `;` terminators; every modal counterfactual was restated in the present or as a condition ("would be refused" → "meets that condition", "may index only" → "must index only", the rule `nestedtune_bad_design` enforces); every roxygen `since` for `because` became `because`. `#'` count 2728 against 2718 at the branch point (cap 2989). `man/` regenerated (26 pages); one NEWS bullet. `devtools::check()` 0/0/0 twice; CI green on 12 checks. The reader prompt `cairn/surveys/M087-reader-prompt.md` and report `cairn/surveys/M087-reader-review.md` (234 entries) are committed.

**Decisions:** none milestone-local. The reader report is a triaged report under D-061, never a pass bar; the 30-word cap stands over the skill's 25 (M086's decision). No defect or amendment return.

**Review:** pass 1, three-lens fan-out plus the AC3 reader. Fixed at the gate: the race page's "when finetune refuses any outer fold's inner `rset`", an event `check_race_burn_in()` pre-empts (two lenses); "A version that built them earlier stays reproducible" and "Left alone, a fold's proposals then depend", hypotheticals stated as fact, recast as conditions; "A named one is refused" given its referent; "nothing it saves exists to be withheld" simplified; the NEWS bullet's "is rewritten" narrowed to "now passes". Rejected: unterminated list items and ragged line wraps (style). Prior-review lens: zero findings. Reader report: 12 since→because plus 4 unlisted sites, 3 present-perfect and 1 dangling sentence fixed; 43 entries under the 25-word cap, 24 article/"that" insertions and 2 "error as a verb" entries rejected; 149 passive-voice and `-ing`-verb entries (rules 3.6, 3.5) routed with M086's 49 to a DESIGN Known issues entry as an accepted limitation, the #91 row narrowed to its three concrete items. Hygiene: one LESSONS line (a modal removed from a counterfactual can name an actor who never reaches the event); the M84 row pruned; nothing graduated or retired.
