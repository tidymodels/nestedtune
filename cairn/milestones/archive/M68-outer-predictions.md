# M68: `save_pred` and `extract` reach the outer fit, and `collect_predictions()` and `collect_extracts()` stack what each fold kept

**Status:** done (2026-09-06, PR #78 https://github.com/tidymodels/nestedtune/pull/78)

**Goal:** Keep what each outer fold's scoring fit already produces, the assessment-set predictions and an extract of the fitted workflow, when the control asks for it, and read them back stacked with the fold labels through tune's two generics.

**Outcome:** `nested_fold_fit()` (`R/nested-tune-grid.R`) keeps `last_fit()`'s `.predictions` element when the control passed through `...` carries `save_pred = TRUE`, read through `[[`, and applies the control's `extract` function to the returned `.workflow` after the fit in its own `tryCatch()`; an erroring extract leaves the fold completed with `NULL` and one note at location `"outer extract"`; `failed_fold()` holds `NULL` in each. `new_nested_results()` writes `.predictions` then `.extracts` after `.notes` only when the slot asked, and `record_columns()` names both, so a present column is part of the dplyr and vctrs record. `collect_predictions()` and `collect_extracts()`, tune's generics re-exported, gain `nested_results` methods in `R/nested-results-collect.R` over `stack_fold_column()`, which took a `tables` argument (the extracts wrapped one value per row, a `NULL` kept as a row); `check_column_saved()` refuses with `nestedtune_column_not_saved` naming the slot when the recorded control did not ask or the column is gone, reading presence alone on an object without a procedure. Help: the four orchestrator pages' seventh heading "Kept from the outer fit", `save_workflow` (and `save_history`) left "Not returned"; the `collect_predictions.nested_results` topic with an executed example; pkgdown row; NEWS; DESIGN Function Families; `results.Rmd` paragraph. Tests: the `last_fit()` reference oracle for both columns on the grid orchestrator, presence on the four siblings through `expect_outer_columns_kept()`, `test-collect-readers.R`'s refusals and label columns, BC14 in `test-parallel-identity.R` (two daemons, both columns identical to serial, ledger row), the five invariant doors, the new methods in the dots probe and the final-fit refusal test.

**Decisions:** D-054 (the two slots govern the outer fit; the columns join the record when present; supersedes D-042's "Not returned" clause), D-055 (tune's column order; the D-030 annotation). Milestone-local: control-gated retention over always keeping predictions; a completed fold with a `NULL` extract and a note over failing it; `save_workflow` stays not returned since `extract = function(x) x` keeps a workflow; `summarize = TRUE` a candidate row; the extract applied after the fit rather than through `control_last_fit()`; the `...` fence kept first in every reader, AC3 narrowed to the two column refusals.

**Review:** three-lens fan-out on PR #78, two rounds. First round: [O] 11 findings, [S] 0 and 0; O4 a defect return (return count 1: `check_dots_empty()` ran ahead of `check_any_completed()` where AC3 said the latter won), resolved by amending AC3 at the mini gate with no code change; fixed O1 the column order, O2 the `[[` read, O3 the control-slot check, O6 D-055, O7 the once-per-repeat help clause narrowed to v-fold, O8 links, O9 `tables` documented, O11 a rewrap; O5 and O10 rejected. Second round on baf1313: [O] 11 findings, [S] 0 and 0; fixed P2 the refusal comment and NEWS clause, P4 a test title, P5 both readers in the final-fit refusal test, P6 the fold record's order, P7 the workflow read, P11 a rewrap; rejected P1 and P8 (hand-altered or pre-M46 objects), P3, P9 (O5 again), P10. Fresh `devtools::check()` 0/0/0 in 6m 0s; `NOT_CRAN=true devtools::test()` 7454 passed; `results.Rmd` rendered after installing the branch. PR conversation empty at every read. CI wait hit the ceiling once (8b9f2b3); merged on green at 595ee75, the stop line held local and carried here. Nothing graduated or retired.
