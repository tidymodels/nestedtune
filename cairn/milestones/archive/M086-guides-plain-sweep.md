# M086: The guides and README pass the plain-English sweep

**Status:** done (2026-09-11, PR #98 https://github.com/tidymodels/nestedtune/pull/98; resolves #91 partial, a comment naming the shipped half and the candidate row posted on the issue)

**Goal:** Rewrite every sentence of the six pages that fails the plain clauses, with those clauses added to `benchmarks/sweep-prose.R` as a `--plain` mode calibrated so the reference intro passes unchanged.

**Outcome:** `benchmarks/sweep-prose.R` gained `--plain` (six clauses: semicolon, contraction, has/have been, the five modals, an `-ing` word after a comma outside an eight-word exclusion list, a 95-phrase slop list copied from the SimpleEnglish skill's word swaps; each hit `file:line: <clause>: <text>`, exit 1) and `--pages <path>...` (replaces the page list, reads a headerless page, errors on no path). Plain mode leaves a mark where a backtick span was, so the words on either side never join into a fabricated hit. `tests/testthat/test-sweep-prose.R` runs the fixture `tests/testthat/fixtures/sweep-prose-plain.Rmd` (hand-authored: one sentence per clause, `MAY` wrapped over two lines, one all-spans sentence, one near-miss sentence per clause) and skips when `benchmarks/` is absent, as under `R CMD check`. 21 sentences rewritten across the six pages (11 modal, 6 comma-ing, 3 semicolon, 1 both); the reference intro untouched; word counts within 0.7% of the branch point; README re-knitted; one NEWS bullet. `devtools::check()` 0/0/0; CI green on 10 checks, `test-coverage` red with main's own covr `readRDS` failure.

**Decisions:** the 30-word cap kept over the skill's 25 because the reference intro has a 29-word sentence; every modal flagged, no exclusion needed since every counterfactual restated without one; the SimpleEnglish check-mode reader as the per-pass report under D-061, never a pass bar; `--plain` beside `--spans`/`--openings`/`--paragraphs` stays silent, the modes being exclusive. No defect or amendment return.

**Review:** pass 1, three-lens fan-out plus the AC5 reader. Fixed at the gate: stripped spans joining neighbours into fabricated matches; `--pages` with no path running clean over zero files; the `nested-cv.Rmd` ranking sentence that read as a capability claim (two lenses); all near-miss markers in one fixture sentence against AC3's "one per clause"; the NEWS bullet's `have been` and "phrases"; `air format` for CI. Rejected: mode exclusivity, latent regex escapes, the list-item sweep gap (existing candidate row), the one-word IP3 paragraph repair, the modal removals the milestone exists to make, the estimate page's counterfactuals in the simple tense. Reader report (`cairn/surveys/M086-reader-review.md`) 78 entries: 11 fixed (six since→because, two American spellings, one present perfect, two pronoun referents), 49 passive-voice and `-ing`-verb entries absorbed into the #91 candidate row, 18 rejected (the 25-word cap, paragraph length, list shape, phrasal verbs, style). Hygiene: one LESSONS line (a stripped span joins its neighbours); the M81 line's M84 extension corrected to D-061; the M83 row pruned; three candidate rows compressed for the byte budget; nothing graduated or retired.
