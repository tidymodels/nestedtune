# M082: The guides, README and NEWS are written for someone running nested cross-validation, and the page that served the author is gone

**Status:** done (2026-09-10, PR #93 https://github.com/tidymodels/nestedtune/pull/93; resolves #91 closes, the issue left open at the user's decision pending a human read of the writing quality, commented instead)

**Goal:** Rewrite the four guides, the parallel article, README and NEWS in plain prose for a tidymodels user, with each explanation on one page, the sections that served the author removed, and the why-nest article deleted.

**Outcome:** The four vignettes, `articles/parallel.Rmd`, `README.Rmd` (re-knit) and `NEWS.md` rewritten (14 files, +947/−2,543); `articles/why-nest.Rmd`, `why-nest-sim.R`, `why-nest.rds` and their `_pkgdown.yml` row deleted. Prose word counts 1198 / 1396 / 1263 / 1286 / 528 / 201 against caps 1500 / 1400 / 1500 / 1500 / 700 / 350. The five shared explanations each live on one page: the case for nesting, the no-subtraction rule and feature selection on `estimate.Rmd`; the fit-count arithmetic and the seeding paragraph on `nested-cv.Rmd`; every other page one linked sentence. The `nested_fit_resamples()` baseline moved from the guide to `tuners.Rmd`. `results.Rmd`'s survival example became one paragraph pointing at `?nested_tune_grid`; the parallel article's wire, `load_all()` and interrupt sections became one sentence pointing at the same page. README is a paragraph, Installation, one example and a five-link list. NEWS is 77 bullets of one or two sentences under its `0.0.0.9000` heading, superseded bullets dropped, the id-column sentence `test-id-columns.R:230` pins kept. The IP3 paragraph is one fixed text on `nested-cv.Rmd` and `estimate.Rmd`. No vocabulary hit (`--`, em dash, contract, invariant, rests on) on the seven files after YAML delimiters and HTML comments are filtered; "you" appears once, in the pinned sentence. `devtools::check()` 0/0/0; CI green on all twelve checks.

**Decisions:** the explanation homing above; the baseline's move to the tuners page; NEWS drops a bullet a later bullet replaced rather than shortening it, no release having shipped; the IP3 paragraph's text fixed at the implement gate, escalation offered on the ip-touching tripwire and declined. Two amendments: AC4 gained a filter for the YAML `---` delimiters and HTML comment lines the grep hit on every page; AC1's locator `:133` corrected to `:131` at the review gate, the check unchanged.

**Review:** three-lens fan-out, 19 findings (17 diff-bug, 0 blame-history, 2 prior-review); the prior-review lens's GitHub walk was blocked by a network failure inside the agent and re-run from the session (62 PRs, no human inline comment on the touched files). Fixed at the gate: a NEWS article count naming the deleted page; `results.Rmd` sweeping `collect_notes()` into the warning and refusing readers (it warns about none and refuses nothing); "biased upward" for a loss metric; the Tibshirani cell given without its learner caveat and the paper's own remedy; the `attr(x, "metrics")` and `tidymodels`-in-Suggests NEWS bullets dropped without successors; the case for nesting made twice on the guide; "each draws ... for every fold" wrong for `nested_final_fit()`; `fn` described as a tuning function; a "shows here" pointing at a deleted demonstration. Rejected: a two-sentence seeding pointer, three superseded NEWS bullets, the CI baseline record naming `why-nest-sim.R`, README's unguarded `library(tidymodels)`, a warning claim without an executed chunk (verified against `R/` and seven test files), the README memory table (at `?nested_resamples`). Nothing graduated or retired.
