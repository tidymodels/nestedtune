# M71: `nested_workflow_map()` runs a `workflow_set` through one nested design, and the readers stack each workflow's results under its id

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** IP2, IP3, IP4, GP1, GP3
- **Resolves:** —
- **Surface tier:** user-facing — a new exported orchestrator over workflow sets, a new results class, and reader methods on it
- **Branch/PR:** `m071-workflow-map` · https://github.com/tidymodels/nestedtune/pull/81

## Goal

Score every workflow of a `workflow_set` on the same outer folds of one nested design in one call, so the comparison a user makes across model families reads off one object with the workflow id beside the fold labels.

## Scope

**In:** `nested_workflow_map(object, fn = "nested_tune_grid", ...)`, shaped like `workflowsets::workflow_map()`, taking a `workflow_set` and the name of one of the six orchestrators, running each workflow in order with the arguments in `...` merged with that workflow's `option` entry, an unmarked workflow routed to `nested_fit_resamples()` (D-057's path) with the arguments narrowed to that orchestrator's formals, and the generator state preserved between workflows so each runs under the same per-fold seeds; a standalone `nested_results_set` class (D-010's rule) holding `wflow_id`, `workflow` and `result` per row; the six `collect_*` readers stacking the per-element tables under `wflow_id`; `print()`; `extract_workflow(x, id)`; `nested_final_fit(object, results, ..., id = NULL)` reading the workflow and its record from the set; entry refusals; `workflowsets` in Suggests, called by no package code (D-058); help, pkgdown, NEWS, DESIGN, a `tuners.Rmd` section and an `estimate.Rmd` paragraph on selection across a set. Lineage: the "`workflow_set` through one nested design" candidate row added 2026-09-06 at M68's plan gate.

**Out:** one `nested_results` with a `wflow_id` column (rewrites the class, every pooled reader and the procedure record; rejected at the gate, see the work log); `summary()`, `autoplot()` and `agreement()` on the set — a candidate row; dispatching workflow-by-fold units in one parallel round — a candidate row; a ranking or best-workflow fit over the set — deliberately absent, the `estimate.Rmd` paragraph says why (IP3); a different tuner per workflow — two calls; the final fit tying a fixed workflow to a no-tuning record on the single-workflow path — the M70 review candidate row stands.

## Acceptance criteria

- [x] AC1: `nested_workflow_map(object, fn = "nested_tune_grid", ...)` in `R/nested-workflow-map.R` takes a `workflow_set` and the name of one of the six orchestrators, runs every workflow of the set in order through that orchestrator with the arguments in `...` (a workflow with no `tune()` marker through `nested_fit_resamples()` instead, whatever `fn` names), a per-workflow entry of the set's `option` column overriding the same-named argument in `...` and, for each workflow, the arguments narrowed to what the orchestrator it routes to accepts: its formals other than `object`, and, for a workflow that routes to `fn`, the `control` in `...`; a workflow routed to `nested_fit_resamples()` instead takes a `control` only from its `option` entry, since a control's class is `fn`'s own and `nested_fit_resamples()` refuses another's; and returns a `nested_results_set`, a tibble of class `c("nested_results_set", "tbl_df", "tbl", "data.frame")` with columns `wflow_id`, `workflow` and `result`, one row per workflow in the set's order, carrying `fn` as an attribute. The generator state the map holds once its entry checks have run (the caller's, or the state a session that never drew initializes then) is reinstated before each workflow, so every workflow's fold `i` runs under the same two seeds. For every row, `result[[i]]` is `identical()` to the orchestrator that ran called by hand on `workflow[[i]]` with the same arguments after `set.seed()` of the same value, asserted in `test-nested-workflow-map-oracles.R` on a two-workflow set (one tuned, one fixed) for each of the five tuning `fn` values (the racers and the annealer under their existing skip guards, each with a `control` in `...`), on a two-workflow set of fixed workflows for `fn = "nested_fit_resamples"`, and on the three-workflow fixture for `fn = "nested_tune_grid"` with a per-workflow `grid` option on one tuned workflow.
- [x] AC2: A `nested_workflow_map()` call leaves the caller's RNG state as it found it (`.Random.seed` identical before and after, and `exists(".Random.seed")` unchanged in a session that never drew), and for `fn = "nested_tune_grid"` on a fixture in which every fold completes, a run under two mirai daemons is `identical()` to the serial run under the same seed, as BC17 in `test-parallel-identity.R` with its row in the `helper-time-budget.R` ledger.
- [x] AC3: `collect_metrics()`, `collect_selections()`, `collect_inner_metrics()`, `collect_notes()`, `collect_predictions()` and `collect_extracts()` on a `nested_results_set` return the same reader's table for each element row-bound in set order (the fixtures' set order differing from their `wflow_id` sort order) with `wflow_id` as the first column, `summarize = FALSE` on the first placing it before the fold label columns, equal to `dplyr::bind_rows()` of the per-element tables keyed by `wflow_id` (an element whose table has no rows contributes none); for each of the five readers other than `collect_notes()`, an element in which no outer fold completed is left out while another element completed one, with one warning of class `nestedtune_partial_summary` naming its `wflow_id`, an element that only partly completed raises that reader's `nestedtune_partial_summary` warning once with its `wflow_id` named in the message, and a set in which no element completed a fold is refused with `nestedtune_no_completed_folds`; `collect_predictions()` and `collect_extracts()`, after that refusal, refuse a set in which an element that would contribute rows did not keep the column, with `nestedtune_column_not_saved` naming its `wflow_id`; `collect_notes()` stacks every element, one in which no outer fold completed included, and refuses no set on that ground; and each of the six methods refuses a stray argument in `...` before any other check. Every clause but the last is asserted for each reader it names in `test-nested-workflow-map-readers.R`; the dots clause is the `test-dots-barrier.R` registry probe.
- [x] AC4: `print()` on a `nested_results_set` names the orchestrator label, the number of workflows, and per row the `wflow_id` with its completed and attempted fold counts (snapshot in `test-nested-workflow-map-readers.R`); `extract_workflow(x, id, ...)` returns the element's workflow; `workflowsets::rank_results()` and `tune::fit_best()` on the set raise an error rather than returning a table or a fit; and `nested_final_fit(object, results, ..., id = NULL)` given a `nested_results_set` as `object`, `results` missing and `id` naming one `wflow_id`, returns a fit whose `$selected`, `$tuning_seed`, `$fit_seed` and `predict()` on the fixture rows are `identical()` to those of `nested_final_fit(extract_workflow(x, id), x$result[[i]])` after `set.seed()` of the same value; an `id` naming no row is refused with `nestedtune_unknown_id`, and a set given with `results` supplied, or `id` given with a workflow as `object`, with `nestedtune_bad_final_fit_args`. Asserted in `test-nested-final-fit-set.R`.
- [x] AC5: Entry refusals, each asserted in `test-nested-workflow-map-checks.R` by condition class where one is named and each raised before any fold runs: `object` not a `workflow_set`, or one lacking the `wflow_id`, `info`, `option` or `result` column (`nestedtune_bad_workflow_set`); `fn` not one of the six orchestrator names (`nestedtune_bad_fn`); a name in `...` that is neither `control` nor a formal of the orchestrator `fn` names other than `object`, an `object` or an unnamed argument in `...`, or `...` carrying no `resamples` (`nestedtune_bad_dots`); an `option` entry named neither `control` nor a formal of the orchestrator its workflow routes to, or named `object` or `resamples` (`nestedtune_bad_option`, naming the `wflow_id`; asserted both for a name a workflow routed to `nested_fit_resamples()` cannot take and for an `object` or `resamples` entry); a fitted workflow in the set (`check_workflow()`'s refusal, asserted by its message naming the `wflow_id`); a workflow carrying a `tune()` marker in a set run with `fn = "nested_fit_resamples"` (`nestedtune_tuned_workflow`, naming its `wflow_id`); and every workflow's packages checked at entry through `check_workflow_pkgs()` (its existing class, naming the `wflow_id`).
- [x] AC6: A help page for `nested_workflow_map()` documenting each argument, the routing of an unmarked workflow, the seeding rule and the per-workflow `option` override; the set's behaviour on the six reader pages, on `extract_workflow`'s and on `nested_final_fit`'s; a pkgdown reference row; a NEWS entry; DESIGN Function Families and Architecture paragraphs; a section in `tuners.Rmd` running a baseline and two tuned workflows through one design and reading `collect_metrics()`; and a paragraph in `estimate.Rmd` stating that choosing among a set's workflows by their nested estimates is a selection the outer loop did not nest, so the chosen workflow's estimate is not an estimate of choosing the best of them, which is why the package registers no ranking or best-workflow method for the set; `devtools::check()` reports 0 errors, 0 warnings, 0 notes, and both pages render after installing the branch.

## Coverage

- AC1 → T1, T2, T3
- AC2 → T3
- AC3 → T4
- AC4 → T4, T5
- AC5 → T2
- AC6 → T6

## Tasks

- [x] T1: `workflowsets` joins Suggests (D-058). `tests/testthat/helper-orchestration.R` gains `wset_two(data)` (the tuned `det_workflow()` and the `fixed_workflow()`), `wset_fixed(data)` (two fixed workflows, the set the plain resampling orchestrator accepts) and `wset_three(data)` (the first two and `cont_workflow()` carrying its own `grid` option) built with `workflowsets::as_workflow_set()` over `make_reg_data()`, a `final_nested()` design with literal arguments (the M05 lesson), and a `memoised()` `wset_results(fn)` cache per orchestrator. Grep every holder of `extract_workflow`, `id`, `wflow_id` and `nested_results_set` before naming anything (the M41 lesson).
- [x] T2: Tests first in `test-nested-workflow-map-checks.R` (AC5), then the entry checks in `R/checks.R`: `check_workflow_set()` (class and the four columns), `check_map_fn()` (the six names), `check_map_dots()` (each name against the named orchestrator's formals less `object` plus `control`, no unnamed argument, `resamples` present), `check_map_options()` (each entry's names against the routed orchestrator's formals less `object` and `resamples` plus `control`, message naming the `wflow_id`), and `check_workflow()`, `check_workflow_pkgs()` and, under `fn = "nested_fit_resamples"`, `check_tuned_workflow()` per element with the `wflow_id` in the message, all run over the whole set before the first workflow runs.
- [x] T3: `R/nested-workflow-map.R`: the loop routing each workflow by `tuned_parameter_ids()` (empty → `nested_fit_resamples`), merging `option` over `...`, narrowing to the routed orchestrator's formals, calling it with `rlang::call2()` in the package namespace, and `new_nested_results_set()` with the `fn` attribute; the RNG envelope leaving the caller's state untouched (`snapshot_rng()` / `restore_rng()` as `nested_loop()` uses them). Oracle tests in `test-nested-workflow-map-oracles.R` (AC1, six `fn` values on `wset_two()`, the grid option on `wset_three()`), the net-zero and never-drew tests in `test-nested-workflow-map-rng.R`, and BC17 with its ledger row (AC2).
- [x] T4: `R/nested-results-set.R`: a `stack_set()` helper mapping a reader over the elements with a completed fold and binding under `wflow_id`; the six `collect_*` methods over it, each fencing `...` first; the all-failed refusal; `print.nested_results_set()` with its snapshot; `extract_workflow.nested_results_set(x, id, ...)`; the signature pin in `test-dots-barrier.R`; `test-nested-workflow-map-readers.R` (AC3, the print and extract halves of AC4).
- [x] T5: `nested_final_fit(object, results, ..., id = NULL)`: the set branch reading `workflow[[i]]` and `result[[i]]` by `id`, `nestedtune_unknown_id`, `nestedtune_bad_final_fit_args`, the `rank_results()` / `fit_best()` error probe; `test-nested-final-fit-set.R` (the rest of AC4).
- [x] T6: Roxygen for the export, the class, the reader pages, `extract_workflow` and `nested_final_fit`; `_pkgdown.yml` row under "Running the loop"; NEWS; DESIGN Function Families and Architecture; the `tuners.Rmd` section and the `estimate.Rmd` paragraph; `devtools::install()` then render both pages; `air format --check` on the touched files (the M56 lesson); `devtools::check()` (AC6).

## Work log

- 2026-09-06: created by /milestone-plan. Investigation by five [S] Explore subagents (orchestrator core, results class and readers, final fit and extracts, fixtures and doc surface, workflowsets API) with file:line citations; the `procedure` record, the tuner description and the results class carry no workflow, every pooled reader assumes one, and workflowsets imports tune with no reverse edge.
- 2026-09-06: criteria audit ran in full mode ([O] fresh reader): 7 findings, all fixed before the gate — arguments narrowed to the routed orchestrator's formals; the seeding rule stated and the identity oracle covering all six `fn`; readers promising `wflow_id` only for elements contributing rows; BC17 scoped to the grid orchestrator on a clean fixture; `extract_workflow` taking `id` ahead of `...`; the no-ranking claim turned into an error probe and the vignette's claim bounded to the registered methods.
- 2026-09-06: plan gate chose a standalone set of per-workflow results over one `nested_results` carrying a `wflow_id` column because the class, the procedure record and every pooled reader assume one workflow and the row's stacking need is met by `collect_metrics(summarize = FALSE)`; falsified by a reader a user needs that the per-element stacking cannot express, such as an estimate pooled across workflows.
- 2026-09-06: plan gate chose routing an unmarked workflow to `nested_fit_resamples()` over refusing a mixed set because a baseline beside tuned models on the same folds is the comparison the set exists for and the element's record names the procedure that ran (IP4); falsified by a user surprised that a named tuner did not run on one workflow.
- 2026-09-06: plan gate chose the same per-fold seeds for every workflow over distinct seeds per workflow because it mirrors `workflow_map()`'s single seed, pairs the comparison on seeds, and makes each element identical to a hand call; falsified by evidence that shared seeds correlate the workflows' estimates in a way a user's comparison must not carry.
- 2026-09-06: plan gate chose an `id` argument on `nested_final_fit()` over hand extraction because the set holds the workflow and its record together, so the wrong pairing cannot happen; falsified by a caller needing a final fit from a set with a workflow the set does not hold.
- 2026-09-06: plan gate chose `workflowsets` in Suggests over Imports because the package reads the set's columns and calls no workflowsets function; falsified by a validator or extractor of the set the package must call at run time.

- 2026-09-06: /milestone-implement started; branch `m071-workflow-map` cut from the pushed default branch.
- 2026-09-06: question gate chose amending AC3 so `collect_notes()` stacks every element, failed ones included, over the leave-out rule for all six readers, because the single-object method reads every fold and a failed element's notes are the reason to ask; falsified by a user needing the failed elements out of the notes table.
- 2026-09-06: question gate chose re-signalling the failed-fold and partial-summary warnings from inside one element with the `wflow_id` at the front of the message, same class, over passing them through, because on a set the fold labels alone do not say which workflow lost folds.
- 2026-09-06: question gate chose refusing at entry a name in `...` that the orchestrator `fn` names does not take (`nestedtune_bad_dots`), with `resamples` required in `...` and `object`/`resamples` refused as option names, over silently narrowing every name, because a typo would otherwise be dropped without a word.
- 2026-09-06: mini gate amended AC1: the identity oracle for `fn = "nested_fit_resamples"` runs on a set of two fixed workflows, since that orchestrator refuses a tuned workflow at entry and the planned mixed set had no result to compare (substantive; user accepted).
- 2026-09-06: re-audit: AC3 (full) — 3 findings: silent on an element whose run did not keep the column; leave-out and all-failed clauses bound to no enumerating probe; the signal for a left-out or partial element unstated; all three fixed.
- 2026-09-06: re-audit: AC3 (full) — 3 findings: closing sentence contradicted the dots clause's citation; column-not-saved refusal unordered against the fold-state clauses; set order unfalsifiable against sort order; all three fixed and the wording accepted by the user at the gate (the criterion's stop).
- 2026-09-06: re-audit: AC1 (full) — 3 findings: narrowing to formals excluded `control`; the shared-seed clause unsatisfiable beside AC2 in a never-drew session as worded; the marked-workflow refusal under the plain resampling orchestrator belonged in AC5; all three fixed, the refusal moved to AC5.
- 2026-09-06: re-audit: AC1 (full) — 3 findings: no oracle passed a `control`; `object` admitted among the narrowed formals; AC1 admitted a `control` option AC5 refused; all three fixed and the wording accepted by the user at the gate (the criterion's stop).
- 2026-09-06: re-audit: AC5 (full) — 3 findings: "named formal" ambiguous between blocking `resamples` and admitting `object`; `check_workflow()` clause promised classes it has no reachable shapes for (`as_workflow_set()` refuses a model-less or preprocessor-less workflow); the option probe one exemplar for two axes; all three fixed and the wording accepted by the user at the gate.
- 2026-09-06: amended AC1, AC3 and AC5 written as accepted at the gate; T2 reworded for the two checks AC5 gained (minor). Coverage unchanged: AC5 → T2.
- 2026-09-06: T1 done. `workflowsets` in Suggests; `wset_two()`, `wset_fixed()`, `wset_three()`, `plain_workflow()`, `cont_grid()`, `MAP_FNS`, `wset_map_args()`, `wset_results()` and `skip_if_no_wset_fixture()` in `helper-orchestration.R`, built with `as_workflow_set()` (the ids are the names, each element the fixture workflow itself); the absent-package fixture moved from `test-workflow-pkgs.R` into the helper so the checks file can reach it under parallel test files; name grep found `id` only as a fold-label column and an argument of `extract_workflow.workflow_set()` upstream; `devtools::test()` on the fixture-adjacent files (fixture-cache, suite-hygiene, dots-barrier, workflow-pkgs) clean.
- 2026-09-06: mini gate amended AC1's narrowing clause: a `control` in `...` reaches only the workflows that run through `fn`, and a workflow rerouted to `nested_fit_resamples()` takes a control from its `option` entry alone, because that orchestrator refuses a racing, Bayesian or annealing control by class (measured 2026-09-06) and the accepted clause named a call that cannot run; the user chose this over converting the shared slots or passing it through (substantive; AC1 at its re-audit stop, so decided at the gate without a further reader).
- 2026-09-06: T2 done. `check_workflow_set()`, `check_map_fn()`, `check_map_dots()`, `check_map_options()`, `route_workflow()` and `orchestrator_args()` in `R/checks.R`; `check_workflow()` and, under `fn = "nested_fit_resamples"`, `check_tuned_workflow()` run per element with the refusal re-signalled naming the `wflow_id`; `test-nested-workflow-map-checks.R` asserts each refusal by class with the stream and the dispatch record untouched. `workflowsets::option_add()` refuses `object` itself, so that entry is planted by hand in the test. Full `devtools::test()` at this checkpoint: one failure, `suite-hygiene` reading `test-parallel-identity.R:973` between BC17's append and its ledger row (both on disk before the next run); everything else clean.
- 2026-09-06: T3 done. `R/nested-workflow-map.R`: the routing loop, `map_args()` (option over `...`, narrowed to the route, a `...` `control` dropped on a rerouted workflow), `run_orchestrator()` evaluating a `call2()` in an environment binding every argument to a name, `for_workflow()`/`resignal_for_workflow()` re-signalling the failed-fold warning and any error with the id, the seed envelope (`restore_map_rng()`: the entry state reinstated before each workflow, initialized and removed again in a never-drew session), `new_nested_results_set()`. Oracles: every element `identical()` to the hand call for all six `fn` (the sixth on `wset_fixed()`), the grid option on `wset_three()`, a `...` control beside an option control, the warning and error re-signal; `test-nested-workflow-map-rng.R` (net zero under Wichmann-Hill, never-drew leaves no state and the same seeds, same seed twice identical on ranger, error mid-set restores); BC17 with its ledger row. A default argument forced after `set.seed()` keyed the fixture set's recipe ids to `make_reg_data()`'s seed, fixed with `force(data)`.
- 2026-09-06: T4 done. `R/nested-results-set.R`: `stack_set()` (the all-failed refusal, the left-out warning naming the id, each element's reader under `for_workflow()`, the `wflow_id` collision refusal, `vec_rbind()`), the six methods, `print.nested_results_set()` (orchestrator and its registry label, workflow count, one bullet per row with completed and attempted counts and the routed label), `extract_workflow.nested_results_set(x, id, ...)` over `match_set_id()` (`nestedtune_unknown_id`); `test-nested-workflow-map-readers.R` checks every clause per reader against `dplyr::bind_rows(.id = "wflow_id")`, two print snapshots, the `rank_results()`/`fit_best()` probe by message; `test-dots-barrier.R` pins the new signature and probes the six new methods.
- 2026-09-06: T5 done. `nested_final_fit(object, results, ..., id = NULL)`: the set branch reads the row's workflow and record, `check_final_fit_set_args()` refuses the three mixed shapes (`nestedtune_bad_final_fit_args`); `test-nested-final-fit-set.R` asserts `$selected`, the two seeds and `predict()` identical to the hand pairing on both rows, and the refusals.
- 2026-09-06: T6 in progress. Help pages, `_pkgdown.yml` rows (`pkgdown::check_pkgdown()` clean), NEWS, DESIGN Function Families and Architecture, the `tuners.Rmd` section and the `estimate.Rmd` paragraph written; branch installed and both pages rendered (`tuners.Rmd` in 118 s), the rendered set section read back against its prose; `devtools::check()` running at this checkpoint, its result and the T6 tick to follow.
- 2026-09-06: first `devtools::check()` on the branch: 0 warnings, 0 notes, one test failure, `test-fixture-cache.R`'s formal-axis registry naming `nested_final_fit()`'s new `id` formal with no variant; an `id = "tuned"` variant registered against the `NULL` default (keys only, nothing runs); check re-running.
- 2026-09-06: T6 done; second `devtools::check()`: 0 errors, 0 warnings, 0 notes, the suite inside it clean. Status set to review; all tasks checked.
- 2026-09-06: /milestone-review started; PR #81 opened as draft; AC1–AC5 evidenced and ticked (checkpoint: `devtools::check()`, the vignette renders and the three reviewers pending).
- 2026-09-06: review gate: fix-now O4, O9, O10, F1 committed on the branch; `test-dots-barrier.R` and `air format --check` clean after them.
- 2026-09-06: step-7 approval: PR #81 approved for merge.

## Decisions

- 2026-09-06: a `control` in `...` reaches only the workflows that run through `fn`; a workflow rerouted to `nested_fit_resamples()` takes a control from its `option` entry alone. Because a control's class is the contract (D-042) and `nested_fit_resamples()` refuses a racing, Bayesian or annealing control by class, so passing it through failed the baseline of every mixed set under those three orchestrators; converting the shared slots was rejected as behaviour the identity oracle would have to rebuild by hand. Falsified by a user needing one `save_pred` to reach every workflow of a mixed set in one argument.
- 2026-09-06: a condition raised inside one workflow's run or reader is re-signalled with the `wflow_id` at the front of its message, under its own class, naming the map or the reader as its call; a condition of another class passes through untouched. Because on a set the fold labels alone do not say which workflow a failed-fold or partial-run warning is about, and muffling the original keeps one warning per event. Falsified by a caller matching the orchestrator's call in a handler.
- 2026-09-06: in a session that never drew, the map initializes the generator, reinstates that state before each workflow, and removes it on exit with the kind restored, where an orchestrator alone keeps the state it created. Because the orchestrators' never-drew restore reseeds the kind from the advanced state, so the second workflow would start from a different state than the first and the workflows would not share seeds; the removal is what AC2 promises. Falsified by a user relying on the state a map call leaves behind.

## Review

Review of PR #81 on 2026-09-06; every result below from a fresh run on the branch head (`8ca5b65`), `main` unmoved since the branch was cut. Files run with `NOT_CRAN=true` where a test carries `skip_on_cran()`.

- AC1: `test-nested-workflow-map-oracles.R` — 10 tests, 108 expectations, 0 failures, 0 skips (finetune present, so the racers and the annealer ran). Six `fn` blocks each assert the class vector, the three columns, set order not equal to sort order, the `fn` attribute, every element `identical()` to the hand call under `set.seed(31)` (the fixed workflow through `nested_fit_resamples()` with design and metrics alone; the racers and annealer with a `control` in `...`), the routed tuner read off each record, and both elements' seed columns equal; `wset_three()` with a per-workflow `grid` option, the `...` control beside an option control, and the re-signalled warning and error each pass. Evidence recorded → ticked.
- AC2: `test-nested-workflow-map-rng.R` — 4 tests, 15 expectations, 0 failures: `.Random.seed` and the kind identical before and after under Wichmann-Hill; a never-drew session left with no `.Random.seed` and its kind, the two elements sharing seeds; same seed twice identical on ranger and a different seed differing; an error mid-set restoring the state. `test-parallel-identity.R` under `NOT_CRAN=true` — 19 tests, 164 expectations, 0 failures; BC17 passed (serial then two daemons, every fold completed, `identical()` on the whole set); its ledger row at `helper-time-budget.R:816`. Evidence recorded → ticked.
- AC3: `test-nested-workflow-map-readers.R` under `NOT_CRAN=true` — 9 tests, 169 expectations, 0 failures. Each of the seven reader calls (the six plus `summarize = FALSE`) equals `dplyr::bind_rows(.id = "wflow_id")` of the per-element tables with `wflow_id` first, in set order (the fixture's `c("tuned", "fixed")` is not its sort order), `summarize = FALSE` placing it before `id`; the fixed element contributes no selection or inner-metrics row. Looped over the five completed-fold readers: the partly-completed element raises `nestedtune_partial_summary` once per element naming its id with the generic as the call; the all-failed element is left out with one such warning naming it; a set with no completed fold is refused with `nestedtune_no_completed_folds`; `collect_predictions()` and `collect_extracts()` refuse the unsaved column with `nestedtune_column_not_saved` naming `"fixed"` and, on an all-failed set, refuse for the folds first. `collect_notes()` stacks every element in each of those shapes and warns on none. Dots clause: `test-dots-barrier.R` — 8 tests, 58 expectations, 0 failures; the registry probe runs over every `S3method()` in `NAMESPACE`, where all six set methods are registered (`NAMESPACE` lines 10–22). Evidence recorded → ticked.
- AC4: the readers file's print test — two snapshots match (`_snaps/nested-workflow-map-readers.md`) and the text names `nested_tune_grid()`, `grid search`, `Workflows: 2`, and each id with its completed-of-attempted count; `extract_workflow(res, id)` returns the element's workflow and refuses a missing, numeric, length-two or unknown `id` with `nestedtune_unknown_id`, the dots fence first; `workflowsets::rank_results()` and `tune::fit_best()` each error by message. `test-nested-final-fit-set.R` — 3 tests, 25 expectations, 0 failures: on both rows `$selected`, `$tuning_seed`, `$fit_seed` and `predict()` on five rows identical to the hand pairing under `set.seed(41)`; `nestedtune_unknown_id` with the stream untouched; the three mixed shapes refused with `nestedtune_bad_final_fit_args`. Evidence recorded → ticked.
- AC5: `test-nested-workflow-map-checks.R` — 7 tests, 95 expectations, 0 failures; every refusal asserted by class, named for `nested_workflow_map`, with `.Random.seed` and the dispatch record untouched: a workflow and a column-less `workflow_set` tibble (`nestedtune_bad_workflow_set`); `"tune_grid"`, a number and two names (`nestedtune_bad_fn`); a typo, another orchestrator's formal, a stray name beside `control`, an unnamed argument and a call with no `resamples` (`nestedtune_bad_dots`); `iter` on a grid-routed workflow, `grid` on a fixed one, and `resamples` and a planted `object` entry (`nestedtune_bad_option`, naming the id); a fitted second element (`check_workflow()`'s message naming `"fitted"`); the mixed set under `fn = "nested_fit_resamples"` (`nestedtune_tuned_workflow` naming `"tuned"`); the absent-package second element (`nestedtune_pkg_not_installed` naming `"absent"`). Evidence recorded → ticked.
- AC6: `man/nested_workflow_map.Rd` documents `object`, `fn` and `...`, a Routing section (the unmarked workflow to `nested_fit_resamples()`, the narrowing, the `control` rule, the `option` override) and a Seeds section; the set's reader behaviour is one page with the six `collect_*.nested_results_set` aliases (the package holds three reader pages for the six readers, so "the six reader pages" resolves to those six topics; the three existing pages carry no pointer to it, raised as F1 below), plus `extract_workflow.nested_results_set.Rd` and the set branch on `nested_final_fit.Rd`; `_pkgdown.yml` rows for the four topics (`pkgdown::check_pkgdown()` clean); a NEWS entry with no milestone number; DESIGN Function Families and Architecture paragraphs in the diff; `tuners.Rmd` "A set of workflows on one design" runs the forest, a baseline and a tuned components workflow and reads `collect_metrics()`; `estimate.Rmd` carries the paragraph (selection the outer loop did not nest, hence no ranking or best-workflow method). `devtools::check()` from the branch head: 0 errors, 0 warnings, 0 notes (27 min). Branch installed with `devtools::install()`; both pages rendered with `rmarkdown::render()`, the tuners page showing `Workflows: 3` and the baseline's `nested_fit_resamples()` label. Evidence recorded → ticked.
- Driving RR: none; no projections to juxtapose.

Consistency gate (2026-09-06): `cairn_validate.py` exit 0, 16 PASS, advisories only (references staleness); no DESIGN principle text changed, so `cairn_impact.py` skipped; `devtools::document()` inside the check left `NAMESPACE` and `man/` with no diff; `README.Rmd` untouched by the branch and at the same commit as `README.md`; `pkgdown::check_pkgdown()` no problems; NEWS entry present with no milestone number; no new top-level file, check reports 0 notes; `air format --check` clean on every touched R file; full check clean as under AC6.

Independent review (three fresh-context reviewers on `git diff main..HEAD`), findings ranked most severe first as each reviewer ranked them, with the disposition decided at the gate recorded beside each:
- O2: `nested_results_set` registers no `[`, `dplyr_reconstruct()`, `names<-` or `vec_restore()` method, so a column subset keeps the class and `print(res[, 1])` warns six times and prints "0 of outer folds completed (unknown)" instead of refusing; the invariants D-031/D-032/D-035/D-048 built for `nested_results` are absent on the set. No AC covers it.
- O3: `collect_metrics(res[0, ])` refuses with `nestedtune_no_completed_folds` and the bullet "All 0 workflows failed", reachable only through O2 since the entry check refuses a zero-row set.
- O4 (also P1 from the prior-review lens, and seen by the orchestrator at AC3): `DOTS_PROBED_METHODS` in `test-dots-barrier.R` names four of the six set readers, omitting `collect_selections` and `collect_inner_metrics`; the registry loop still probes both, so only the named guard is short.
- O5: `resignal_for_workflow()` rebuilds the condition with `abort()`/`warn()` from its message and own classes, dropping the parent chain and any data fields; the milestone decision accepted losing the call only.
- O6: a `NULL`-valued `option` entry removes the `...` argument rather than overriding it; every affected argument defaults to `NULL` on all six orchestrators, so the effect is the same today.
- O7: the never-drew exit restores `sample.kind` unconditionally, so under R's legacy `"Rounding"` sampler the call emits R's sampler warning.
- O8: the `rank_results()`/`fit_best()` probe asserts foreign message text with no upstream condition class available.
- O9: the set readers' `@return` says `NA` fills a missing column where `vec_rbind()` fills a missing list column with `NULL`.
- O10: DESIGN Function Families writes `nested_final_fit(object, results, id = NULL)` without the `...` barrier the signature carries.
- O11: `capture_dots()` runs before `check_workflow_set()` and `check_map_fn()`, so an error evaluating a dots argument surfaces ahead of those refusals.
- S1: `restore_map_rng()` has a body near `restore_rng()`'s; the comment states the different contract (reinstated per workflow, removed on exit).
- S2: the `id = function() "tuned"` fixture-key variant in `test-fixture-cache.R` pairs a workflow object with an `id`, a shape the final fit refuses, but `fixture_key()` only hashes and never calls.
- F1 (orchestrator, at AC6): the three existing reader pages (`collect_metrics.nested_results`, `collect_predictions.nested_results`, `collect_selections`) carry no `@seealso` to the set page.
- Refuted by the diff-bug reviewer with evidence: `which` shadowing in `stack_set()` (R resolves the function), `capture_dots()` leaving `.Random.seed` (it snapshots and restores), `check_workflow_pkgs()` unimplemented (called inside `check_workflow()`), `extract_workflow(x, id, ...)` tripping the S3 consistency check (upstream has the same shape; check 0 notes), subsetting losing the `fn` attribute (row subsets keep it). The history lens found no undone intent and no contradicted decision; the prior-review lens found no regressed lesson and no real GitHub review threads to walk.
- Dispositions at the gate (2026-09-06, user accepted the recommended triage): O4/P1 fix now (the two names added to `DOTS_PROBED_METHODS`); O9 fix now (`@return` names `NULL` for a list column); O10 fix now (DESIGN carries the `...` barrier); F1 fix now (`@seealso` to the set page on the three reader pages); O2, O3 and O5 follow-up, one candidate row at post-merge hygiene; O6 rejected (every affected argument defaults to `NULL`, so removal and override coincide); O7 rejected (only R's legacy `"Rounding"` sampler in a never-drew session); O8 rejected (no upstream condition class exists; the probe is the best available); O11 rejected (the same dots-first shape as every orchestrator); S1 rejected (a deliberate, commented variant with a different contract); S2 rejected (`fixture_key()` hashes and never calls).
- conversation: PR #81 — empty read (no reviews, no comments, no review threads).
