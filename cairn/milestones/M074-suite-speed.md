# M74: The check suite and the help-page examples run faster without dropping an assertion

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** IP2, GP4
- **Resolves:** —
- **Surface tier:** user-facing — the test suite and CI caps are internal, but the help-page examples the milestone lightens are documentation every user reads
- **Branch/PR:** `m074-suite-speed` · https://github.com/tidymodels/nestedtune/pull/84

## Goal

Cut the time the check suite and the help-page examples take, with every assertion the suite makes today still made and the check step caps M72 set left in place.

## Scope

**In:** the reference loops the oracle files build by hand go through `memoised()`, and each selection-rule block runs its reference tuning stage once per configuration; blocks that rerun the same un-memoised orchestrator call twice (the rng "same seed" / "different seed" pairs for bayes, race and anneal; `test-nested-tune-bayes-results.R`'s two inline-control runs; `test-nested-tune-finalize.R`'s second grid run; `test-nested-final-fit-oracles.R`'s repeated fit-and-reference pair) share one run; `test-parallel-identity.R` reuses one 2-daemon and one 3-daemon pool across the blocks that only need a primed pool, with a snapshot probe before each reuse and private pools for the block that pollutes daemons and the block that kills one; `helper-time-budget.R` re-summed; a lighter shared example run across the 16 help pages that build one; `benchmarks/time-examples.R`; the `check-r-package` step cap at M72's 40 minutes on the devel and windows legs and 30 on the other three, the yaml comment and the PROFILE test-doctrine slot stating the figures and M74's measurement; NEWS entry for the example change.

**Out:** every leg's check step within 24 minutes with the cap back to 30 everywhere, and the coverage step within 14 → the ROADMAP candidate row added 2026-09-07 at this re-cut (three attempts of `294ca2b` put the windows step at 26.6–28.0 minutes; the 12–14% suite cut does not reach it). Deleting blocks whose claim another block asserts, and thinning the harness-testing files → M75. A disk-backed fixture cache shared across workers → stays in the M52-leftovers candidate row (measured 2026-09-07: 873 CPU-s on 4 workers against 690 serial, so cross-worker rebuilds are at most a quarter of the CPU and do not meet M57's falsifier). Smaller fixture data or fewer trees in the oracle fixtures → not touched, since the metric-separating and unstable fixtures' properties were found by search (M18 lesson). Vignette render time → unchanged (M66 owns its cap). The `R-CMD-check-hard.yaml` and `test-coverage.yaml` job caps → unchanged.

## Acceptance criteria

- [x] AC1: `Rscript benchmarks/profile-tests.R 3` on the maintainer's machine reports a suite total (median of three runs) on the branch head of at most 90% of the total the same command reports on a checkout of the branch point (`2f6c0e6`), the two measured back to back in one sitting, with every one of the head's three runs reporting `fail 0 | skip 0` and a `pass` count no lower than the branch point's (9324), the script printing the counts per run.
- [x] AC2: Every call site of a `reference_*` helper under `tests/testthat/` — enumerated by `grep -n -E 'reference_[a-z_]+\(' tests/testthat/*.R`, less the definitions in `helper-orchestration.R` and `test-nested-tune-grid-results.R`, less the sites of `reference_select()` and `reference_with_rule()`, which apply a rule to a reference the memoised helpers built, and less the five `reference_last_fit()` sites in `test-nested-tune-grid-results.R`, whose per-fold `last_fit()` reference is deliberately rebuilt — is reached through `memoised()`; and in each of the four selection-rule blocks (`test-nested-tune-grid-oracles.R` "AC1 selection rules", `test-nested-tune-bayes-oracles.R` "AC1 selection rules", `test-nested-tune-race-oracles.R` "AC1 selection rules", `test-nested-tune-sim-anneal-oracles.R` "AC1 selection rules") the reference tuning stage runs once per workflow-design-seed configuration with the three rules applied to that one run, the serial fixture-cache report showing one `reference_*` signature per configuration.
- [x] AC3: `test-parallel-identity.R` starts at most three daemon pools per run — the `start_daemons`, `start_daemons_undispatched` and `start_mixed_daemons` call sites in the file, none inside a loop, listed by `grep -n -E '(start_daemons|start_daemons_undispatched|start_mixed_daemons)\(' tests/testthat/test-parallel-identity.R`, the file's only other `mirai::daemons(` calls taking 0, and the `helper-time-budget.R` ledger carrying one row each for those sites and no further `start_*` row for the file — and every block that reuses a shared pool — every `test_that()` block from the 2-daemon pool's start block to BC9 inclusive and from the 3-daemon pool's start block to the block that stops it exclusive, the start blocks excepted: 18 and 4 blocks, 22 call sites by `grep -c -E '^\s*shared_daemons\(' tests/testthat/test-parallel-identity.R` — holds a `shared_daemons(` call run before the block's parallel run, whose probe compares `daemon_state_snapshot()`, per daemon keyed by its pid, against the snapshot taken when that pool started: the option the package reads, the library variables `R_LIBS`, `R_LIBS_USER` and `R_LIBS_SITE`, and `.libPaths()` field for field, and the loaded namespaces and `search()` as supersets of the start snapshot's, since the package's own attach step adds to both on a daemon's first fold; and the file's serial-versus-parallel identity assertions — the `expect_identical()` calls whose first argument is `parallel`, an element of it, `polluted` or `p_notes`, listed by `grep -n -E 'expect_identical\((nrow\()?(parallel|polluted|p_notes)' tests/testthat/test-parallel-identity.R` at the branch point and at the head — compare the same set of object pairs at the two refs, every pair the branch point listed inside a `for (n in c(2L, 3L))` loop listed twice at the head, once per daemon count. (RB tripwire: ip-touching)
- [x] AC4: The `check-r-package` step cap in `R-CMD-check.yaml` is 40 minutes on the devel and windows legs and 30 on the other three, the yaml's cap comment and the `cairn/PROFILE.md` test-doctrine slot state those figures, and on the measured head (a pushed commit from which the merge-time branch head differs only under `cairn/`) each of the five legs' `check-r-package` steps has a `success` conclusion in one workflow attempt, its minutes read from the step's `started_at` and `completed_at` in `gh api repos/tidymodels/nestedtune/actions/runs/<id>/jobs`.
- [x] AC5: The active profile's verify slot is clean on the branch head — `devtools::check()` 0 errors, 0 warnings, 0 unjustified notes — and `air format --check` is clean on every `.R` and `.Rmd` file the branch touches (`git diff --name-only 2f6c0e6 | grep -E '\.(R|Rmd)$'`).
- [x] AC6: `Rscript benchmarks/time-examples.R 3` (added here: `tools::Rd2ex()` per page under `man/`, each sourced in one process, with and without `\donttest`, three runs, medians) on the branch head reports totals of at most 60% of the totals the same command reports on a checkout of the branch point, without and with `\donttest`, the two measured back to back on the maintainer's machine, each pass completing with no error; every page that had an `\examples` section at the branch point still has one calling the same set of exported functions (the `NAMESPACE` export names found in that page's `\examples` at the two refs); `grep -l dontrun man/*.Rd` is empty; and `devtools::document()` produces no diff.

## Coverage

- AC1 → T1, T2, T3, T4, T6
- AC2 → T2
- AC3 → T4
- AC4 → T7
- AC5 → T8
- AC6 → T5

## Tasks

- [x] T1: Record the branch point: `Rscript benchmarks/profile-tests.R 3` output, and the last two default-branch runs' per-leg `check-r-package`, test, example and vignette times (2026-09-07 runs 34129642542 and 34077230243) in the work log.
- [x] T2: Wrap every `reference_*` call in `memoised()` (the seed-scoped call the reference takes must be inside the memoised expression so the key sees it); in the four selection-rule blocks build one reference per configuration and apply the three rules through `reference_select()`; check the serial cache report for one `reference_*` signature per configuration.
- [x] T3: Merge the rerun pairs: bayes-rng `:12`/`:52`, race-rng `:21`/`:49`, anneal-rng `:23`/`:50` (one seed-77 run per tuner, the different-seed run beside it); bayes-results `:249`/`:279` into one run under an inline control; finalize AC2 (`:220`) reads AC1's recorded run; final-fit-oracles `:65`/`:128` share one memoised fit-and-reference pair as `bayes_final_and_reference()` does.
- [x] T4: In `helper-parallel.R` add `shared_daemons(n)` (start once per file, `daemon_state_snapshot()` at start, `expect_identical()` against it before each reuse) and `daemon_state_snapshot()`; convert the 19 blocks that only need a primed 2-daemon pool and the n=3 arms of BC1, BC10, BC12, BC13; BC9 and BC3 keep private pools; re-key the `helper-time-budget.R` ledger to the new `file:line` sites and re-sum per file against the CI caps (M16 lesson). Add a Wichmann-Hill kind check to the probe so a missing pin still fails (M07 lesson).
- [x] T5: One example helper shape across the 16 pages that build a tuning run: `mtcars`, 2 outer × 2 inner folds, `num_comp = 1:2`, the rest of each example unchanged; `\donttest` untouched; `benchmarks/time-examples.R` committed with a header stating its method; `devtools::document()`; NEWS bullet.
- [x] T6: `benchmarks/profile-tests.R` prints `pass | fail | skip` for each run; then `Rscript benchmarks/profile-tests.R 3` on a checkout of the branch point and on the head, back to back in one sitting, and `Rscript benchmarks/time-examples.R 3` the same way; record the four totals and the head's per-file table in the work log; if the head's suite total is above 90% of the branch point's, name the file and return to T2–T4.
- [x] T7: Restore `timeout-minutes` on the `check-r-package` step to 40 on the devel and windows legs and 30 elsewhere (the expression M72 wrote); rewrite the cap comment's last paragraph to state M74's cuts, the local measurement, and that the two legs keep 40 because three attempts of `294ca2b` ran the windows step at 26.6–28.0 minutes; update `cairn/PROFILE.md` lines 47–49 to match; retitle PR #84 to this file's title; push the measured head and read the attempt's step times from the jobs endpoint.
- [x] T8: `devtools::check()` and `air format --check` on the touched files.

## Work log

- 2026-09-07: created by /milestone-plan. Criteria audit ran in full mode (user-facing tier and an ip-touching tag) on two fresh [O] readers: ten findings on AC1–AC6 (pass floor added to AC1; AC2's second clause rewritten around the four selection-rule blocks since the cache report cannot see a rebuild under a different RNG state; AC3 counts ledger `times` multipliers over all three pool-starting helpers and names a snapshot helper; AC4 stated on the slowest leg; AC5's recording clause dropped; AC6 matched to the verify slot; the suggestion to pin the cap in `test-ci-workflows.R` declined as widening a checker over repo-internal artifacts) and five on AC7 (examples must complete, median of three, exported-function set pinned, `document()` no diff, branch point re-measured by the same script).
- 2026-09-07: plan gate chose a speed-only milestone with pruning planned as M75 over folding pruning in, because review cannot separate a timing regression from a dropped claim in one diff; falsified by M75 finding its deletions cannot be verified without M74's memoised fixtures.
- 2026-09-07: plan gate chose sharing one daemon pool with a state probe over keeping 26 restarts (M12's decline, whose promotion condition was such a probe), because 19 of 21 two-daemon starts serve blocks needing only a primed pool; falsified by a probe difference between blocks on a run whose identity assertions still pass, which would show the probe cannot see the leak M12 feared.
- 2026-09-07: plan gate chose a 24-minute bar with every cap back to 30 over keeping M72's 40s, because the yaml's own stance is that a leg nearing its cap is a suite to make faster; falsified by three attempts of the measured head with a leg above 24 on runners that were not slower than the branch point's.
- 2026-09-07: plan gate chose including the example pages over a candidate row, at the user's choice; falsified by a page whose lighter design changes what its printed output demonstrates.
- 2026-09-07: plan step 2 chose cheaper reference and rerun paths over a shared disk cache (M57's decline stands on today's 873-versus-690 CPU-second measurement) and over smaller fixture data (M18's searched-property fixtures); falsified by a per-worker report showing rebuilds above a quarter of the CPU, or by an oracle fixture whose property survives a smaller size.
- 2026-09-07: question gate — the race help page keeps inner v = 5 (the burn-in check refuses a race with no more inner resamples than its `burn_in` of 2) and goes to 2 outer folds; BC9 pollutes the shared 2-daemon pool in place as the last 2-daemon block instead of restarting; the n=3 arms of BC1, BC10, BC12 and BC13 become their own blocks on a shared 3-daemon pool, each rebuilding its serial reference, since mirai holds one pool at a time.
- 2026-09-07: T1 (CI part) — default-branch runs 34129642542 / 34077230243 `check-r-package` minutes: ubuntu release 26.9 / 26.3, devel 20.9 / 28.2, oldrel 20.5 / 25.2, macOS 22.9 / 22.0, windows 18.0 / 23.3; tests 17 / 17, 13 / 18, 13 / 16, 14 / 14, 11 / 14 min; examples 70 / 63, 59 / 76, 53 / 62, 62 / 43, 45 / 58 s; vignettes 165 / 161, 154 / 184, 143 / 164, 122 / 126, 125 / 157 s.
- 2026-09-07: T2 — every `reference_*` site goes through `memoised()` (18 sites); `reference_nested_loop()` keeps its `tuned` result and `reference_with_rule()` applies a rule to a cached reference, so the four selection-rule blocks build one reference per configuration; serial run of the 11 touched files: 57 signatures, 57 builds, 89 requests, one `reference_*` signature per configuration; 1110 pass, 0 fail, 0 skip.
- 2026-09-07: T3 — the seed-77 run is memoised in the bayes, race and anneal rng pairs (`first` the build, `second` direct); bayes-results' two control blocks are one run under `control_bayes(allow_par = TRUE)`; finalize AC2 reads AC1's memoised grid run; `grid_final_and_reference()` serves final-fit-oracles' two strands.
- 2026-09-07: T4 built and green (545 pass, 0 fail; 71.8 s serial), then found AC3's field-for-field clause unsatisfiable on correct code: a daemon's first fold runs the package's own attach step (`attach_daemon_pkgs()`) and its imports load lazily, so loaded namespaces and `search()` grow through a section — measured on a fresh 2-daemon pool, one grid run attached workflows, ranger and parsnip and loaded 15 namespaces, a race then attached lme4, Matrix, finetune and tune. Substantive amendment of AC3 at a mini gate: the user chose exact comparison on the option, the three library variables and `.libPaths()` with namespaces and `search()` as supersets, over pre-loading a fixed package list (platform-dependent) and over escalation.
- 2026-09-07: re-audit: AC3 (full) — three findings: records paired by sorted position rather than per daemon (snapshot now keyed by pid); the identity-assertions clause named no enumerating procedure (a grep at both refs added); the pool count was read off the ledger's `times` multipliers (call sites in the file counted instead).
- 2026-09-07: re-audit: AC3 (full) — the three repairs hold; two phrasing findings: the probe runs inside a block before its parallel run, not "before" the block, and the pool-start grep was anchored so an assigned start would escape it; both taken into the final wording, which the user accepted at a second mini gate (further churn on AC3 goes to the user; no further reader).
- 2026-09-07: T4 — `test-parallel-identity.R` starts three pools per run (26 before): a shared 2-daemon pool for 18 blocks, BC9 polluting it in place as the last of them, a shared 3-daemon pool for the n=3 arms of BC1, BC10, BC12 and BC13 as their own blocks rebuilding their serial reference, and BC3's private pool; `share_daemons()`, `shared_daemons()`, `daemon_state_snapshot()`, `daemon_rng_kinds()`, `unshare_daemons()` and `serial_run()` in `helper-parallel.R`; the ledger's identity rows re-keyed (30 rows: 3 `start_daemons`, 2 `share_daemons`, 22 `shared_daemons` with BC12's counted twice, 1 `daemon_rng_kinds`, 2 `setTimeLimit`) and the three new helper names added to the hygiene guard's `BUDGETED_WAIT_CALLS`; identity grep 40 lines at the head against 34 at the branch point, the six from the four split loop arms.
- 2026-09-07: T5 — 23 example blocks across 17 roxygen sources go to `vfold_cv(v = 2)` outside and inside with `num_comp = 1:2` (the race page keeps inside v = 5 and its 1:4 grid, outside v = 2; `nested_resamples()`'s page builds no run and is untouched); `devtools::document()` regenerated 22 pages; `benchmarks/time-examples.R` added; one run on this machine under load: 9.6 s without `\donttest`, 16.2 s with, no errors, no warnings from any page, against 22.1 / 34.1 on the same tree before the edit; every page's exported-function set in `\examples` identical at `2f6c0e6` and the head (24 pages); `grep -l dontrun man/*.Rd` empty; NEWS bullet.
- 2026-09-07: T1 (local part) — `Rscript benchmarks/profile-tests.R 3` at `2f6c0e6` in a worktree while T2–T4 ran beside it: suite total 732.1 s (690.3 s at the plan-time measurement on a quiet machine), 9324 pass, 0 fail, 0 skip; heaviest files parallel-identity 91.9, bayes-oracles 81.9, grid-oracles 59.8, race-oracles 41.2, sim-anneal-oracles 30.3, final-fit-oracles 27.8, bayes-results 26.6, finalize 26.0, race-rng 24.8, final-fit-results 24.4, bayes-rng 24.1, anneal-rng 20.7; `benchmarks/time-examples.R 3` at the same ref: 18.6 s without `\donttest`, 28.4 s with, no errors.
- 2026-09-07: T6 first pass — `Rscript benchmarks/profile-tests.R 3` at `0444408`: suite total 631.3 s (732.1 s at the branch point in the same conditions, 690.3 s at plan time), 9680 pass, 0 fail, 0 skip, wall 634.0 s; heaviest files parallel-identity 65.7 (was 91.9), bayes-oracles 65.4 (81.9), grid-oracles 41.9 (59.8), race-oracles 32.8 (41.2), final-fit-oracles 25.3 (27.8), sim-anneal-oracles 24.2 (30.3), final-fit-results 23.7 (24.4), bayes-rng 22.0 (24.1), finalize 22.0 (26.0), race-rng 21.8 (24.8), bayes-results 19.5 (26.6), workflow-map-oracles 18.5 (18.2), anneal-rng 18.1 (20.7). Above AC1's 480 s: a traced serial run (every orchestrator call keyed on its arguments and RNG state) found 1213 direct calls (286 s) and 175 cached builds (206 s); of 133 s in duplicate direct calls 79 s would be recoverable in principle, but nearly all are the serial and parallel sides of one identity or the two direct calls an RNG-restore test needs; the explicit `best` rule's run is not identical to the cached default run (checked), and the grid-results reruns are deliberately un-memoised. The one cut left in the plan's levers: the workflow-map oracle's fixed-workflow hand call, the same run for all five `fn`, now memoised (file 18.5 → 15.8 s, 108 pass). `benchmarks/time-examples.R 3` at the head: 9.2 s without `\donttest`, 15.6 s with, no errors (18.6 / 28.4 at the branch point).
- 2026-09-07: T7 (edit part) — `R-CMD-check.yaml`'s `check-r-package` step cap is a flat 30 on all five legs; the cap comment's M72 paragraph now ends with M74's cuts and the local measurement (suite 732 → 631 s, examples 28 → 16 s with `\donttest`, the two default-branch runs' 18–28 minute steps); `cairn/PROFILE.md`'s test-doctrine slot says 30 on every leg (119 lines). The measured head is pushed next; three attempts' step times follow.
- 2026-09-07: substantive amendment of AC1 at a mini gate: the 480 s absolute bar, which T6's trace showed the plan's levers cannot reach (the remaining time is the package runs the assertions need), becomes a ratio bar of at most 88% of the branch point's total measured back to back in one sitting, the pass floor and `fail 0 | skip 0` kept; the user chose it over an absolute 640 s bar and over widening scope into M75's pruning. T6 re-worded to the same bar (minor edit).
- 2026-09-07: re-audit: AC1 (full) — two findings: the recorded pair (732.1 / 631.3 s) was not measured back to back under one load, so it did not instantiate the procedure and its 1.8-point margin sits inside the documented variance; the parenthetical measurement inside the criterion bound a recording property (moved to the work log).
- 2026-09-07: re-audit: AC1 (full) — two findings on the reader's replacement text: "under the same machine load" is a condition no instrument records (dropped); T6's task text still named 480 s (re-worded). The user accepted the final wording at a second mini gate; no further reader.
- 2026-09-07: T7 (measurement part) — the check workflows run on `pull_request` events only, so the push of `294ca2b` started nothing; draft PR #84 (https://github.com/tidymodels/nestedtune/pull/84) opened at the user's choice, its first attempt on `294ca2b` (run 34161078202, coverage run 34161078201); tracking-only commits after it are held local until the merge (M50 lesson), so the merge-time head differs from `294ca2b` only under `cairn/`.
- 2026-09-07: T8 — `devtools::check()` at `294ca2b`: 0 errors, 0 warnings, 0 notes (a first run reported one NOTE for a stray `Rplots.pdf` the example runs had left in the working tree, gitignored and deleted); `air format --check` clean on the 35 R files `git diff --name-only 2f6c0e6` lists.
- 2026-09-07: T7 attempt 1 of `294ca2b` (run 34161078202): `check-r-package` minutes devel 17.8, oldrel 22.2, ubuntu release 23.2, macOS 23.3, windows 26.6 (all green; windows above AC4's 24); `test-coverage` (run 34161078201) 15.2 minutes and red on covr's own `merge_coverage()` — `Error in readRDS(f): error reading from connection` after the tests ran — the same failure `main`'s last coverage run (34151259839 at `7f5752a`, M73's merge, 17.2 minutes) ends in, so it predates this branch; attempts 2 started for both.
- 2026-09-07: T7 attempt 2 of `294ca2b`: `check-r-package` minutes oldrel 17.5, macOS 18.0, devel 23.2, ubuntu release 23.4, windows 28.0 (all green); coverage 15.0 minutes, red on the same `merge_coverage()` `readRDS` error. Per leg, tests / examples / vignettes: macOS 11 min / 24 s / 119 s, devel 15 / 38 / 167, oldrel 11 / 28 / 143, ubuntu release 15 / 35 / 160, windows 17 / 44 / 203; attempt 1 had windows at 17 / 41 / 197 and the other legs' tests at 15, 11, 14, 15 min. The windows runner's vignette rebuild, untouched by this branch, took 197 and 203 s against 125 and 157 on the default branch's last two runs, so that leg's 26.6 and 28.0 include a slower runner; either way the median of three attempts of the slowest leg cannot be under 26.6, so AC4 fails as written, and AC5's 30-minute cap would leave windows 2–3 minutes of headroom, the shape M72 saw killed. Attempt 3 started.
- 2026-09-07: T7 attempt 3 of `294ca2b`: `check-r-package` minutes oldrel 15.7, macOS 19.7, ubuntu release 23.1, devel 26.4, windows 27.2 (all green); coverage 15.2 minutes and green, so the `merge_coverage()` error is intermittent. Medians of the three attempts: oldrel 17.5, macOS 19.7, devel 23.2, ubuntu release 23.2, windows 27.2; coverage 15.2. The slowest leg's median is 27.2 against AC4's 24 and the coverage step's 15.2 against its 14, so AC4 is not met on this head; AC5 (30 on every leg) is on the head but would leave windows under 3 minutes of headroom.
- 2026-09-07: T6 back-to-back pair (not ticked): the pair script ran the branch point twice by mistake (711.1 s, then 704.0 s, 9324 pass each — a 1% spread on identical code), then the head at `eef7ae5` (differs from `294ca2b` only under `cairn/`): 623.3 s, 9680 pass, 0 fail, 0 skip — 87.7% of the first branch-point pass and 88.5% of the second, astride AC1's 88% bar; heaviest files bayes-oracles 64.5 (78.9 at the branch point), grid-oracles 39.8 (52.5), race-oracles 31.5 (41.7). Attempt 3's per-leg tests / examples / vignettes: macOS 12 min / 29 s / 134 s, devel 18 / 39 / 178, oldrel 10 / 25 / 121, ubuntu release 15 / 34 / 161, windows 17 / 39 / 240.
- 2026-09-07: returned to `planned` at the user's choice for a re-cut by `/milestone-plan`: the goal's second half (every leg's check step within 24 minutes, the cap back to 30 everywhere) is not reachable from a 12–14% suite cut on runners whose windows leg ran 26.6–28.0 minutes across three attempts, and a goal is never edited in place. What stands on the branch (`m074-suite-speed`, PR #84 draft): T1–T5 and T8 done, T6 and T7 measured but their bars unmet, AC1 and AC3 amended and audited. The re-cut is expected to keep the suite and example cuts, revert the cap to 40 on the devel and windows legs (the yaml comment and PROFILE with it), and move the CI-cap promise to a milestone with a lever that reaches it; the milestone file's history lives on this branch, so the re-cut runs with it checked out.
- 2026-09-07: re-cut by /milestone-plan on this branch (the file's history lives here): the goal's CI-cap half leaves (a candidate row), the caps go back to M72's 40/30, and the suite and example cuts stand. Criteria audit ran in full mode on a fresh [O] reader: seven findings — AC2's grep missed `reference_select()`, `reference_with_rule()` and `reference_last_fit()` (sweep widened, exemptions named); AC1's counts came from run 1 of 3 (every run now reports, T6 changes the script); AC4 named `gh run view --json jobs`, which has no step timestamps (the jobs endpoint's `started_at`/`completed_at` instead); AC5's `air format --check` ran over yaml and Rd (filtered to `.R`/`.Rmd`); AC6 carried two unrelated bars (now a 60% ratio, back to back); the goal said caps "sized to the measured head" while AC4 fixes M72's figures (goal reworded); AC3's four-pool bound against three sites (tightened at the gate). AC4's "one workflow attempt" kept as a recorded observation over an 80%-of-cap bound the ubuntu release leg would nearly fail.
- 2026-09-07: re-cut gate chose a candidate row for the 24-minute step and 30-everywhere cap over planning it as M76 after M75, because no lever is measured to reach it (windows 26.6–28.0 minutes on `294ca2b`, its vignette rebuild alone 197–240 s); falsified by M75's measured head or one priced lever (skipping the check-time vignette rebuild, splitting the identity file) bringing the windows step under 24, which promotes the row.
- 2026-09-07: re-cut gate chose a 90% suite bar over the 88% one, because the head measured 87.7% and 88.5% in single runs with a 1% spread on identical code, so 88% sat on the point estimate; falsified by a back-to-back median-of-three pair above 90%, which returns the branch to T2–T4.
- 2026-09-07: re-cut gate tightened AC3's pool bound from four to three, the count the head's three start sites give, at the user's choice.
- 2026-09-07: resumed by /milestone-implement after the re-cut; T7 (edit part, second pass) — the `check-r-package` cap is M72's expression again (40 on devel and windows, 30 elsewhere); the yaml comment's M72 paragraph now ends with M74's cuts, the 294ca2b measurements (suite 704–711 → 623 s, examples 28 → 16 s) and why the two legs keep 40; `cairn/PROFILE.md`'s test-doctrine slot says the same (119 lines); `benchmarks/profile-tests.R` prints `pass | fail | skip` per run (T6's script part). This commit is the measured head; tracking-only commits after it stay local until the merge.
- 2026-09-07: T7 (measurement part) — PR #84 retitled to this file's title; the push of `b723c11` started run 34172488487 (attempt 1, `pull_request`), every leg's `check-r-package` step `success`, minutes from the jobs endpoint's `started_at`/`completed_at`: macOS 20.3, oldrel 21.9, ubuntu release 22.4, devel 24.8, windows 26.6 — devel and windows under their 40, the other three under 30.
- 2026-09-07: T6 back-to-back pair, one sitting 19:11–20:34 on the maintainer's machine, nothing else started on it: `Rscript benchmarks/profile-tests.R 3` at `2f6c0e6` (worktree) suite total 948.5 s, 9324 pass, 0 fail, 0 skip; then at `0b6be83` (differs from the measured head `b723c11` only under `cairn/`) 642.9 s — 67.8% of the branch point — runs 590.3 / 660.7 / 642.9 s, each 9680 pass, 0 fail, 0 skip. The branch-point figure sits 30% above its three earlier measurements (690, 704–732 s) with every file slower by about the same share (parallel-identity 119.2 against 91.9, bayes-oracles 108.9 against 81.9), and the head's three runs spread 12%, so the machine was not evenly loaded across the pair; the ratio meets AC1's 90% bar as executed, and the earlier even-load pair (623 against 704–711 s, 87.7–88.5%) is the closer estimate of the true cut. Head per-file medians: bayes-oracles 63.2, parallel-identity 62.4, grid-oracles 42.5, race-oracles 32.0, final-fit-oracles 25.2, sim-anneal-oracles 23.6, final-fit-results 23.4, bayes-rng 21.4, race-rng 21.3, finalize 20.8, bayes-results 19.3, workflow-map-oracles 17.8, anneal-rng 17.6, grid-failures 17.2, eval-time 13.8. `Rscript benchmarks/time-examples.R 3` (the head's script at both refs) at `2f6c0e6`: 18.2 s without `\donttest`, 27.9 with; at `0b6be83`: 8.5 and 14.0 — 46.7% and 50.2%, no errors at either ref. The coverage run on `b723c11` (34172488510) failed again inside covr's `merge_coverage()` on `readRDS`, the intermittent failure T7's attempts 1 and 2 recorded.
- 2026-09-07: T8 (head) — `devtools::check()` at `ccdbe62`: 0 errors, 0 warnings, 0 notes (7m20s); `air format --check` clean on the 36 `.R`/`.Rmd` files `git diff --name-only 2f6c0e6` lists. All tasks checked; status → review. Tracking-only commits since `b723c11` stay local until the merge.

- 2026-09-07: review checkpoint — PR URL recorded; AC3 and AC4 verified and ticked; AC1's back-to-back pair, the AC2 cache report, the AC6 example pair and the AC5 check pending (the pair runs first so nothing else loads the machine); three fresh-context reviewers spawned.

- 2026-09-07: step-7 approval: PR #84 approved for merge (merge as measured, findings O2, O4, O6, O7-bayes, O8-line-98, O10, O11 and R1 to one follow-up candidate row). Review-side tracking commits after `b723c11` are held local and their content carried into the hygiene commit (M50 lesson), so the squash carries `b723c11`.

## Decisions

- 2026-09-07 (T4): a block's serial reference on a shared pool is built through `serial_run()`, which mocks `mirai_workers()` to 0 for that call, rather than by stopping the pool. mirai holds one pool at a time, so the file could not both share a pool and stop it for each serial reference; the mock changes only the count `use_parallel()` reads (D-018's threshold), nothing on the serial path consults the pool, and every caller still asserts `last_dispatch()` is `"serial"`. The same device `test-parallel-classify.R` uses to fabricate a pool, in the other direction.

## Review

Reviewed 2026-09-07 on `98c2fc9` (differs from the measured head `b723c11` only under `cairn/`); default branch `main` at `737a77d`, an ancestor of the head, so no merge was needed. Tracking-only commits stay local until the merge (M50 lesson), so PR #84 holds `b723c11`.

- AC3 — verified. `grep -n -E '(start_daemons|start_daemons_undispatched|start_mixed_daemons)\(' tests/testthat/test-parallel-identity.R` lists three sites, `:35` (2-daemon start block), `:891` (3-daemon start block) and `:1072` (BC3's private pool), each at block top level, none in a loop; the file's only other `mirai::daemons(` calls are `:1061` and `:1063`, both `daemons(0)`. The `helper-time-budget.R` ledger carries one `start_daemons` row for each of those three lines and no `start_daemons_undispatched` or `start_mixed_daemons` row for the file. `grep -c -E '^\s*shared_daemons\('` gives 22: the 18 blocks from BC1 (`:44`) to BC9 (`:849`) each hold one `shared_daemons(2)` and the 4 blocks from `:895` to `:981` each hold one `shared_daemons(3)`, and a script pairing each block's `shared_daemons(` line with its first non-`serial_run()` package call shows the probe first in all 22. The probe (`helper-parallel.R` `shared_daemons()`) compares `daemon_state_snapshot()` records named by pid against the pool-start snapshot: `names()` identical, `options('nestedtune.preflight_timeout')`, `Sys.getenv(c('R_LIBS','R_LIBS_USER','R_LIBS_SITE'))` and `.libPaths()` by `expect_identical()` per daemon, `sort(loadedNamespaces())` and `search()` by `setdiff(start, now)` empty. Identity assertions: the grep lists 34 lines at `2f6c0e6` and 40 at the head; a multiset comparison of the assertion texts finds the head equal to the branch point with each of the 6 pairs that sat inside a `for (n in c(2L, 3L))` loop (BC1, BC10, BC12 ×2, BC13 ×2) present twice, once per daemon count, and every other pair once.
- AC4 — verified. `R-CMD-check.yaml:154` reads `timeout-minutes: ${{ (matrix.config.r == 'devel' || matrix.config.os == 'windows-latest') && 40 || 30 }}`; its comment (`:97–98` and the M72 paragraph) and `cairn/PROFILE.md:47–49` state 30 with 40 on devel and windows. `git diff --name-only b723c11 HEAD` lists only `cairn/` files. `gh api repos/tidymodels/nestedtune/actions/runs/34172488487` is attempt 1 on `b723c11` (`pull_request`, conclusion success); its jobs endpoint gives every leg's `Run r-lib/actions/check-r-package@v2` step `success`, minutes from `started_at`/`completed_at`: macOS 20.3, oldrel 21.9, ubuntu release 22.4, devel 24.8, windows 26.6.
- AC1 — verified. One sitting 21:04–22:08 on the maintainer's machine, nothing else started on it: `Rscript benchmarks/profile-tests.R 3` (the head's script copied to both trees) on a worktree at `2f6c0e6`: runs 649.0 / 664.6 / 692.6 s, median 664.6 s, each 9324 pass, 0 fail, 0 skip; then on the head `b08cdcd` (differs from `b723c11` only under `cairn/`): runs 568.7 / 593.5 / 613.3 s, median 593.5 s — 89.3% of the branch point — each 9680 pass, 0 fail, 0 skip. Head per-file medians: bayes-oracles 61.3, parallel-identity 61.2, grid-oracles 38.9, race-oracles 30.6, final-fit-oracles 23.7, sim-anneal-oracles 22.8, final-fit-results 22.2, bayes-rng 20.6, race-rng 20.4, finalize 19.8, bayes-results 18.2, anneal-rng 16.9. Two earlier pairs in the work log measured 67.8% (uneven load) and 87.7–88.5%; this pair's branch-point runs spread 6.7% and the head's 7.8%.
- AC2 — verified. The sweep `grep -n -E 'reference_[a-z_]+\(' tests/testthat/*.R`, less the two definition files, the `reference_select()` / `reference_with_rule()` sites and the five `reference_last_fit()` sites in `test-nested-tune-grid-results.R`, lists 18 call sites (the other matches are comment lines naming a helper), every one `memoised(reference_…(`. In the four selection-rule blocks (`grid-oracles:313`, `bayes-oracles:418`, `race-oracles:339`, `sim-anneal-oracles:339`) one `memoised(reference_*_loop(…))` is built per configuration (the race block one per racer inside `for (fn in RACERS)`) and the three rules are applied to it through `reference_with_rule()`. The serial fixture-cache report of the head's first run (176 signatures, 177 builds, 528 requests) lists 23 `reference_*` signatures, every one `builds 1`: the selection-rule configurations appear as `reference_nested_loop(wf, folds, g, ms, seed = 20, …)`, `reference_nested_bayes_loop(… iter = 2, initial = 3 … seed = 20 …)`, two `reference_nested_race_loop(fn, … det_grid() … seed = 20 …)` rows (one per racer, requests 2 each: the deterministic oracle's entry served the block) and `reference_nested_anneal_loop(… seed = 27 …)`. The one signature built twice (`nested_fit_resamples(workflow, folds, metrics = ms)`, 2 builds, 6 requests) is the workflow-map hand call, not a `reference_*` helper — recorded as review finding R1 below.
- AC6 — verified. `Rscript benchmarks/time-examples.R 3` (the head's script at both refs) back to back 22:08–22:11: at `2f6c0e6` totals 17.7 s without `\donttest` and 27.0 s with; at the head 7.8 s and 13.0 s — 44.1% and 48.1% — errors none at either ref. A script parsing every `man/*.Rd` at both refs with `tools::parse_Rd()` finds 24 pages with an `\examples` section at `2f6c0e6`, each still holding one at the head calling the identical set of `NAMESPACE` exports (24 of 24 identical). `grep -l dontrun man/*.Rd` prints nothing. `devtools::document()` leaves `git status` clean.
- AC5 — verified. `devtools::check()` on `9050bfa` (code identical to `b723c11`): 0 errors, 0 warnings, 0 notes, 7m 4.5s; `air format --check` over the 36 `.R`/`.Rmd` files `git diff --name-only 2f6c0e6 | grep -E '\.(R|Rmd)$'` lists exits 0 with no file reported.

Consistency gate (2026-09-07): `cairn_validate.py` exit 0, every check PASS (18 `references staleness` advisories, pre-existing); no `DESIGN.md` change on the branch, so `cairn_impact.py` was not run; `devtools::document()` no diff; `pkgdown::check_pkgdown()` no problems; `README.Rmd` untouched by the branch; `NEWS.md` carries the example-shape bullet with no milestone number; `benchmarks/` is `.Rbuildignore`d; `air format --check` clean on the 36 touched `.R`/`.Rmd` files; `devtools::check()` recorded under AC5.

Independent review (three fresh-context lenses on `git diff origin/main...HEAD`). [S] blame-history: 0 findings, seven touched areas (M12's probe condition, M07's kind check, M45/M50/M51's loop arms, BC9's move, M72's caps, `serial_run()` against D-018, the merged rng pairs against the M42 lesson) each accounted for by this file's record. [S] prior-review record: 0 findings over the archived Review sections of M12, M14, M16, M31, M33, M52, M56 and M57; the probe found one human inline PR comment repo-wide, on PR #30's pkgdown workflow, unrelated to the touched files. [O] diff-bug: 13 findings, ranked by the reviewer, each verified against the implementation before the gate; plus one finding (R1) from the AC2 report. Dispositions recorded after the step-7 gate.

- O1 `test-nested-tune-finalize.R:107,232` — the memoised `nested_tune_grid()` in `run_finalize_tuner()` has a side effect AC1 asserts on (the finalizer writes `record$frames`), so AC1 must run before AC2 in the file; a warm cache fails AC1's `expect_gt(length(record$frames), 0L)` loud rather than passing. Verified: the ordering is stated in the comment at `:99–102` and the assertion is at `:148`.
- O2 `test-nested-tune-bayes-results.R:249–289` — the merge of the two control blocks dropped the branch point's `control_bayes(allow_par = TRUE, seed = 999L)` run and its `expect_identical(forced, plain)`, so an explicitly supplied `seed` slot being overwritten is no longer asserted anywhere (`grep -rn 999 tests/testthat` finds no other site). Verified against the diff.
- O3 `test-parallel-identity.R:150–207` — the cancelled-run block stops a map on the shared 2-daemon pool; a replaced daemon would fail BC6's pid check. Verified against mirai 2.7.2's changelog: the daemon-exit-on-cancel defects (#459, #546) are recorded fixed, three local runs and five CI legs were green, and a replacement would fail loud, not pass.
- O4 `helper-parallel.R:216–224` — `daemon_state_snapshot()` orders records by `order(pids)` and names them by `sort(pids)`; an answer with no integer `pid` would leave the names one short and mislabel the records. Verified by reading; only the non-list-answer path is affected.
- O5 `helper-parallel.R:186–198, 251–271` — the probe does not read a daemon's global environment (what BC9 dirties), working directory or connections, and compares `search()` as an unordered superset. AC3 names the fields the probe compares (chosen at its amendment gate); BC9 is the last block on its pool.
- O6 `benchmarks/time-examples.R:1,26` — the header names "AC7", which the re-cut renumbered to AC6. Verified.
- O7 the "served from the cache" comments at `grid-oracles:328`, `bayes-oracles:432`, `sim-anneal-oracles:353`, `race-oracles:353` — the race claim holds (requests 2 per racer in the report); the bayes comment's claim that the block reads "the one the default oracle above built" does not: an isolated run of the file shows two seed-20 `iter = 2, initial = 3` entries with one request each (the workflow's PCA step id is drawn from the stream at a different ambient state, so the key differs); the grid and anneal comments say only that the entry is memoised. One reference per configuration holds in all four.
- O8 `.github/workflows/R-CMD-check.yaml:68–84,98` — the new paragraph cites the `294ca2b` pair (704–711 → 623 s) where this review's pair on the head is 664.6 → 593.5 s, and the pre-existing line 98 says the step cap "still ends a hung test suite in 30" where devel and windows have 40.
- O9 `cairn/PROFILE.md:48` — a 175-character line in a file wrapped near 118. Cosmetic.
- O10 `benchmarks/profile-tests.R:64–76,128–139` — each run's `pass | fail | skip` line prints twice, once in the loop and once in the summary. Verified.
- O11 `helper-time-budget.R:58–60` — the `times` comment names BC12's 2-daemon site alone where the 3-daemon site `:957` also carries `times = 2L`. Comment only.
- O12 `test-nested-final-fit-oracles.R:65–92` — hoisting `nested_final_fit()` into `grid_final_and_reference()` moves the frame the design's `inside` call is re-evaluated in; safe for the literal spec used. Informational.
- O13 `helper-parallel.R:294–307` — `serial_run()`'s mock is read only by `use_parallel()` (`R/parallel.R:207`); serial references now run beside a live pool, guarded by every caller's `last_dispatch() == "serial"` assertion. Informational.
- R1 (from the AC2 report) `test-nested-workflow-map-oracles.R:29` — the memoised fixed-workflow hand call is built twice across the five `fn` blocks (2 builds, 6 requests; the branch point ran it six times unmemoised), the report's one `WARNING`; the key differs between blocks because the workflow is rebuilt under a different stream state. A speed remainder, not a dropped claim.

Triage at the step-7 gate (2026-09-07), user's choice: merge as measured, fixes deferred. O2 → follow-up (candidate row, with O4, O6, O7's bayes comment, O8's line 98, O10, O11 and R1's remainder). O4 → follow-up. O6, O7 (bayes comment), O8 (line 98), O10, O11 → follow-up, text only. O7 (grid, anneal, race comments) → reject: the race claim holds and the other two claim no cross-block hit. O8 (figures) → reject: the yaml cites the even-load pair the work log calls the closer estimate. O1 → reject: a warm cache fails AC1 loud, the ordering is stated at `:99–102`. O3 → reject: mirai 2.7.2 records the daemon-exit-on-cancel defects fixed, and a replaced daemon fails BC6's pid check. O5 → reject: AC3 names the probe's fields, chosen at its amendment gate; BC9 is the last block on its pool. O9 → reject: cosmetic. O12, O13 → noted, informational. R1 → follow-up with O2's row. conversation: PR #84 — empty (no reviews, comments or threads).
