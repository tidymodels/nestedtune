% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-results.R
\name{predict.nested_results}
\alias{predict.nested_results}
\alias{predict.nested_results_set}
\title{Predicting from a nested run}
\usage{
\method{predict}{nested_results}(object, ...)

\method{predict}{nested_results_set}(object, ...)
}
\arguments{
\item{object}{A \code{nested_results} or a \code{nested_results_set}.}

\item{...}{Not used.}
}
\value{
None. The call is an error.
}
\description{
A nested run holds one model per outer fold, each fitted to estimate how
the procedure performs, and none of them is the model to deploy.
\code{predict()} on its results therefore refuses, with class
\code{nestedtune_predict_results}, and points you to \code{\link[=nested_final_fit]{nested_final_fit()}},
which fits that model on all the data. For a workflow set, the message
names \code{nested_final_fit()}'s \code{id} argument, which picks the workflow.
}
\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

rec <- recipes::recipe(mpg ~ ., data = mtcars) |>
  recipes::step_pca(recipes::all_predictors(), num_comp = tune::tune())
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 2),
  inside = rsample::vfold_cv(v = 2)
)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
set.seed(2)
res <- nested_tune_grid(wf, folds, grid = data.frame(num_comp = 1:2))
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
set.seed(3)
final <- nested_final_fit(wf, res)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
# Predict from the final fit, not from `res`.
predict(final, new_data = mtcars[1:3, ])
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nested_final_fit]{nested_final_fit()}}, \code{\link[=predict.nested_final_fit]{predict.nested_final_fit()}}
}
