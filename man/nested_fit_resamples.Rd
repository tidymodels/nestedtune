% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-fit-resamples.R
\name{nested_fit_resamples}
\alias{nested_fit_resamples}
\title{Score a workflow with nothing to tune on a nested design}
\usage{
nested_fit_resamples(
  object,
  resamples,
  ...,
  metrics = NULL,
  event_level = "first",
  eval_time = NULL
)
}
\arguments{
\item{object}{A \code{\link[workflows:workflow]{workflows::workflow()}} with no parameter marked for tuning
with \code{\link[tune:tune]{tune::tune()}}, every value fixed as \code{\link[tune:fit_resamples]{tune::fit_resamples()}} takes
it; a workflow carrying a marker is refused at entry.}

\item{resamples}{A nested resampling design from \code{\link[=nested_resamples]{nested_resamples()}} or
\code{\link[rsample:nested_cv]{rsample::nested_cv()}}, one row per outer fold. The section on nested
designs says what the design must hold.}

\item{...}{A control object from \code{\link[tune:control_resamples]{tune::control_resamples()}}, as
\code{control}, and nothing else; every argument after \code{...} is matched by
name. The section on differences from tune says what becomes of each
slot.}

\item{metrics}{A \code{\link[yardstick:metric_set]{yardstick::metric_set()}}, or \code{NULL} to use tune's
defaults for the model's mode; there is no inner run to select on, so
the set's order carries no weight here.}

\item{event_level}{\code{"first"} (the default) or \code{"second"}, naming which
level of a two-class outcome is the event in the one tune call a fold
makes, the outer scoring fit.}

\item{eval_time}{A numeric vector of evaluation times for a censored
regression model, or \code{NULL} (the default) to leave the choice to tune;
anything not numeric, an empty vector, or an element that is missing,
negative or not finite is refused at entry.}
}
\value{
A \code{nested_results} with one row per outer fold and the columns
\code{\link[=nested_tune_grid]{nested_tune_grid()}} documents, three of them holding what no tuning
leaves. \code{.selected} is a zero-row, zero-column tibble on every completed
fold and \code{NULL} on a failed one. \code{.inner_metrics} is a zero-row table
with tune's summary columns and no parameter column. \code{.tuning_seed}
holds the seed the loop drew for the fold's tuning step, consumed by
nothing. There is no \code{grid} attribute, and the \code{procedure} record names
the tuner \code{"fit_resamples"} with no \code{grid}, \code{param_info} or \code{select}
entry.
}
\description{
\code{nested_fit_resamples()} runs the outer loop of a nested design for a
workflow whose parameters are all fixed: for each outer fold it fits the
workflow on the fold's analysis set and scores it on the assessment set
with \code{\link[tune:last_fit]{tune::last_fit()}}, skipping the inner stage, since there is nothing
to search. It is \code{\link[=nested_tune_grid]{nested_tune_grid()}} with the inner tuner removed, so a
fixed workflow and a tuned one score on identical outer folds and their
per-fold metrics join by fold label. That page is the reference for
everything the orchestrators share.

Use it for the baseline a tuned procedure is compared against. A plain
\code{rset} of outer folds is what \code{\link[tune:fit_resamples]{tune::fit_resamples()}} already serves; what
this function adds is the same nested design, so the two runs' folds are
the same rows.
}
\section{One door for a fixed workflow, one for a tuned one}{


A workflow that still carries a \code{\link[tune:tune]{tune::tune()}} marker is refused at entry
with condition class \code{nestedtune_tuned_workflow}, naming the five
orchestrators that tune; each of those refuses a workflow with no marker,
with class \code{nestedtune_untuned_workflow}, naming this one.
}

\section{What the readers answer}{


Every reader of a \code{nested_results} answers on the result.
\code{\link[=collect_metrics]{collect_metrics()}}, \code{summary()}, \code{print()}, \code{\link[=collect_notes]{collect_notes()}},
\link[=collect_predictions.nested_results]{collect_predictions()} and
\link[=collect_predictions.nested_results]{collect_extracts()} read what the
outer fits produced; \code{\link[=collect_selections]{collect_selections()}}, \code{\link[=collect_inner_metrics]{collect_inner_metrics()}} and
\code{\link[=agreement]{agreement()}} return zero rows; \code{autoplot(type = "performance")} draws the
fold scores, where \code{autoplot(type = "parameters")} refuses with class
\code{nestedtune_no_tuned_parameters}, there being nothing to draw.
\code{\link[=nested_final_fit]{nested_final_fit()}} accepts the result and fits the workflow on every
row, with no tuning run.
}

\section{Reproducibility}{


Seed the session before the call, as elsewhere in tidymodels; there is no
\code{seed} argument. On entry the function draws \code{2 * n} seeds in a single
\code{sample.int(.Machine$integer.max, 2 * n)} call, where \code{n} is the number of
outer folds. Fold \code{i} uses element \code{2 * i - 1} for its tuning step and
element \code{2 * i} for its outer fit, each applied with the generator kind
pinned. A fold's seed depends on its position, not on the order the folds
run in. So the same seed gives the same result serially and in parallel,
at any number of daemons. The two seeds are kept on the result as
\code{.tuning_seed} and \code{.outer_fit_seed}, and \code{?nested_tune_grid} shows how to
reproduce one fold by hand from them.

The caller's RNG state and generator kind are restored on exit, including
when the call errors, so a seeded script that draws afterwards is
unaffected. One consequence: two consecutive calls with no \code{set.seed()}
between them return identical results, exactly as repeated
\code{\link[tune:tune_grid]{tune::tune_grid()}} calls do.

This binds randomness that flows through R's generator. Engines that
randomize outside it (kernlab's SVMs, the deep-learning engines) cannot be
pinned by any R-side scheme, here or in tune.
}

\section{The two seeds on a run with no tuning}{


The same \code{2 * n} seeds are drawn as on a tuned run, so a tuned run under
the same session seed on the same design shares each fold's outer-fit
seed with this one, and the record keeps one layout across the six
orchestrators. The tuning seed is drawn and consumed by nothing, and is
recorded as drawn rather than as \code{NA} so the two seed columns read the
same on every result. Fold \code{i} is exactly \code{\link[tune:last_fit]{tune::last_fit()}} of \code{object}
on \code{resamples$splits[[i]]} under \code{res$.outer_fit_seed[[i]]}, applied as
\code{?nested_tune_grid} shows, with no tuning line before it.
}

\section{Differences from calling tune directly}{


There is no \code{control} formal, but a \code{\link[tune:control_resamples]{tune::control_resamples()}} passed
through \code{...} as \code{control} is recorded, and two of its slots reach the
outer fit. What is recorded is the control passed, or tune's default when
none is, with the slots this package forces overwritten, as
\code{extract_procedure(res)$control}. tune gives \code{control_resamples()},
\code{\link[tune:control_grid]{tune::control_grid()}} and \code{\link[tune:control_last_fit]{tune::control_last_fit()}} one class, so any of
the three is accepted here as the same object. Every slot of
\code{control_resamples()} falls under one of seven headings, most under the
last, because the one tune call a fold makes here is the outer scoring
fit, whose own control this package builds.

\strong{Forced: \code{allow_par}.} The outer fit runs at \code{allow_par = FALSE}
whatever the control carries, since parallelism belongs over the outer
folds.

\strong{Settable as its own argument: \code{event_level}.} The argument is the one
place the level is set: a control at tune's default takes it, and a
control naming another level is refused at entry, naming both.
\code{eval_time} is offered the same way, for the same reason.

\strong{Refused: none.} No slot is refused on its own; a control of another
class (a \code{control_bayes()}) is refused at entry, as is the \code{event_level}
conflict above.

\strong{Passed through: none.} There is no inner tuning call for a slot to be
passed through to.

\strong{Kept from the outer fit: \code{save_pred}, \code{extract}.} With \code{save_pred = TRUE} the result carries a \code{.predictions} list column, each completed
fold's predictions on its assessment rows as \code{\link[tune:last_fit]{tune::last_fit()}} returns
them; with \code{extract} a function, an \code{.extracts} list column holding the
function's value on each completed fold's fitted workflow. Here there is
no inner run whose predictions and extracts are discarded; the outer
fit's are the only ones, and they are still discarded on a run that did
not ask.

\strong{Not returned: none.} Nothing an inner run would have saved exists to be
withheld.

\strong{Inert: \code{verbose}, \code{pkgs}, \code{save_workflow}, \code{parallel_over},
\code{backend_options}, \code{workflow_size}.} Each governs an inner tuning call
this function never makes. The outer fit runs under
\code{\link[tune:control_last_fit]{tune::control_last_fit()}} at the level and parallelism above, which reads
none of these; the workflow's packages are required at entry, and the
fitted workflow is reached through \code{extract}.
}

\section{Nested designs}{


\code{resamples} is a data frame with one row per outer fold. Its \code{splits}
column holds that fold's \code{rsplit}, its \code{inner_resamples} column holds an
\code{rset} with at least one row, and every other column labels the fold. A
label column is named \code{id}, or \code{id} followed by a digit from 1 to 9, and
holds character or factor values. Together the label columns give every
outer fold a distinct label with no \code{NA}.

Inside each inner \code{rset}, every element of \code{splits} is an \code{rsplit}. All
of a fold's inner splits carry one data frame: either the outer split's
own frame, as \code{\link[=nested_resamples]{nested_resamples()}} builds, or that split's analysis set,
as \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds. An inner split carrying the outer frame
may index only rows the outer split's \code{in_id} holds, in its \code{in_id} and
any non-\code{NA} \code{out_id}. So no inner analysis or assessment set reaches a
row the outer fold holds out.

A design breaking any of this, or using a bootstrap for the outer loop, is
refused before anything is fitted. The error has condition class
\code{nestedtune_bad_design} and names every offending row, column, inner
split or index. The checks exist because \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds a
design whatever its \code{inside} argument returned, and because a design
assembled by hand can index rows its outer fold never sees.
}

\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

rec <- recipes::recipe(mpg ~ ., data = mtcars) |>
  recipes::step_pca(recipes::all_predictors(), num_comp = tune::tune())
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 2),
  inside = rsample::vfold_cv(v = 2)
)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
# A fixed workflow: no tune() marker anywhere.
fixed <- workflows::workflow(mpg ~ ., parsnip::linear_reg())

set.seed(2)
res <- nested_fit_resamples(fixed, folds)
collect_metrics(res)

# The record says no tuning ran.
extract_procedure(res)$tuner
res$.selected[[1]]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nested_tune_grid]{nested_tune_grid()}}, \code{\link[=nested_resamples]{nested_resamples()}}, \code{\link[=nested_final_fit]{nested_final_fit()}},
\code{\link[tune:fit_resamples]{tune::fit_resamples()}}
}
