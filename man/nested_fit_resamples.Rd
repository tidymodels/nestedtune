% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-fit-resamples.R
\name{nested_fit_resamples}
\alias{nested_fit_resamples}
\title{Score a workflow with nothing to tune on the outer folds of a nested design}
\usage{
nested_fit_resamples(
  object,
  resamples,
  ...,
  metrics = NULL,
  event_level = "first",
  eval_time = NULL
)
}
\arguments{
\item{object}{A \code{\link[workflows:workflow]{workflows::workflow()}} with no parameter marked for tuning
with \code{\link[tune:tune]{tune::tune()}}: every value fixed, as \code{\link[tune:fit_resamples]{tune::fit_resamples()}} takes
it. A workflow carrying a marker is refused at entry.}

\item{resamples}{A nested resampling design, from \code{\link[=nested_resamples]{nested_resamples()}} or
\code{\link[rsample:nested_cv]{rsample::nested_cv()}}: a data frame whose \code{splits} column holds one
\code{rsplit} per outer fold, whose \code{inner_resamples} column holds one \code{rset}
with at least one row per outer fold, and whose every other column
labels the outer folds. A label column must be named \code{id}, or \code{id}
followed by a digit from 1 to 9 (the names rsample and tune read id
columns by), and hold character or factor values; taken together, the
label columns must give every outer fold a distinct label with no \code{NA}.
Inside each inner \code{rset}, every element of its \code{splits} column is an
\code{rsplit}; all of a fold's inner splits carry one frame, either the outer
split's own data frame (what \code{\link[=nested_resamples]{nested_resamples()}} builds) or that split's
analysis set (what \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds); and an inner split
carrying the outer data frame indexes, in its \code{in_id} and any non-\code{NA}
\code{out_id}, only rows the outer split's \code{in_id} holds, so that no inner
analysis or assessment set reaches a row the outer fold holds out. A
design breaking any of this, or using a bootstrap for the outer loop, is
refused at the call, before anything is fitted, with condition class
\code{nestedtune_bad_design} and every offending row, column, inner split or
index named. The checks exist because \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds a
design whatever its \code{inside} argument returned (a specification that
produces no \code{rset}, or an empty one, gives a design that cannot be run,
where \code{\link[=nested_resamples]{nested_resamples()}} refuses one at construction), and because a
design assembled by hand can index rows its outer fold never sees.}

\item{...}{A control object as \code{control} -- what
\code{\link[tune:control_resamples]{tune::control_resamples()}} returns -- and nothing else. Two of its
slots reach the outer fit, \code{save_pred} and \code{extract}; the section on
differences from tune says what becomes of each slot. Any other name is
an error, as is an unnamed value.}

\item{metrics}{A \code{\link[yardstick:metric_set]{yardstick::metric_set()}}, or \code{NULL} to use tune's
defaults for the model's mode. There is no inner run to select on, so
the set's order carries no weight here.}

\item{event_level}{\code{"first"} (the default) or \code{"second"}, naming which
level of a two-class outcome factor is the event, as on
\code{\link[=nested_tune_grid]{nested_tune_grid()}}. It reaches the one tune call a fold makes, the
outer scoring fit, where it decides what the reported metrics mean.}

\item{eval_time}{A numeric vector of evaluation times for a censored
regression model, or \code{NULL} (the default) to leave the choice to tune. It
reaches every tune call whose answer depends on it, so a dynamic or
integrated survival metric -- \code{brier_survival()}, \code{roc_auc_survival()} and
their relatives -- is measured at the times you name. It is ignored, with
a warning from tune, whenever the metric set has no metric that reads it.
tune keys that warning on the metrics rather than on the model's mode: a
set with no survival metric draws one saying the argument is only used
for censored regression, and a censored regression model scored only by a
static metric such as \code{concordance_survival()} draws a different one,
saying it is only used for dynamic or integrated survival metrics.

Refused here, ahead of tune: anything that is not numeric, an empty
vector, and any element that is missing, negative or not finite. tune
treats those unevenly, and only once a metric reads the times -- a
character value that reads as a number, such as \code{"1"}, is coerced with
\code{as.numeric()} and accepted, one that does not becomes missing; a
missing, negative or infinite element is dropped with a warning; and an
empty vector, or one that dropping has emptied, aborts -- and this package
refuses them all at entry, before a whole run is paid for. Zero, repeated
times and times out of order are accepted and passed on untouched, since
tune normalizes those itself; a repeated time draws tune's warning that 0
inappropriate evaluation time points were removed, once per tune call.}
}
\value{
An object of class \code{nested_results}, one row per outer fold, with
the columns \code{\link[=nested_tune_grid]{nested_tune_grid()}} documents and three of them holding
what no tuning leaves. \code{.selected} is a zero-row, zero-column tibble on
every completed fold and \code{NULL} on a failed one: nothing was chosen, and
no row is written to say so. \code{.inner_metrics} is a zero-row table with
tune's summary columns (\code{.metric}, \code{.estimator}, \code{mean}, \code{n}, \code{std_err},
\code{.config}, and \code{.eval_time} for a dynamic survival metric) and no
parameter column: no inner run scored anything. \code{.tuning_seed} holds the
seed the loop drew for the fold's tuning step, consumed by nothing (the
reproducibility section says why it is kept). \code{.metrics}, \code{.notes},
\code{.completed} and \code{.outer_fit_seed} are as on every orchestrator, and
\code{.predictions} and \code{.extracts} are present exactly when the control asked
for them.

There is no \code{grid} attribute. The \code{procedure} record, which
\code{\link[=extract_procedure]{extract_procedure()}} returns, names the tuner (\code{"fit_resamples"}) and
holds \code{event_level}, \code{eval_time} and the effective control, with no
\code{grid}, \code{param_info} or \code{select} entry: no parameter set was read and no
rule applied. \code{attr(x, "metrics")} holds the \code{metrics} argument, absent
when none was given.

Every reader of a \code{nested_results} answers on the result.
\code{\link[=collect_metrics]{collect_metrics()}}, \code{summary()}, \code{print()}, \code{\link[=collect_notes]{collect_notes()}},
\link[=collect_predictions.nested_results]{collect_predictions()} and
\link[=collect_predictions.nested_results]{collect_extracts()} read what the
outer fits produced; \code{\link[=collect_selections]{collect_selections()}}, \code{\link[=collect_inner_metrics]{collect_inner_metrics()}}
and \code{\link[=agreement]{agreement()}} return zero rows; and \code{autoplot(type = "performance")}
draws the fold scores, where \code{autoplot(type = "parameters")} refuses with
class \code{nestedtune_no_tuned_parameters}, there being nothing to draw.
\code{\link[=nested_final_fit]{nested_final_fit()}} accepts the result and fits the workflow on every
row, with no tuning run.
}
\description{
\code{nested_fit_resamples()} runs the outer loop of a nested design for a
workflow whose parameters are all fixed: for each outer fold it fits the
workflow on the fold's analysis set and scores it on the assessment set
with \code{\link[tune:last_fit]{tune::last_fit()}}, and it skips the inner stage entirely, since there
is nothing to search. It is \code{\link[=nested_tune_grid]{nested_tune_grid()}} with the inner tuner
removed, and the sixth member of that family: the loop, the seeds, the
results object and its methods are the same, so a fixed workflow and a
tuned one score on identical outer folds and their per-fold metrics join by
fold label. The grid function's help page is the reference for everything
the orchestrators share -- what a failed fold records, how the folds run in
parallel, and what an operation on the result may do.
}
\details{
Use it for the baseline a tuned procedure is compared against. A plain
\code{rset} of outer folds is what \code{\link[tune:fit_resamples]{tune::fit_resamples()}} already serves; what
this function adds is the same \emph{nested} design, so the two runs' folds are
the same rows. The record says no tuning ran: the \code{procedure} record names
the tuner \code{"fit_resamples"} and carries no \code{grid}, \code{param_info} or
\code{select}, and every completed fold's \code{.selected} is an empty table.

A workflow that still carries a \code{\link[tune:tune]{tune::tune()}} marker is refused at entry,
with condition class \code{nestedtune_tuned_workflow}, naming the five
orchestrators that tune; and each of those refuses a workflow with no
marker, with class \code{nestedtune_untuned_workflow}, naming this one. One
path for a fixed model, one for a tuned one.
}
\section{Reproducibility}{


The seed contract is \code{\link[=nested_tune_grid]{nested_tune_grid()}}'s: seed the session before the
call, there is no \code{seed} argument, \code{2 * n} seeds are drawn in one
\code{sample.int(.Machine$integer.max, 2 * n)} call on entry, and fold \code{i} is
assigned element \code{2 * i - 1} as its tuning seed and element \code{2 * i} as its
outer-fit seed. The same draw is made as on a tuned run, so a tuned run
under the same session seed on the same design shares each fold's
outer-fit seed with this one, and the record keeps one layout across the
six orchestrators. The tuning seed is drawn and consumed by nothing: there
is no inner stage for it to seed, and it is recorded as drawn rather than
as \code{NA} so the two seed columns read the same on every result. Fold \code{i}
is exactly:

\if{html}{\out{<div class="sourceCode">}}\preformatted{set.seed(res$.outer_fit_seed[[i]], kind = "Mersenne-Twister",
         normal.kind = "Inversion", sample.kind = "Rejection")
last_fit(object, resamples$splits[[i]], metrics = metrics,
         eval_time = eval_time,
         control = control_last_fit(event_level = event_level))
}\if{html}{\out{</div>}}

and \code{res$.tuning_seed[[i]]} appears in no line of it.

The caller's RNG state and generator kind are restored on exit, including
when the call errors. The same seed gives the same result serially and in
parallel, at any number of daemons.
}

\section{Differences from calling tune directly}{


There is no \code{control} formal, but a \code{\link[tune:control_resamples]{tune::control_resamples()}} passed
through \code{...} as \code{control} is recorded, and two of its slots reach the
outer fit. What is recorded is the control passed, or tune's default when
none is, with the slots this package forces overwritten, as
\code{extract_procedure(res)$control}. tune gives \code{control_resamples()} and
\code{\link[tune:control_grid]{tune::control_grid()}} one class, so either is accepted here as the same
object. Every slot of \code{control_resamples()} falls under one of seven
headings; most fall under the last, because the one tune call a fold makes
here is the outer scoring fit, whose own control this package builds.

\strong{Forced: \code{allow_par}.} The outer fit runs at \code{allow_par = FALSE},
whatever the control carries. Parallelism belongs over the outer folds,
and leaving that to a caller would put two pools in contention.

\strong{Settable as its own argument: \code{event_level}.} As on
\code{\link[=nested_tune_grid]{nested_tune_grid()}}: the argument is the one place the level is set, a
control left at tune's default takes it, and a control naming a level that
is neither tune's default nor the argument's is refused at entry, naming
both. \code{eval_time} is offered the same way.

\strong{Refused: none.} No slot is refused on its own. What is refused at entry
is a control of another class -- a \code{control_bayes()} -- and the
\code{event_level} conflict above.

\strong{Passed through: none.} There is no inner tuning call for a slot to be
passed through to.

\strong{Kept from the outer fit: \code{save_pred}, \code{extract}.} As on
\code{\link[=nested_tune_grid]{nested_tune_grid()}}: with \code{save_pred = TRUE} the result carries a
\code{.predictions} list column, each completed fold's predictions on its
assessment rows as \code{\link[tune:last_fit]{tune::last_fit()}} returns them; with \code{extract} a
function, an \code{.extracts} list column, the function's value on each
completed fold's fitted workflow. Here there is no inner run whose
predictions and extracts are discarded; the outer fit's are the only ones,
and they are still discarded on a run that did not ask.

\strong{Not returned: none.} Nothing an inner run would have saved exists to be
withheld.

\strong{Inert: \code{verbose}, \code{pkgs}, \code{save_workflow}, \code{parallel_over},
\code{backend_options}, \code{workflow_size}.} Each governs an inner tuning call
this function never makes. The outer fit runs under
\code{\link[tune:control_last_fit]{tune::control_last_fit()}} at the level and parallelism above, which
reads none of these; the workflow's packages are required at entry, and
the fitted workflow is reached through \code{extract}. Nothing that changes a
reported number is left to a slot listed here.
}

\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

# A fixed workflow: two components, no tune() marker.
rec <- recipes::step_pca(
  recipes::recipe(mpg ~ ., data = mtcars),
  recipes::all_predictors(),
  num_comp = 2L
)
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 3),
  inside = rsample::vfold_cv(v = 3)
)

set.seed(2)
res <- nested_fit_resamples(wf, folds)
collect_metrics(res)

# The record says no tuning ran.
extract_procedure(res)$tuner
res$.selected[[1]]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nested_tune_grid]{nested_tune_grid()}}, \code{\link[=nested_resamples]{nested_resamples()}}, \code{\link[=nested_final_fit]{nested_final_fit()}},
\code{\link[tune:fit_resamples]{tune::fit_resamples()}}
}
