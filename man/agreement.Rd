% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-results-agreement.R
\name{agreement}
\alias{agreement}
\title{Tabulate how often each parameter setting was selected across the outer folds}
\usage{
agreement(x, ...)
}
\arguments{
\item{x}{A \code{nested_results} from \code{\link[=nested_tune_grid]{nested_tune_grid()}} or one of its
siblings.}

\item{...}{Not used; must be empty. An argument passed here is an error
rather than silently ignored.}
}
\value{
A tibble with one column per parameter any completed fold selected,
then \code{n}, the number of completed folds that chose that combination, and
\code{prop}, \code{n} over the number of completed folds. Rows run from the largest
\code{n} down, ties in the order the combination first appears.
}
\description{
\code{agreement()} tells you how often the outer folds agreed on what to
select. Each outer fold tunes on its own inner resamples and selects one
candidate, one parameter setting. \code{agreement()} counts those selections:
one row per distinct combination of selected parameter values, most
frequent first.

The most frequent combination is not the final model's parameters. The
tuning procedure is tune then select, and the folds say how stable its
choice is. The model to deploy comes from \code{\link[=nested_final_fit]{nested_final_fit()}}, which
runs the procedure once more on the whole dataset and selects for
itself.
}
\section{How the folds are counted}{


Failed folds are left out, so \code{sum(n)} counts the completed folds whenever
the table has rows, each counted once. A run with some folds failed is
tabulated over the rest, with a warning saying so. A run in which none
completed is an error of class \code{nestedtune_no_completed_folds}, as it is
for \code{\link[=collect_metrics]{collect_metrics()}}, \link[=autoplot.nested_results]{autoplot()} and
\code{\link[=nested_final_fit]{nested_final_fit()}}.

Looking for tune's \code{.config}? It is not a column here: it labels a
candidate inside one fold's own tuning run, and folds can differ in grid.
}

\section{Missing and colliding values}{


A completed fold whose selection carries no value for a parameter is counted
under \code{NA} for it, in the same row as a fold that selected \code{NA}.
\code{\link[=summary.nested_results]{summary.nested_results()}} tells the two apart. A workflow with nothing to
tune gives columns \code{n} and \code{prop} and no rows. A parameter whose id is \code{n}
or \code{prop} would collide with the counts and is an error.
}

\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

rec <- recipes::recipe(mpg ~ ., data = mtcars) |>
  recipes::step_pca(recipes::all_predictors(), num_comp = tune::tune())
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 2),
  inside = rsample::vfold_cv(v = 2)
)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
set.seed(2)
res <- nested_tune_grid(wf, folds, grid = data.frame(num_comp = 1:2))
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
agreement(res)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=summary.nested_results]{summary.nested_results()}}, \code{\link[=autoplot.nested_results]{autoplot.nested_results()}},
\code{\link[=nested_final_fit]{nested_final_fit()}}, \code{\link[=summary.nested_results_set]{summary.nested_results_set()}} for a workflow-set
run
}
