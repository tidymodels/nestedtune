% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-results-collect.R
\name{compute_metrics.nested_results}
\alias{compute_metrics.nested_results}
\title{Score the saved predictions of a nested run with a metric set}
\usage{
\method{compute_metrics}{nested_results}(x, metrics, ..., summarize = TRUE, event_level = NULL)
}
\arguments{
\item{x}{A \code{nested_results} run whose control set \code{save_pred = TRUE}.}

\item{metrics}{A \code{\link[yardstick:metric_set]{yardstick::metric_set()}}.}

\item{...}{Not used. It must be empty.}

\item{summarize}{Whether to average the per-fold metrics (\code{TRUE}, the
default) or return them one row per outer fold (\code{FALSE}).}

\item{event_level}{For a two-class outcome, \code{"first"} or \code{"second"}: which
level is the event. The default, \code{NULL}, takes the level the run
recorded, so the new metrics treat the same level as the event.}
}
\value{
A tibble in the shapes \code{\link[=collect_metrics]{collect_metrics()}} returns.
}
\description{
\code{compute_metrics()} scores the outer-fold predictions a run kept under
\code{save_pred = TRUE} with a metric set you give it, and summarizes them the
way \code{\link[=collect_metrics]{collect_metrics()}} does. You can report a metric the run did not
compute without running the nested loop again.
}
