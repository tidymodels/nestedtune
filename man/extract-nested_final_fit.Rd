% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-final-fit.R
\name{extract-nested_final_fit}
\alias{extract-nested_final_fit}
\alias{extract_fit_parsnip.nested_final_fit}
\alias{extract_fit_engine.nested_final_fit}
\alias{extract_recipe.nested_final_fit}
\alias{extract_mold.nested_final_fit}
\alias{extract_preprocessor.nested_final_fit}
\alias{extract_spec_parsnip.nested_final_fit}
\alias{outcome_names.nested_final_fit}
\title{Extract the parts of a final fit's workflow}
\usage{
\method{extract_fit_parsnip}{nested_final_fit}(x, ...)

\method{extract_fit_engine}{nested_final_fit}(x, ...)

\method{extract_recipe}{nested_final_fit}(x, ..., estimated = TRUE)

\method{extract_mold}{nested_final_fit}(x, ...)

\method{extract_preprocessor}{nested_final_fit}(x, ...)

\method{extract_spec_parsnip}{nested_final_fit}(x, ...)

\method{outcome_names}{nested_final_fit}(x, ...)
}
\arguments{
\item{x}{A \code{nested_final_fit} from \code{\link[=nested_final_fit]{nested_final_fit()}}.}

\item{...}{Must be empty. The methods for a workflow ignore an argument
they do not know, so these refuse it rather than pass it on.}

\item{estimated}{For \code{extract_recipe()}, whether to return the trained
recipe (\code{TRUE}, the default) or the recipe as it was given.}
}
\value{
What the same call returns for the trained workflow.
}
\description{
These methods reach the fitted model, the preprocessor and the outcome
names of the workflow that \code{\link[=nested_final_fit]{nested_final_fit()}} trained. Each one gives
the same answer as the same call on \code{\link[=extract_workflow]{extract_workflow()}}'s output.
}
\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

rec <- recipes::recipe(mpg ~ ., data = mtcars) |>
  recipes::step_pca(recipes::all_predictors(), num_comp = tune::tune())
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 2),
  inside = rsample::vfold_cv(v = 2)
)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
set.seed(2)
res <- nested_tune_grid(wf, folds, grid = data.frame(num_comp = 1:2))
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
set.seed(3)
final <- nested_final_fit(wf, res)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
extract_fit_parsnip(final)
extract_recipe(final)
outcome_names(final)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=extract_workflow]{extract_workflow()}}, \code{\link[=extract_tune_results]{extract_tune_results()}}
}
