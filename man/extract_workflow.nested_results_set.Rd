% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-results-set.R
\name{extract_workflow.nested_results_set}
\alias{extract_workflow.nested_results_set}
\title{Extract one workflow of a workflow-set run}
\usage{
\method{extract_workflow}{nested_results_set}(x, id, ...)
}
\arguments{
\item{x}{A \code{nested_results_set} from \code{\link[=nested_workflow_map]{nested_workflow_map()}}.}

\item{id}{The \code{wflow_id} of the workflow to return, one of \code{x$wflow_id}.}

\item{...}{Not used; must be empty.}
}
\value{
The workflow, untrained, as the set held it. An \code{id} naming no
row of the set is refused with class \code{nestedtune_unknown_id}.
}
\description{
Extract one workflow of a workflow-set run
}
\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick", "workflowsets"))) withAutoprint(\{ # examplesIf}
data(mtcars)
rec <- recipes::recipe(mpg ~ ., data = mtcars)
wset <- workflowsets::workflow_set(
  preproc = list(none = rec),
  models = list(lm = parsnip::linear_reg())
)
set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 3),
  inside = rsample::vfold_cv(v = 3)
)
set.seed(2)
res <- nested_workflow_map(wset, resamples = folds)
extract_workflow(res, "none_lm")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nested_workflow_map]{nested_workflow_map()}}, \code{\link[=nested_final_fit]{nested_final_fit()}}
}
