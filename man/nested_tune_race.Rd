% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-tune-race.R
\name{nested_tune_race}
\alias{nested_tune_race}
\alias{nested_tune_race_anova}
\alias{nested_tune_race_anova.default}
\alias{nested_tune_race_anova.model_spec}
\alias{nested_tune_race_anova.workflow}
\alias{nested_tune_race_win_loss}
\alias{nested_tune_race_win_loss.default}
\alias{nested_tune_race_win_loss.model_spec}
\alias{nested_tune_race_win_loss.workflow}
\title{Nested cross-validation with racing inside}
\usage{
nested_tune_race_anova(object, ...)

\method{nested_tune_race_anova}{default}(object, ...)

\method{nested_tune_race_anova}{model_spec}(
  object,
  preprocessor,
  resamples,
  ...,
  param_info = NULL,
  grid = 10,
  metrics = NULL,
  event_level = "first",
  eval_time = NULL,
  select = selection_rule()
)

\method{nested_tune_race_anova}{workflow}(
  object,
  resamples,
  ...,
  param_info = NULL,
  grid = 10,
  metrics = NULL,
  event_level = "first",
  eval_time = NULL,
  select = selection_rule()
)

nested_tune_race_win_loss(object, ...)

\method{nested_tune_race_win_loss}{default}(object, ...)

\method{nested_tune_race_win_loss}{model_spec}(
  object,
  preprocessor,
  resamples,
  ...,
  param_info = NULL,
  grid = 10,
  metrics = NULL,
  event_level = "first",
  eval_time = NULL,
  select = selection_rule()
)

\method{nested_tune_race_win_loss}{workflow}(
  object,
  resamples,
  ...,
  param_info = NULL,
  grid = 10,
  metrics = NULL,
  event_level = "first",
  eval_time = NULL,
  select = selection_rule()
)
}
\arguments{
\item{object}{A \code{\link[workflows:workflow]{workflows::workflow()}} with at least one parameter marked
for tuning with \code{\link[tune:tune]{tune::tune()}}, or a parsnip model specification with
such a parameter, given with \code{preprocessor}. A workflow with no marker is
refused, and \code{\link[=nested_fit_resamples]{nested_fit_resamples()}} scores one on the same design.}

\item{...}{On the generic, the arguments of the method that \code{object}
selects. On a method, a control object from \code{\link[finetune:control_race]{finetune::control_race()}}, as
\code{control}, and nothing else. Every argument after \code{...} is matched by
name. The section on differences from finetune says what becomes of
each slot.}

\item{preprocessor}{A formula or a recipe, when \code{object} is a model
specification. The two are combined as
\code{workflows::workflow(preprocessor, object)}, and the call runs as it
does on that workflow. Other preprocessors, such as
\code{\link[workflows:workflow_variables]{workflows::workflow_variables()}}, go through a workflow. A workflow
already carries its preprocessor, so one given beside a workflow is
refused.}

\item{resamples}{A nested resampling design from \code{\link[=nested_resamples]{nested_resamples()}} or
\code{\link[rsample:nested_cv]{rsample::nested_cv()}}, one row per outer fold. The section on nested
designs says what the design must hold.}

\item{param_info}{A \code{\link[dials:parameters]{dials::parameters()}} object or \code{NULL}. If none is given,
a parameters set is derived from other arguments. Passing this argument can
be useful when parameter ranges need to be customized.}

\item{grid}{A data frame of tuning combinations or a positive integer. The
data frame should have columns for each parameter being tuned and rows for
tuning parameter candidates. An integer denotes the number of candidate
parameter sets to be created automatically.}

\item{metrics}{A \code{\link[yardstick:metric_set]{yardstick::metric_set()}}, or \code{NULL} for tune's default
set. Under the \code{"best"}, \code{"one_std_err"} and \code{"pct_loss"} rules of
\code{\link[=selection_rule]{selection_rule()}}, the first metric in the set chooses each fold's
candidate. The \code{"desirability"} rule chooses by its goals instead.
\code{\link[=nested_tune_grid]{nested_tune_grid()}} and \code{\link[=nested_tune_bayes]{nested_tune_bayes()}} accept that rule, and the
racing and annealing tuners refuse it. \code{\link[=nested_tune_bayes]{nested_tune_bayes()}},
\code{\link[=nested_tune_race_anova]{nested_tune_race_anova()}}, \code{\link[=nested_tune_race_win_loss]{nested_tune_race_win_loss()}} and
\code{\link[=nested_tune_sim_anneal]{nested_tune_sim_anneal()}} also steer their inner search on the first
metric. The outer loop scores every metric in the set, and
\code{\link[=collect_metrics]{collect_metrics()}} reports each one.

So the order of the set matters. If the first metric is not the one you
read, the run chooses under one loss and assesses under another. For
example, \code{metric_set(mae, rmse)} chooses by \code{mae} and also reports
\code{rmse}. Stone (1974, p. 116) says that the two losses need not match.
A set that tune cannot use for the model's mode, such as a
classification metric on a regression model, is refused before any
fold runs.}

\item{event_level}{\code{"first"} (the default) or \code{"second"}. It names which
level of a two-class outcome is the event. It applies to the inner tuning run and the outer scoring fit alike.}

\item{eval_time}{A numeric vector of evaluation times for a censored
regression model, or \code{NULL} (the default) to leave the choice to tune.
The section on evaluation times says what this package refuses.}

\item{select}{A \code{\link[=selection_rule]{selection_rule()}} naming the selector each outer fold
picks its candidate with, on its own inner run. The default is
\code{\link[tune:select_best]{tune::select_best()}} on the first metric. \code{\link[=nested_tune_race_anova]{nested_tune_race_anova()}},
\code{\link[=nested_tune_race_win_loss]{nested_tune_race_win_loss()}} and \code{\link[=nested_tune_sim_anneal]{nested_tune_sim_anneal()}} refuse the
\code{"desirability"} rule.}
}
\value{
A \code{nested_results} with one row per outer fold and the columns
\code{\link[=nested_tune_grid]{nested_tune_grid()}} documents. The \code{procedure} record names the tuner,
\code{"tune_race_anova"} or \code{"tune_race_win_loss"}, and holds the \code{grid}
beside the arguments every orchestrator records. The section below
says what \code{.inner_metrics} and the recorded grid mean on a race.
}
\description{
\code{nested_tune_race_anova()} and \code{nested_tune_race_win_loss()} give you an
honest score for a model you tune with finetune's two racing tuners,
\code{\link[finetune:tune_race_anova]{finetune::tune_race_anova()}} and \code{\link[finetune:tune_race_win_loss]{finetune::tune_race_win_loss()}}. Each
is \code{\link[=nested_tune_grid]{nested_tune_grid()}} with the inner tuner swapped, and that page is
the reference for everything the racers share with the other
orchestrators. For each outer fold the race scores every candidate in
\code{grid} on the first \code{burn_in} inner resamples. It then drops the
candidates that are already clearly worse than the best, by a
repeated-measures ANOVA or by a Bradley-Terry model of pairwise wins and
losses. The survivors are scored on the remaining resamples, and the
fold then selects, finalizes, fits and scores on the outer split as the
grid page describes.

The estimate describes the race-and-fit procedure as a whole. The model
to deploy comes from \code{\link[=nested_final_fit]{nested_final_fit()}}, which races the same grid once
more on all the data, and the estimate is the number to report for that
model.
}
\details{
Both functions need finetune installed. \code{nested_tune_race_anova()} also
needs lme4, which fits the ANOVA, and \code{nested_tune_race_win_loss()}
BradleyTerry2, which fits the win/loss model. A missing package is refused
at entry, before any fold runs.

\code{grid} is the design the race is offered. A data frame must have one
column per tuned parameter and no other column.

\code{param_info} reaches the inner call as \code{\link[tune:tune_grid]{tune::tune_grid()}} takes it. The
section on finalizing a parameter range says where a range that depends
on the data is finalized.
}
\section{What a race records}{


Each fold's \code{.inner_metrics} holds every candidate its race scored,
eliminated candidates included: \verb{tune::collect_metrics(<the race>, all_configs = TRUE)}, where finetune's own default keeps the survivors
alone. In that table \code{n} is the number of inner resamples each candidate
was scored on. A candidate that survived to the end has the full inner
resample count, and one eliminated along the way has fewer. The recorded
\code{grid}, in the \code{procedure} record and as \code{attr(x, "grid")}, is the
design the race was offered, exactly as given. What each candidate ran
is \code{n}. A candidate that failed on every inner resample is absent, and
its failure is in \code{.notes}.

A race draws from the generator even with a deterministic engine. With
\code{randomize = TRUE}, finetune's default, the inner resamples are shuffled
before the burn-in. So which resamples the burn-in uses, and with it
which candidates are eliminated when, comes from the fold's tuning seed.
On the parallel path every daemon's library must hold finetune. The loop
attaches it in each daemon before the first fold is sent, and warns
where it cannot.
}

\section{Weighting the outer folds}{


Weights set on the design with \code{\link[tune:add_resample_weights]{tune::add_resample_weights()}} reach the
outer average. \code{collect_metrics()}, \code{summary()}, \code{autoplot()} and
\code{compute_metrics()} then report the weighted mean of the folds'
estimates. Their \code{std_err} is the weighted standard deviation over the
square root of the effective sample size, as tune computes both. The
per-fold table from \code{collect_metrics(summarize = FALSE)} carries each
fold's weight in a \code{.weight} column. tune stores the weights scaled to
sum to one, and stores none when they are equal, so equal weights give
the unweighted average.

A fold that fails, or that scores \code{NA} on a metric, is left out of that
average and the remaining weights are scaled up to sum to one. \code{n} counts
the folds that scored. The weights on a design's inner resamples are read
by tune itself.
}

\section{Reproducibility}{


Seed the session before the call, as elsewhere in tidymodels. There is no
\code{seed} argument. On entry the function draws \code{2 * n} seeds in a single
\code{sample.int(.Machine$integer.max, 2 * n)} call, where \code{n} is the number of
outer folds. Fold \code{i} uses element \code{2 * i - 1} for its tuning step and
element \code{2 * i} for its outer fit, each applied with the generator kind
pinned. A fold's seed depends on its position, not on the order the folds
run in. So the same seed gives the same result serially and in parallel,
at any number of daemons. The two seeds are kept on the result as
\code{.tuning_seed} and \code{.outer_fit_seed}, and \code{?nested_tune_grid} shows how to
reproduce one fold by hand from them.

The caller's RNG state and generator kind are restored on exit, including
when the call errors, so a seeded script that draws afterwards is
unaffected. One consequence: two consecutive calls with no \code{set.seed()}
between them return identical results, exactly as repeated
\code{\link[tune:tune_grid]{tune::tune_grid()}} calls do.

This binds randomness that flows through R's generator. Engines that
randomize outside it (kernlab's SVMs, the deep-learning engines) cannot be
pinned by any R-side scheme, here or in tune.
}

\section{Differences from calling finetune directly}{


There is no \code{control} formal. A \code{\link[finetune:control_race]{finetune::control_race()}} passed
through \code{...} as \code{control} reaches the inner race in every fold and the
final fit that re-runs the result. \code{control = control_race(burn_in = 2)}, say, fits a design with three inner resamples. What runs is the
control passed, or finetune's default when none is, with the slots this
package forces overwritten. The result records that effective control
as \code{extract_procedure(res)$control}. Every slot of \code{control_race()}
falls under one of seven headings.


\strong{Forced: \code{allow_par}.} The inner race and the outer scoring fit both
run at \code{allow_par = FALSE}, whatever the control carries, because
parallelism belongs over the outer folds.


\strong{Settable as its own argument: \code{event_level}.} The argument is the one
place the level is set, as on the grid page. A control at finetune's
default takes it, and a control naming another level is refused at
entry, with a refusal that names both.
\code{grid} and \code{eval_time} are the racing functions' own arguments rather than control slots, offered here as arguments and reaching them unchanged.


\strong{Refused: none.} No slot is refused on its own. Three things are
refused at entry. The first is a control of another class, such as a
\code{control_grid()} that finetune itself accepts here. The second is
the \code{event_level} conflict above, and the third a \code{burn_in} no fold's
inner design can meet. finetune refuses
a race whose resample count is not greater than \code{burn_in}. This package
refuses the whole call before any fold runs when any outer fold's inner
\code{rset} meets that condition, and the refusal names the count and the
burn-in.
\code{control_race()} defaults \code{burn_in} to 3, so a design with three inner
resamples needs \code{control = control_race(burn_in = 2)} or fewer.

\strong{Passed through: \code{burn_in}, \code{alpha}, \code{num_ties}, \code{randomize},
\code{verbose_elim}, \code{verbose}, \code{pkgs}, \code{parallel_over}, \code{workflow_size}.}
Each reaches the race as given:
\itemize{
\item \code{burn_in}, \code{alpha}, \code{num_ties} and \code{randomize} govern each fold's race
as they do in a direct call. \code{burn_in} is how many resamples every
candidate is scored on before elimination starts. \code{alpha} is the
significance level an elimination needs. \code{num_ties} is how many rounds
two tied survivors are given before one is dropped. \code{randomize} is
whether the resamples are shuffled first.
\item \code{verbose_elim} prints finetune's elimination log from a serial run,
once per fold, and from a mirai daemon where nothing shows it.
\code{verbose} likewise.
}


\code{pkgs}, \code{parallel_over} and \code{workflow_size} behave as the grid page
describes. \code{parallel_over} changes the numbers a stochastic engine
produces even at \code{allow_par = FALSE}.


This classification was read on finetune 1.3.0. The version that added
\code{workflow_size} to \code{control_race()} is not named in finetune's NEWS,
and the \verb{>= 1.0.1} floor this package declares does not require it.


\strong{Kept from the outer fit: \code{save_pred}, \code{extract}.} Each reaches the
outer fit as well as the inner race. The outer fit's
predictions and extracts are kept as \code{.predictions} and \code{.extracts}, as
the grid page describes, and the inner race's own are still discarded.


\strong{Not returned: \code{save_workflow}.} It lands on the inner \code{tune_results}
a fold record discards, so setting it costs the work and returns
nothing. The final fit keeps its own tuning run as \verb{$tuning}, where what
it saved is reachable.


\strong{Inert: \code{backend_options}.} Options for a parallel backend, with no
backend to reach at \code{allow_par = FALSE}.
}

\section{Nested designs}{


\code{resamples} is a data frame with one row per outer fold. Its \code{splits}
column holds that fold's \code{rsplit}, its \code{inner_resamples} column holds an
\code{rset} with at least one row, and every other column labels the fold. A
label column is named \code{id}, or \code{id} followed by a digit from 1 to 9, and
holds character or factor values. Together the label columns give every
outer fold a distinct label with no \code{NA}.

Inside each inner \code{rset}, every element of \code{splits} is an \code{rsplit}. All
of a fold's inner splits carry one data frame: either the outer split's
own frame, as \code{\link[=nested_resamples]{nested_resamples()}} builds, or that split's analysis set,
as \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds. An inner split carrying the outer frame
must index only rows the outer split's \code{in_id} holds, in its \code{in_id} and
any non-\code{NA} \code{out_id}. So no inner analysis or assessment set reaches a
row the outer fold holds out.

A design breaking any of this, or using a bootstrap for the outer loop, is
refused before anything is fitted. The error has condition class
\code{nestedtune_bad_design} and names every offending row, column, inner
split or index. The checks exist because \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds a
design whatever its \code{inside} argument returned, and because a design
assembled by hand can index rows its outer fold never sees.

Time-series designs are supported for an outer
\code{\link[rsample:rolling_origin]{rsample::rolling_origin()}}, \code{\link[rsample:sliding_window]{rsample::sliding_window()}},
\code{\link[rsample:sliding_index]{rsample::sliding_index()}} or \code{\link[rsample:sliding_period]{rsample::sliding_period()}} with an inner
\code{\link[rsample:rolling_origin]{rsample::rolling_origin()}}. \code{\link[=nested_tune_grid]{nested_tune_grid()}} and
\code{\link[=nested_fit_resamples]{nested_fit_resamples()}} are tested on all four, and so is
\code{\link[=nested_final_fit]{nested_final_fit()}} for their results. \code{\link[=nested_tune_bayes]{nested_tune_bayes()}},
\code{\link[=nested_tune_race_anova]{nested_tune_race_anova()}}, \code{\link[=nested_tune_race_win_loss]{nested_tune_race_win_loss()}} and
\code{\link[=nested_tune_sim_anneal]{nested_tune_sim_anneal()}} are tested on the rolling-origin design, and
so is \code{\link[=nested_final_fit]{nested_final_fit()}} for their results, plus the sliding-window
design for \code{\link[=nested_tune_bayes]{nested_tune_bayes()}} results. \code{\link[=nested_workflow_map]{nested_workflow_map()}} is
tested on the rolling-origin design. The outer design never reaches the
tuner: each tuner is handed one outer fold's inner resamples at a time.
An inner design other than \code{\link[rsample:rolling_origin]{rsample::rolling_origin()}} is not tested.
\code{\link[=augment.nested_results]{augment()}} refuses these designs, because their
assessment sets leave rows out. When the assessment sets overlap, its
error also counts the rows held out more than once.
}

\section{Finalizing a parameter range}{


\code{param_info} is passed unchanged to the inner tuning call on every outer
fold, so a restricted range restricts what every fold searches. Some
ranges are unknown until the data is seen: \code{mtry()}, or a \code{min_n()}
finalized by row count. tune finalizes such a range on the outer fold's
analysis rows, never on the rows that fold holds out. On a
\code{\link[=nested_resamples]{nested_resamples()}} design the inner call therefore receives the fold's
inner resamples re-pointed at its analysis set, not the design's own
\code{inner_resamples} element, which indexes the whole data. A design from
\code{\link[rsample:nested_cv]{rsample::nested_cv()}} already carries the analysis set and is passed as
it is. So is the design's element under an outer split that repeats a
row, an evaluated \code{\link[rsample:manual_rset]{rsample::manual_rset()}}, where the re-pointing is
ambiguous. \code{\link[=nested_final_fit]{nested_final_fit()}} finalizes on the full data.
}

\section{Evaluation times}{


\code{eval_time} reaches every tune call whose answer depends on it. So a
dynamic or integrated survival metric, \code{brier_survival()},
\code{roc_auc_survival()} and their relatives, is measured at the times you
name. When the metric set has no metric that reads it, tune ignores it
with a warning. tune keys that warning on the metrics, not on the
model's mode.

Refused here, ahead of tune: anything that is not numeric, an empty
vector, and any element that is missing, negative or not finite. tune
treats those unevenly, and only once a metric reads the
times, so they are refused at entry, before a whole run is paid for.
Zero, repeated times and times out of order are accepted and passed on
untouched, because tune normalizes those itself. A repeated time draws
tune's warning that 0 inappropriate evaluation time points were removed,
once per tune call.

The selection rule is applied without \code{eval_time}. Left unset, it
selects at the first of the evaluation times the tuning run was built
with, which are the ones named here. Passing them again changes no
choice, and repeats tune's message about which time it took.
}

\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

rec <- recipes::recipe(mpg ~ ., data = mtcars) |>
  recipes::step_pca(recipes::all_predictors(), num_comp = tune::tune())
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 2),
  inside = rsample::vfold_cv(v = 2)
)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("finetune", "lme4", "recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
# A race needs more inner resamples than its burn-in, so five inner folds.
set.seed(1)
folds5 <- nested_resamples(mtcars, outside = rsample::vfold_cv(v = 2),
                           inside = rsample::vfold_cv(v = 5))

set.seed(2)
res <- nested_tune_race_anova(
  wf,
  folds5,
  grid = data.frame(num_comp = 1:4),
  control = finetune::control_race(burn_in = 2, verbose_elim = FALSE)
)
collect_metrics(res)

# Every candidate the first fold's race scored, and on how many inner
# resamples: `n` below 5 is a candidate the race eliminated.
res$.inner_metrics[[1]]
\dontshow{\}) # examplesIf}
}
\references{
Stone, M. (1974). Cross-validatory choice and assessment of statistical
predictions. \emph{Journal of the Royal Statistical Society, Series B}, 36(2),
111–147.
}
\seealso{
\code{\link[=nested_tune_grid]{nested_tune_grid()}}, \code{\link[=nested_resamples]{nested_resamples()}}, \code{\link[=nested_final_fit]{nested_final_fit()}},
\code{\link[=nested_tune_bayes]{nested_tune_bayes()}}, \code{\link[finetune:tune_race_anova]{finetune::tune_race_anova()}}, \code{\link[finetune:tune_race_win_loss]{finetune::tune_race_win_loss()}}
}
