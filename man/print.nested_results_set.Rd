% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-results-set.R
\name{print.nested_results_set}
\alias{print.nested_results_set}
\title{Print a workflow-set run}
\usage{
\method{print}{nested_results_set}(x, ...)
}
\arguments{
\item{x}{A \code{nested_results_set} from \code{\link[=nested_workflow_map]{nested_workflow_map()}}.}

\item{...}{Not used; must be empty.}
}
\value{
\code{x}, unchanged and invisible.
}
\description{
Shows the orchestrator the set ran through and how many workflows it holds,
then one line per workflow: its id, how many of its outer folds completed,
and the procedure that ran for it.
}
\examples{
\dontshow{if (rlang::is_installed(c("recipes", "yardstick"))) withAutoprint(\{ # examplesIf}
data(mtcars)

rec <- recipes::recipe(mpg ~ ., data = mtcars) |>
  recipes::step_pca(recipes::all_predictors(), num_comp = tune::tune())
wf <- workflows::workflow(rec, parsnip::linear_reg())

set.seed(1)
folds <- nested_resamples(
  mtcars,
  outside = rsample::vfold_cv(v = 2),
  inside = rsample::vfold_cv(v = 2)
)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick", "workflowsets"))) withAutoprint(\{ # examplesIf}
# One tuned workflow and one baseline, on the same nested design.
wset <- workflowsets::workflow_set(
  preproc = list(pca = rec, none = recipes::recipe(mpg ~ ., data = mtcars)),
  models = list(lm = parsnip::linear_reg())
)

set.seed(2)
res <- nested_workflow_map(wset, resamples = folds, grid = data.frame(num_comp = 1:2))
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "yardstick", "workflowsets"))) withAutoprint(\{ # examplesIf}
print(res)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nested_workflow_map]{nested_workflow_map()}}, \code{\link[=collect_metrics.nested_results_set]{collect_metrics.nested_results_set()}},
\code{\link[=summary.nested_results_set]{summary.nested_results_set()}} for what the run means
}
